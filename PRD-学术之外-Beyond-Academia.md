# 产品需求文档 · 学术之外（Beyond Academia）

---

## 模块 1：文档信息

### 1.1 文档元数据

| 字段 | 内容 |
|---|---|
| 文档名称 | 学术之外（Beyond Academia）产品需求文档 |
| 版本号 | V0.1 |
| 状态 | 草稿 |
| 起草人 | nadine |
| 审批人 | （待定） |
| 创建日期 | 2026-07-12 |
| 最后更新 | 2026-07-12 |

> **[PM 笔记] 为什么版本号用 V0.1 而不是 V1.0？**
>
> 版本号约定：草稿阶段用 `0.x`，表示"我还在写，任何人看到都不要当成最终结论"。当文档走完评审、获得审批人签字后，版本号升为 `V1.0`。
>
> 这样约定的好处：工程师瞥一眼版本号就能立刻判断"这份文档能不能拿来排期"——V0.x 不行，V1.x 才行。这比反复口头沟通"你看了吗？能开发了吗？"高效得多。
>
> **常见错误**：新手 PM 喜欢直接写 V1.0，觉得"显得正式"。但 V1.0 意味着"我已经和关键干系人对齐过了"，如果实际上并没有，团队照着开发后发现需求不对，PM 的公信力就受损了。**宁可版本号低一点，也不要假装已经对齐。**

### 1.2 版本历史

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| V0.1 | 2026-07-12 | nadine | 初稿创建，完成文档信息模块 |
| V0.2 | 2026-07-13 | nadine | 新增 §1.6~§1.9 技术栈约定、代码仓库、代码组织规范、开发环境部署 |
| V0.3 | 2026-07-13 | nadine | 新增模块 2（产品概述）与模块 3（市场背景与问题分析） |
| V0.4 | 2026-07-13 | nadine | 新增模块 4（用户画像：迷茫型小陈、纠结型小周、探索型小赵） |
| V0.5 | 2026-07-14 | nadine | 新增模块 6（产品架构总览）。职业分类定稿为 8 分类，统一评估输出与地图展示 |
| V0.6 | 2026-07-14 | nadine | 新增 §7.3~§7.7 全部功能详述（双层结构）。全文术语统一修正 |
| V0.7 | 2026-07-14 | nadine | 完成模块 7（§7.1-§7.7 共 7 个功能模块全部采用双层结构） |
| V0.8 | 2026-07-14 | nadine | 新增模块 8（算法与匹配模型）、模块 9（数据模型汇总）、模块 11（技术架构） |
| V0.9 | 2026-07-15 | nadine | 开发阻塞项补齐：18项价值观种子数据、Prisma Schema、API清单（附录A）、枚举字典（附录B）、DB索引（附录C）、统一 ?family→?category、修正作答页分步模式、V1占位级别L1/L2/L3标准 |

> **[PM 笔记] 为什么版本历史要用表格而不是一段话？**
>
> 三个原因：
> 1. **可追溯**：三个月后有人问"验收条件是什么时候加进来的？"翻这个表格就能定位到具体版本和修改人。
> 2. **控制范围蔓延**：每次修改都要留一条记录。这个行为本身会迫使 PM 自问"这个改动真的需要进 PRD 吗？"——它本质上是一个轻量级的变更控制机制。
> 3. **保护 PM**：如果开发说"你没说过要做这个"，你可以翻出 V0.4 的记录："2026-08-03 根据 XX 评审意见新增了价值观追问清单功能"。

### 1.3 审批记录

| 角色 | 姓名 | 审批日期 | 审批意见 |
|---|---|---|---|
| （待定） | — | — | — |

> **[PM 笔记] 审批记录为什么要独立于版本历史？**
>
> 版本历史回答"文档怎么变成今天这样的"，审批记录回答"谁在什么时间点拍了板"。它们是两个不同的问题。
>
> 当一份 PRD 经过了 5 轮修改才被批准，版本历史可能有 10 条记录，但审批记录只有 1 条。分开维护，让需要找"谁批准了这个功能"的人（比如法务、合规）不用在版本历史里翻。

### 1.4 术语约定

| 术语 | 定义 |
|---|---|
| PRD | Product Requirements Document，产品需求文档 |
| 职业分类（Category） | 博士毕业后可进入的 8 个职业大类，如"学术支撑""科技企业"等，同时用于职业地图展示和评估工具的输出维度 |
| 权重矩阵 | 由领域专家为每个"题目 × 职业分类"评定相关度（0-3）所形成的二维矩阵，是匹配算法的核心参数 |
| 决策工具 | 平台提供的三类核心分析工具：学术职位门槛评估、职位预警、博士非学术职业评估 |
| 匹配度 | 用户通过职业评估获得的与某个职业方向的适配百分比（0-100%） |
| 模块（Module） | 职业评估问卷的结构单元，共 A/B/C/D/E 五个模块，合计 87 题 |
| 专家权重矩阵 | 由领域专家为每个"题目 × 职业方向"评定相关度（0-3）所形成的二维矩阵，是匹配算法的核心参数 |

> **[PM 笔记] 为什么 PRD 开头就要定义术语？**
>
> 这份文档会被不同背景的人阅读：前端工程师、后端工程师、设计师、运营、甚至未来的新 PM。每个人对同一个词的理解可能完全不同——比如"模块"在设计师那里指的是页面区域，在工程师那里指的是代码拆分，在这份 PRD 里特指问卷结构单元。
>
> 术语约定做的是一个简单但关键的事：**在文档开头统一语言**。如果不做这一步，评审时会出现"我以为你说的是 A，结果你指的是 B"的无效争论。
>
> **判断标准**：如果一个词在 PRD 里出现超过 5 次，且可能产生歧义，就应该进术语表。

### 1.5 文档范围说明

本文档描述「学术之外」产品的**完整功能需求**，覆盖：

- 已有原型中的 6 大功能模块（职业地图、门槛评估、职位预警、职业评估、岗位精选、职业数据）
- 待开发的新功能模块（职业评估完整问卷、用户系统、管理后台）
- 核心算法与匹配模型
- 数据模型与接口约定
- 非功能需求与发布计划

**不覆盖的内容**：

- 品牌视觉规范（由设计师单独输出）
- 内容运营策略（由运营团队单独输出）
- 商业化方案（本阶段产品定位为非盈利工具，商业化另案处理）

> **[PM 笔记] 为什么需要"文档范围说明"？**
>
> PM 新手常犯的错误是把 PRD 写成"万能文档"——品牌、运营、商业化全都往里塞。结果是一份 50 页的文档，什么都说了但什么都没说清楚。
>
> 范围说明的价值在于**主动画线**：告诉读者"以下内容不在这里讨论"。这样做有两个好处：
> 1. 如果有人在评审时问"品牌色为什么不在这份文档里？"，你可以指向这一节——"品牌规范不在 PRD 范围内，设计师会单独输出"。
> 2. 约束自己——写的过程中想塞新内容时，先检查是否超出了本 PRD 的承诺范围。

### 1.6 技术栈约定

> **[PM 笔记] 为什么技术栈要写在 PRD 里？**
>
> 通常技术栈是技术负责人决定的，不属于 PRD 范围。但当 PM 是产品的 owner 且团队没有独立的技术负责人时，PM 必须主动锁定技术选型，否则开发过程中出现"前端用 Vue 还是 React"的争论会直接阻塞进度。提前锁定的本质不是 PM 替开发做技术决策，而是 **PM 基于产品特征选择最匹配的技术方向，减少无效争论**。
>
> 以下每一项选择都附有**决策理由**（为什么选这个）、**替代方案**（考虑过但放弃的其他选项）、**放弃原因**（为什么替代方案不匹配本项目）。

#### 1.6.1 前端技术栈

| 技术 | 选型 | 决策理由 | 替代方案 | 放弃原因 |
|---|---|---|---|---|
| 构建工具 | **Vite 5.x** | CRA 已停止维护；Vite 启动速度极快（<1s），迁移成本低 | Create React App | 已停更，社区不再支持 |
| | | | Next.js 14+ | SSR/SSG 能力本项目不需要，功能过剩，学习成本高 |
| UI 框架 | **React 18** | 现有原型已用 React，团队无需重新学习 | Vue 3 | 迁移成本太高，2742 行全部重写 |
| 类型系统 | **TypeScript 5.x** | 87 题问卷 + 权重矩阵数据模型复杂；前后端可共享类型定义，降低"字段名拼错"类 bug | JavaScript | 数据模型越复杂，JS 的弱类型越容易制造隐蔽 bug |
| 路由 | **react-router v6** | URL 可分享、浏览器前进/后退按钮可用、支持页面级埋点分析 | useState 手写路由 | 当前做法（一个 view 变量切换页面）让地址栏永远是 index.html |
| 状态管理 | **Zustand** | API 极简（一个 create 函数），体积 <1KB，V1 需要管理的全局状态不超过 5 个 | Redux Toolkit | 概念过多（store/reducer/middleware/selector），团队规模不匹配 |
| | | | React Context | 跨组件渲染性能不如 Zustand |
| CSS 方案 | **Tailwind CSS**（渐进采用） | 设计令牌可映射到 Tailwind config；原子类开发效率高；社区活跃 | 内联 style | 当前 2742 行混在 JSX 里，可维护性差 |
| | | | CSS Modules | 无法像 Tailwind 那样用 config 统一管理设计令牌 |
| 图表库 | **Recharts** | 现有原型已用，RadarChart/AreaChart/BarChart 满足所有可视化需求 | ECharts | 更重，Recharts 对 React 的天然亲和力更好 |
| 图标库 | **Lucide React** | 现有原型已用，图标集足够覆盖所有 UI 场景 | — | — |

#### 1.6.2 后端技术栈

| 技术 | 选型 | 决策理由 | 替代方案 | 放弃原因 |
|---|---|---|---|---|
| 运行时 | **Node.js 20 LTS** | 前后端同语言（TypeScript），类型定义可共享；匹配算法是 O(n) 级别纯算术，不需要 Python 的 NumPy | Python (FastAPI) | 前后端两种语言，类型无法共享，沟通成本高 |
| | | | Java (Spring Boot) | 代码量大，快速迭代阶段开发速度慢 |
| Web 框架 | **Fastify** | 比 Express 快 2-3 倍；内置 JSON Schema 校验，适合需要严格接口定义的场景 | Express | 性能不如 Fastify，Schema 校验需要额外插件 |
| ORM | **Prisma** | 类型安全的数据库操作；自动生成 TypeScript 类型；Migration 管理方便 | TypeORM | API 设计较老，类型推导不如 Prisma |
| | | | 原生 SQL | 缺少类型安全，开发效率低 |

> **什么是 ORM？为什么需要它？**
>
> ORM（Object-Relational Mapping）是一个"翻译层"。没有 ORM 时，你写 SQL 字符串操作数据库：
> ```sql
> SELECT * FROM users WHERE id = 123;
> ```
> 有 ORM 时，你写 JS 代码操作数据库：
> ```typescript
> const user = await prisma.user.findUnique({ where: { id: 123 } });
> ```
> 区别在于：ORM 写出来的代码有**类型检查**——如果你拼错了字段名（比如写了 `usrname` 而不是 `username`），TypeScript 编译时就会报错，而不是等到用户访问时才崩。对于本项目这种数据模型多、字段多的情况，ORM 能显著减少低级 bug。

#### 1.6.3 数据库

| 技术 | 选型 | 决策理由 | 替代方案 | 放弃原因 |
|---|---|---|---|---|
| 数据库 | **MySQL 8.0+** | 国内云服务支持最成熟；团队更熟悉；JSON 列类型可存储问卷答案 | PostgreSQL | 功能更强但团队不熟悉，MySQL 的 JSON 列已满足需求 |
| | | | MongoDB | 权重矩阵需要关联查询，文档型数据库不适合 |
| 缓存 | **可选，V1 不引入** | V1 用户量小，无性能瓶颈 | Redis | V1 不需要，V2 评估后再决定 |

#### 1.6.4 部署

| 层面 | 选型 | 决策理由 |
|---|---|---|
| 服务器 | **国内云服务器（阿里云 / 腾讯云）** | 目标用户在中国大陆，国内服务器访问速度快且稳定 |
| 前端部署 | **Nginx 静态托管** | 前端构建产物（HTML + JS + CSS）通过 Nginx 直接响应 |
| 后端部署 | **PM2 进程守护** | 保持 Node.js 进程持续运行，崩溃自动重启 |
| HTTPS | **必须开启** | 用户问卷数据涉及个人隐私，传输必须加密 |
| 自动部署 | **GitHub Actions → 服务器 rsync** | 代码推送到 GitHub main 分支后，自动同步到服务器并重启后端 |

> **[PM 笔记] 为什么部署方案不用 Vercel + Railway？**
>
> Vercel 和 Railway 是海外云平台，国内用户访问速度和稳定性不可控。目标用户群在中国大陆，必须用国内服务器。牺牲一定的"一键部署便利性"，换取"用户访问速度"——这是 PM 基于**用户特征**而不是技术偏好做的权衡。

---

### 1.7 代码仓库与分支策略

#### 1.7.1 代码仓库

| 项 | 约定 |
|---|---|
| 托管平台 | **GitHub** |
| 仓库名 | `phd-career` |
| 可见性 | 私有（V1 阶段） |
| 初始化时间 | PRD 评审通过后、开发启动前 |

#### 1.7.2 分支策略：GitHub Flow

**规则只有 3 条：**

```
1. main 分支 = 永远可以部署到生产的代码
2. 每个功能从 main 开一个 feature/xxx 分支
3. 功能做完 → 提 Pull Request → 有人 Review 通过 → 合并回 main → 删除 feature 分支
```

**分支命名规范：**

| 分支类型 | 命名格式 | 示例 |
|---|---|---|
| 新功能 | `feature/<功能简称>` | `feature/assessment-questionnaire` |
| Bug 修复 | `fix/<问题简称>` | `fix/radar-chart-overlap` |
| 紧急修复 | `hotfix/<问题简称>` | `hotfix/api-timeout` |

**Commit Message 规范（采用 Conventional Commits）：**

```
<type>: <简短描述>

示例：
feat: 新增职业评估问卷模块 B（兴趣评估 36 题）
fix: 修复门槛评估雷达图五维标签重叠问题
refactor: 将 FAMILIES_DATA 从 App.jsx 提取到独立文件
chore: 初始化 Vite + React + TypeScript 项目
```

| Type | 含义 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修 Bug |
| `refactor` | 重构（不改变功能的代码调整） |
| `chore` | 工程配置类改动 |
| `docs` | 文档修改 |
| `style` | 纯样式修改 |

#### 1.7.3 Pull Request 规范

每个 PR 必须包含：
- **变更摘要**：一句话说清楚改了什么、为什么改
- **关联的 PRD 章节**：如"PRD §7.4.3 模块 C（技能评估 24 题）"
- **测试说明**：手动测试了哪些场景（V1 无自动化测试，用手动测试清单代替）
- **Screenshot（UI 变更时）**：改动前 vs 改动后的截图

---

### 1.8 代码组织规范

#### 1.8.1 目录结构

```
phd-career/
  prd/
    PRD-学术之外-Beyond-Academia.md     # 本文档
    assets/                               # PRD 附件（原型截图、流程图）
  src/
    components/                           # 共享 UI 组件（跨页面复用）
      Nav.tsx                             # 顶部导航栏
      Footer.tsx                          # 页脚
      SectionTitle.tsx                    # 模块标题
      Crumb.tsx                           # 面包屑导航
      FormCard.tsx                        # 表单调板
      StatusBadge.tsx                     # 状态徽章（通过/警告/未达标）
      LoadingBlock.tsx                    # 加载占位
      QRPlaceholder.tsx                   # 装饰二维码
    pages/                                # 页面组件（一个文件一个页面）
      HomePage.tsx                        # 首页
      MapPage.tsx                         # 职业地图
      CategoryDetailPage.tsx              # 职业分类详情
      ToolsHubPage.tsx                    # 决策工具中心
      ThresholdPage.tsx                   # 学术职位门槛评估
      WarningPage.tsx                     # 职位预警
      PersonalityPage.tsx                 # 博士非学术职业评估
      JobsPage.tsx                        # 岗位精选
      JobDetailPage.tsx                   # 岗位详情
      InsightsPage.tsx                    # 职业数据
      AssessmentPage.tsx                  # [新] 职业评估完整问卷作答
      AssessmentResultPage.tsx            # [新] 职业评估结果
      LoginPage.tsx                       # [新] 登录页
      UserCenterPage.tsx                  # [新] 个人中心
    data/                                 # 业务数据（纯数据，不含 UI）
      categories.ts                       # 8 个职业分类
      jobs.ts                             # 岗位数据
      destinations.ts                     # 就业去向数据
      salaries.ts                         # 薪资数据
      interviews.ts                       # 转型者访谈
      assessment-questions.ts             # [新] 87 道评估题目
      assessment-weights.ts               # [新] 权重矩阵
    hooks/                                # 自定义 React Hook
      useAssessment.ts                    # [新] 评估问卷状态管理
      useAuth.ts                          # [新] 用户认证
    utils/                                # 工具函数（纯逻辑）
      scoring.ts                          # 门槛评估评分算法
      matching.ts                         # 职业评估匹配算法
      jd-analyzer.ts                      # JD 风险检测
      format.ts                           # 格式化工具
    styles/                               # 全局样式
      index.css                           # Tailwind 入口 + 全局 CSS
    App.tsx                               # 根组件（路由编排）
    main.tsx                              # 应用入口
  public/
    favicon.ico
  package.json
  tsconfig.json
  vite.config.ts
  tailwind.config.ts
  .gitignore
```

> **拆分原则**：一个文件超过 **300 行**，开始考虑拆。一个文件超过 **500 行**，必须拆。

#### 1.8.2 组件命名规范

| 类型 | 命名规则 | 示例 |
|---|---|---|
| 页面组件 | `PascalCase + Page` 后缀 | `HomePage.tsx`、`AssessmentPage.tsx` |
| 共享组件 | `PascalCase`，不加后缀 | `Nav.tsx`、`FormCard.tsx` |
| 组件内部子组件 | 不单独建文件（拆分阈值：用了 3 次以上才独立文件） | — |
| 组件 Props 类型 | 组件名 + `Props` | `NavProps`、`FormCardProps` |

#### 1.8.3 数据与 UI 分离（强制）

**原则**：业务数据和 UI 代码**不能在同一个文件里**。

- 数据常量（8 个职业分类、评估题目、权重矩阵等）必须放在 `src/data/` 目录
- 工具函数（算法、格式化、校验）必须放在 `src/utils/` 目录
- 组件只负责渲染和交互，数据从外部导入

**为什么强制这条规则？**

1. 非技术人员（如内容编辑）更新职业分类信息时不需要碰组件代码，不会把页面改崩
2. 将来数据改成从后端 API 获取时，只需改数据层的导入路径，组件代码不受影响
3. 128KB 的纯数据和 20KB 的 UI 逻辑分开后，找人 review 代码时可以只看相关部分

#### 1.8.4 TypeScript 使用规范

- **所有新文件必须使用 `.tsx`（含 JSX）或 `.ts`（不含 JSX）**
- 任何从后端接口获取的数据、传给后端接口的数据，必须定义 interface
- 不允许使用 `any` 类型（除非万不得已，且必须加注释说明原因）
- 现有 `.jsx` 文件在重构时逐步迁移，不在 V1 的范围中强制改

---

### 1.9 开发环境与部署

#### 1.9.1 环境划分

| 环境 | 用户 | 数据 | 用途 |
|---|---|---|---|
| **本地开发（Dev）** | 开发者 | 本地 MySQL + 种子数据 | 日常开发调试 |
| **生产（Prod）** | 真实用户 | 阿里云/腾讯云 MySQL | 对外服务 |

> V1 不设立独立的 Staging 环境。上线验证阶段通过**限制邀请用户访问**的方式在生产环境中验证，相当于"软启动"。验证确认后对外公开。

#### 1.9.2 本地开发环境搭建

开发者需要安装的软件及版本：

| 软件 | 版本要求 | 用途 |
|---|---|---|
| Node.js | 20.x LTS | 前后端运行时 |
| npm | 9.x+ | 包管理 |
| MySQL | 8.0+ | 本地数据库 |
| Git | 2.40+ | 版本管理 |

**项目启动命令（V1 目标）**：

```bash
# 1. 克隆仓库
git clone git@github.com:<org>/phd-career.git
cd phd-career

# 2. 安装依赖
npm install

# 3. 初始化数据库（创建表 + 插入种子数据）
npx prisma migrate dev
npx prisma db seed

# 4. 启动开发服务器（前端 + 后端同时启动）
npm run dev
```

> PM 不需要理解每条命令的技术细节。PM 需要关心的是：一个新人开发者从零到能把项目跑起来，需要几步？如果超过 5 步，说明开发环境搭建的门槛太高，应该在 PRD 里提出简化要求。

#### 1.9.3 生产环境部署架构

```
用户浏览器
    │
    ▼
┌─────────────────────────────────┐
│  阿里云 / 腾讯云 ECS              │
│                                  │
│  ┌──────────┐  ┌──────────────┐  │
│  │  Nginx   │  │  Node.js     │  │
│  │  静态文件  │  │  (Fastify)   │  │
│  │  反向代理  │◄─┤  API 服务    │  │
│  │  HTTPS   │  │  PM2 守护    │  │
│  └──────────┘  └──────┬───────┘  │
│                       │          │
│                 ┌─────▼──────┐   │
│                 │   MySQL    │   │
│                 │   8.0+     │   │
│                 └────────────┘   │
└─────────────────────────────────┘
```

**部署流程（自动触发）**：

```
开发者 push 代码到 GitHub main 分支
  → GitHub Actions 自动运行 build
  → rsync 同步构建产物到服务器
  → PM2 重启后端服务
  → 发送部署通知到企业微信/钉钉群
```

---

## 模块 2：产品概述

### 2.1 一句话产品定位

> **「学术之外」是专为中国博士设计的职业探索与决策平台，从了解选项到匹配方向到岗位投递，一站完成从"我该去哪里"到"我知道怎么去"的全流程。**

**关键词拆解：**

| 关键词 | 含义 | 为什么选这个词 |
|---|---|---|
| 专为中国博士 | 不是通用职业测评，只服务博士群体 | myIDP 只覆盖 STEM，ImaginePhD 只覆盖人文社科——中国博士缺本土工具 |
| 职业探索与决策 | 不是"测试完就走"的一次性工具，是持续使用的决策辅助 | 职业选择不是一个测试结果能解决的，需要多轮信息收集和验证 |
| 从了解选项到匹配方向到岗位投递 | 覆盖完整链路：认知 → 匹配 → 行动 | 用户不是来找工作的——而是想清楚自己应该找什么工作 |
| 一站完成 | 不用在多个工具之间跳来跳去 | 降低决策启动门槛——每多跳一个工具，用户流失增加一次 |

> **[PM 笔记] 产品定位写多长合适？**
>
> 一个测试：把定位句读给一个完全不了解项目的人，读完问他"你觉得这产品是干嘛的？"。如果他复述的内容和你预期一致，定位就写对了。不一致，说明核心信息没传达清楚。
>
> 新手 PM 常见的错误是用高大上的词汇堆砌（"赋能""生态""闭环"），但这些词在读者脑子里不产生画面。好的定位句全是**具体信息**——"中国博士""从选项到投递""一站完成"——每个词都传递了可验证的信息。
>
> 定位句写好之后放在 PRD 最显眼的位置，不是为了好看——它是一把尺子。后续每写一个新功能需求，都用这把尺子量一下："这个功能帮中国博士完成从选项到投递的哪一步？"如果回答不上来，说明这个功能不该做。

---

### 2.2 产品战略定位

> **[PM 笔记] 产品定位 vs 战略定位的区别**
>
> 产品定位（§2.1）回答"这个产品是什么"。战略定位（§2.2）回答"这个产品在行业里的站位是什么"——它的上游是谁、下游是谁、它的不可替代性从哪里来。
>
> 产品定位变了需要重写 PRD。战略定位变了需要重新评估商业模型。它们是两层不同的问题。

#### 2.2.1 位于招聘平台的上游

招聘平台的用户行为是"我有明确的求职意向，我要找到匹配的岗位"。但博士就业的现状是：**大量博士生不知道自己应该去学术还是去产业、去企业还是去体制内。**

这是博士群体的结构性特征决定的：
- 博士培养体系默认"学术是唯一出路"，导师不教你怎么去产业
- 博士的专业能力高度垂直，不知道自己的技能往哪里迁移
- 博士的职业试错成本极高（35 岁门槛、编制窗口期、第一份工作决定后续赛道）

所以传统招聘平台（Boss 直聘、猎聘、LinkedIn）解不了博士的问题——他们只连接"知道自己要什么"的人和岗位。「学术之外」解决的是上游问题：**帮助你搞清楚自己应该要什么。**

```
「学术之外」              Boss 直聘 / 猎聘
   │                          │
   ▼                          ▼
搞清楚方向                  找到具体岗位
（选项探索 + 自我认知        （搜索 + 投递）
  + 方向匹配 + 岗位了解）        │
   │                          ▼
   └────────────┬─────────────┘
                ▼
            用户投出简历
```

**这个战略定位意味着**：产品成功的标志不是"用户在我们平台上投了多少份简历"（那是招聘平台要关心的事），而是"用户在探索完成后，明确知道自己的 Top 3 职业方向并为此采取了行动"。

#### 2.2.2 以学者身份做产品

「学术之外」不是互联网公司做的通用测评工具，它由**替代性学术职业专委会**出品。

**这意味着三个差异化优势：**

| 优势 | 具体表现 | 用户为什么在意 |
|---|---|---|
| **学者主导** | 权重矩阵由专委会编辑部基于文献调研与各方向职业特征分析后预填设计，方法论参照 myIDP（AAAS / Science Careers）和 ImaginePhD（Graduate Career Consortium）这两个经过全球学术圈验证的工具 | 测评结果有学术逻辑支撑，不是"网上那些随便测着玩的东西" |
| **学术公信力** | 算法设计公开透明（个体内中心化 + 加权得分 + 归一化），每个权重值的来源可追溯，上线后持续推进正式专家调查以校准权重 | 博士这个群体对"方法论有没有依据"极度敏感 |
| **可承担政策建言** | 平台积累的博士就业数据（去向趋势、薪资中位数、学科差异）可以作为博士职业发展政策研究的参考素材 | 用户填写问卷不仅获得个人收益，也参与了一项有公共价值的数据收集 |

> **[PM 笔记] "学者身份"既是优势也是约束**
>
> 优势：学术公信力是通用招聘平台无法复制的壁垒。
>
> 约束：产品风格必须与学术气质一致——不能有"测测你的职场性格，分享到朋友圈看看朋友怎么说"这类社交裂变玩法。用户对学术机构的期望是严谨、隐私保护、方法论透明。任何时候想加社交功能，先用这个标尺量一下。

---

### 2.3 核心差异化 vs 对标产品

#### 2.3.1 对标产品速览

| 对标产品 | 出品方 | 面向人群 | 覆盖学科 | 中国情境 | 产品形态演变 |
|---|---|---|---|---|---|
| **myIDP** | AAAS / Science Careers | STEM 博士 | 仅 STEM | 无 | 从网站演变为 pdf 手册，现已停更 |
| **ImaginePhD** | Graduate Career Consortium | 人文社科博士 | 仅人文社科 | 无 | 网站问卷 + 结果页 |
| **学术之外** | 替代性学术职业专委会 | **中国博士（全学科）** | 全学科 | **有** | Web 应用 |

#### 2.3.2 核心差异：中国情境

myIDP 和 ImaginePhD 解决的是"博士如何探索非学术职业"的通用问题。但它们进入不了中国博士的核心决策场景，因为中国博士的职业选择有几个**独有的约束维度**：

| 中国特有的决策维度 | 国际工具是否覆盖 | 「学术之外」如何处理 |
|---|---|---|
| **编制（事业编 / 员额制 / 合同制）** | ✗ 不存在这个概念 | 模块 E01 直接询问编制偏好，结果页在对应方向上标注冲突提示 |
| **地域绑定**（北京户口、上海落户、配偶工作安置） | ✗ 没有户口概念 | 模块 E02 询问地域接受范围，与方向薪资中位数交叉比对 |
| **薪资底线**（博士群体对"低薪但稳定"vs"高薪但风险"的权衡） | ✗ 对标的是美元薪酬体系 | 模块 E03 询问年薪底线，结合职业数据页的薪资表给出实际预期 |
| **纪律性组织接受度**（部队文职、党政机关对政治面貌和行为规范的要求） | ✗ 不涉及 | 模块 E05 询问接受度，匹配到军警文职或党政方向时显式提示 |
| **学科门类差异**（经管法 vs 人文 vs 理工 vs 医学的就业逻辑完全不同） | 有学科分类但不够细 | 模块 A01 选择学科门类，常模分组后匹配算法和结果文案均按学科适配 |

> **[PM 笔记] 差异化的判断标准**
>
> 判断一个差异化点是否成立，不看"我们有没有"，看"用户能不能感知到"。一个测试：你把这个差异化点删掉，产品的核心体验是否变样？
>
> 如果删掉"中国情境"，「学术之外」就变成了 myIDP 的中文翻译版。用户填完问卷发现没有一个问题问编制、问地域、问体制内接受度，结果页推荐的 Top 1 方向是"军工文职"却没有冲突提示——这个体验已经完全变了。所以中国情境不是锦上添花，是**产品成立的前提**。

---

### 2.4 目标用户群（V1）

| 用户类型 | 角色 | 权限范围 | V1 范围 |
|---|---|---|---|
| **普通用户** | 博士在读生（前期 + 毕业年） | 浏览职业地图与岗位、使用三大决策工具、查看职业数据、保存个人评估记录 | ✅ V1 核心用户 |
| **管理者（Admin）** | 平台运营方（专委会编辑部成员） | 对职业地图数据、岗位精选数据、决策工具的配置参数（权重矩阵、风险规则、门槛基准值）进行增删改查 | ✅ V1 包含（见 §2.5.3 管理后台） |

**普通用户（博士在读生）详细说明：**

| 维度 | V1 范围 | 决策理由 |
|---|---|---|
| 身份 | 博士在读生 | V1 要验证的核心假设是"博士生在职业迷茫期愿意花 8 分钟完成测评并认为结果有用"。在读阶段的博士生是职业决策压力最集中的人群 |
| 学科 | 不限 | 产品覆盖全学科的职业分类体系，初期即面向所有学科门类（经管法 / 人文 / 理工 / 医学 / 艺术与其他） |
| 地域 | 中国大陆 | V1 部署在国内服务器，产品内容（编制、地域、薪资）均以中国情境为基准 |
| 是否已有心仪方向 | 不限（迷茫者与有大致范围者均可） | 问卷模块 A04 会区分三类用户，用于不同场景的结果页文案适配和数据埋点 |

> **[PM 笔记] 为什么 V1 只选博士在读生？**
>
> 不是"博士后和已毕业博士不重要"，而是**每一类用户的核心问题不同，产品需要不同版本的功能来服务**：
> - 在读博士生：问题是"我应该去哪里？"→ 职业评估 + 职业地图
> - 博士后：问题是"我离目标岗位还差什么？"→ 门槛评估 + JD 预警
> - 已毕业想转行：问题是"我的学术资本有多少可迁移？"→ 需要转行成本估算工具（V1 没有）
>
> 如果 V1 把三类用户都放进来，每个功能都要适配三种场景，开发量翻 3 倍，而且三类用户的反馈混在一起，PM 分不清"哪个群体的需求真正被满足了"。
>
> **一次只验证一个核心假设，一次只服务一类核心用户。**V1 验证完博士在读生的价值，V2 再扩展用户群。

---

### 2.5 用户角色与权限体系

#### 2.5.1 角色定义

| 角色 | 身份 | 权限模型 |
|---|---|---|
| **普通用户（User）** | 博士在读生，注册后即为普通用户 | 只读 + 个人数据 |
| **管理者（Admin）** | 平台运营方（专委会编辑部成员），由系统管理员手动设为 Admin | 内容管理（CRUD） |

#### 2.5.2 权限矩阵

| 功能模块 | 普通用户 | 管理者 |
|---|---|---|
| **博士职业地图** — 查看 8 个职业分类 | ✅ 可读 | ✅ 可读 |
| **博士职业地图** — 增/删/改职业分类、岗位、雇主、访谈 | ❌ | ✅ 全部 CRUD |
| **岗位精选** — 浏览与搜索岗位 | ✅ 可读 | ✅ 可读 |
| **岗位精选** — 发布/编辑/下架岗位 | ❌ | ✅ 全部 CRUD |
| **职业数据** — 查看去向、薪资、访谈 | ✅ 可读 | ✅ 可读 |
| **职业数据** — 增/删/改数据条目 | ❌ | ✅ 全部 CRUD |
| **学术职位门槛评估** — 填写表单并获取结果 | ✅ | ✅ |
| **学术职位门槛评估** — 修改评分算法参数/基准值 | ❌ | ✅ |
| **职位预警** — 粘贴 JD 并获取分析 | ✅ | ✅ |
| **职位预警** — 修改风险识别规则与阈值 | ❌ | ✅ |
| **博士非学术职业评估** — 填写 87 题问卷并获取结果 | ✅ | ✅ |
| **博士非学术职业评估** — 修改权重矩阵（题目 × 方向相关度） | ❌ | ✅ |
| **个人中心** — 查看/导出个人评估记录 | ✅（仅自己的） | ✅ |
| **管理后台** — 访问后台入口 | ❌ | ✅ |
| **管理后台** — 管理用户账号（设 Admin/禁用） | ❌ | ✅ |

> **[PM 笔记] 为什么产品概述阶段就要定义权限？**
>
> 权限是架构级决策，不是功能级决策。如果 PRD 写到一半才发现"这个数据普通人能不能改？"，说明功能设计缺少一个基本前提。先定义权限，后续写每个功能模块时直接引用权限表，不用重复讨论。
>
> **常见错误**：把所有管理功能都推迟到"以后再说"，结果开发做完前端后发现后端没有管理接口，数据全要靠直接操作数据库来改——这就是因为 PRD 里没有提前定义管理者角色。

#### 2.5.3 管理后台

| 管理模块 | 管理者可以做什么 | 对应的普通用户可见内容 |
|---|---|---|
| **职业分类管理** | 新增/编辑/删除职业分类（名称、简介、代表岗位、典型雇主、关联访谈） | 博士职业地图 + 职业分类详情页 |
| **岗位管理** | 新增/编辑/下架岗位（职位名称、单位、地区、类型、薪资、职责、要求、福利） | 岗位精选列表 + 岗位详情页 |
| **职业数据管理** | 新增/编辑/删除去向记录、薪资记录、访谈记录 | 职业数据洞察页（三个 Tab） |
| **权重矩阵管理** | 修改题目 × 方向的权重值（0-3），支持批量导入（CSV/Excel） | 职业评估问卷的匹配算法 |
| **风险规则管理** | 修改 JD 预警的风险识别关键词、风险等级、得分权重 | 职位预警的分析逻辑 |
| **门槛基准管理** | 修改各层次院校的入职门槛基准值（教育背景、论文、基金、海外、学术年龄） | 门槛评估报告的对比基准 |
| **用户管理** | 查看用户列表、设置/撤销管理员权限、禁用账号 | — |

**管理后台访问方式**：
- 管理者通过导航栏或特定 URL（如 `/admin`）进入后台
- 后台与前台共用同一套技术栈（React 组件），但权限校验在后端强制拦截（非 Admin 返回 403）

> **[PM 笔记] 为什么管理后台用同一套前端技术栈？**
>
> 常见做法是另起一个独立的后台项目（比如 Vue + Element Admin）。但 V1 团队规模小（预计 2-3 个开发），维护两套前端代码成本太高。在当前 React 项目内嵌套一个 Admin 区域，共用组件（FormCard、StatusBadge 等），只增加管理专用的数据操作页面，总工作量远小于独立项目。

---

### 2.6 明确排除的用户群体

| 排除群体 | 排除理由 | 未来是否考虑 |
|---|---|---|
| **硕士生 / 本科生** | 产品定位、问卷设计、职业分类体系均围绕博士经历设计 | 不考虑。如果需求被验证，应单独立项 |
| **企业 HR / 招聘方** | 产品战略定位在招聘平台的上游——不撮合招聘，不提供简历筛选、候选人推荐、JD 发布功能 | 不考虑。产品不是招聘平台 |
| **海外博士（非中国籍）** | 中国情境（编制、户口、体制内）是产品的核心差异化，对海外博士不适用 | 不考虑 |


### 2.7 功能全景

> **[PM 笔记] 为什么在产品概述部分就要放功能全景？**
>
> 读者（开发、设计、老板）读完产品定位后，脑子里有概念了，但还缺一个"地图"来理解马上要展开写的众多功能之间的层级关系。功能全景就是这张地图——放在前面让读者带着框架去读后续章节。

```
学术之外 (Beyond Academia)
│
├── 1. 博士职业地图（了解选项）
│   ├── 1.1 8 个职业分类浏览
│   ├── 1.2 职业分类详情（岗位 / 雇主 / 访谈 / 招聘）
│   └── 1.3 职业数据洞察（去向趋势 / 薪资 / 访谈集）
│
├── 2. 三大决策工具（匹配方向）
│   ├── 2.1 学术职位门槛评估
│   │     └── 4 步向导 → 竞争力百分位 + 目标院系对比 + 雷达图
│   ├── 2.2 职位预警
│   │     └── 粘贴 JD → 风险评分 + 风险清单 + 面试确认问题
│   └── 2.3 博士非学术职业评估 ★ V1 核心新建
│         ├── 模块 A：基本信息（4 题）
│         ├── 模块 B：兴趣评估（36 题，李克特 5 点）
│         ├── 模块 C：技能评估（24 题，李克特 5 点）
│         ├── 模块 D：价值观（18 选 5，拖拽排序）
│         ├── 模块 E：现实约束（5 题）
│         └── 结果页：8 分类匹配排序 + 五维雷达图 + Top 3 卡片 + 价值观追问 + 约束冲突提示
│
├── 3. 岗位精选（开始行动）
│   ├── 3.1 岗位列表与搜索筛选
│   └── 3.2 岗位详情
│
├── 4. 用户系统（V1 新建）
│   ├── 4.1 注册 / 登录（普通用户）
│   ├── 4.2 评估记录保存与回看
│   └── 4.3 个人中心
│
└── 5. 管理后台（V1 新建）
    ├── 5.1 职业分类管理（CRUD）
    ├── 5.2 岗位管理（CRUD）
    ├── 5.3 职业数据管理（CRUD）
    ├── 5.4 权重矩阵管理（查看 / 编辑 / 导入）
    ├── 5.5 风险规则管理（关键词 / 阈值 / 权重）
    ├── 5.6 门槛基准管理（各层次院校基准值）
    └── 5.7 用户管理（列表 / 权限设置 / 禁用）
```

**页面清单（V1 完整版）：**

| 编号 | 页面 | 路由 | 角色 | 状态 |
|---|---|---|---|---|
| P01 | 首页 | `/` | 所有用户 | 已有，需重构 |
| P02 | 博士职业地图 | `/careers` | 所有用户 | 已有，需重构 |
| P03 | 职业分类详情 | `/careers/:id` | 所有用户 | 已有，需重构 |
| P04 | 决策工具中心 | `/tools` | 所有用户 | 已有，需重构 |
| P05 | 学术职位门槛评估（作答） | `/tools/threshold` | 所有用户 | 已有，需重构 |
| P06 | 学术职位门槛评估（结果） | `/tools/threshold/result` | 所有用户 | 已有，需重构 |
| P07 | 职位预警 | `/tools/warning` | 所有用户 | 已有，需重构 |
| P08 | 博士非学术职业评估（作答） | `/tools/assessment` | 所有用户 | **V1 新建** |
| P09 | 博士非学术职业评估（结果） | `/tools/assessment/result` | 所有用户 | **V1 新建** |
| P10 | 岗位精选 | `/jobs` | 所有用户 | 已有，需重构 |
| P11 | 岗位详情 | `/jobs/:id` | 所有用户 | 已有，需重构 |
| P12 | 职业数据 | `/insights` | 所有用户 | 已有，需重构 |
| P13 | 登录 / 注册 | `/login` | 所有用户 | **V1 新建** |
| P14 | 个人中心 | `/me` | 普通用户 | **V1 新建** |
| P15 | 管理后台 — 仪表盘 | `/admin` | Admin | **V1 新建** |
| P16 | 管理后台 — 职业分类管理 | `/admin/categories` | Admin | **V1 新建** |
| P17 | 管理后台 — 岗位管理 | `/admin/jobs` | Admin | **V1 新建** |
| P18 | 管理后台 — 职业数据管理 | `/admin/insights` | Admin | **V1 新建** |
| P19 | 管理后台 — 权重矩阵管理 | `/admin/weights` | Admin | **V1 新建** |
| P20 | 管理后台 — 风险规则管理 | `/admin/rules` | Admin | **V1 新建** |
| P21 | 管理后台 — 门槛基准管理 | `/admin/benchmarks` | Admin | **V1 新建** |
| P22 | 管理后台 — 用户管理 | `/admin/users` | Admin | **V1 新建** |

> **[PM 笔记] 页面清单每项标注角色和状态的作用**
>
> 角色列让开发和测试明确"这个页面有权限校验"，后端需要在 API 层拦截非 Admin 请求。
>
> 状态列让排期时能快速区分工作量——"已有需重构"和"V1 新建"的估时完全不同。

---

*模块 2 结束。下一模块：市场背景与问题分析。*

---

## 模块 3：市场背景与问题分析

> **[PM 笔记] 这个模块要回答什么问题？**
>
> 四个问题，分别对应四类读者：
> 1. **市场有多大？**→ 老板/投资人想看天花板
> 2. **市场在发生什么变化？**→ 设计师和开发需要理解"为什么现在做"
> 3. **用户到底痛在哪里？**→ 团队全员需要共情，否则做的功能就会偏离
> 4. **为什么市面上没人做？**→ 竞品分析不是为了比功能，是为了证明"这个空白不是没人看见，而是没人做对"

---

### 3.1 市场规模

根据教育部公开数据，截至 2024 年，中国在学博士生 **67.63 万人**，当年招收博士生 **17.11 万人**，毕业博士生 **9.72 万人**（另一统计口径为 8.3 万人）。2025 年博士招生规模逼近 **20 万人**。过去十年间（2013—2023），中国博士生规模翻了一番。

本产品的直接目标用户是博士在读生，V1 阶段面向的即期市场规模约为 **68 万在学博士生**，且以每年约 17 万新增招生的速度持续扩大。

> **数据来源**：教育部 2024 年全国教育事业发展统计公报。以下数据如无特别标注，均来源于教育部公开数据或已发表的学术调研报告。

---

### 3.2 结构性转变：从"学术独木桥"到多元就业

博士就业正在经历不可逆的结构性转变。以下数据来自教育部公开统计及学术调研：

**高校教职通道收窄。**全国高校每年提供的教学科研、辅导员及行政岗位总和约 3.5 万个，而每年毕业博士生超 9.7 万人——约 5 个博士竞争 1 个高校岗位。到高校和科研机构就业的博士比例首次降至 40% 以下，顶尖研究型大学毕业博士进入高校的比例已降至 50% 以下。

**企业就业快速上升。**博士到企业就业的比例已超过 1/5，且三年来持续上升。理工科博士在战略性新兴产业中受热捧——AI、集成电路、生物医药、新能源、低空经济等领域需求旺盛。2025 年 1—10 月，AI 领域岗位量同比增长超过 500%，AI 领域要求博士学历的岗位占比超过 60%，平均月薪 4 万元以上。

**博士后成为"缓冲带"。**大量博士选择做博士后以延迟就业决策——48% 的博士后已从事博士后研究超过 3 年，30% 已做 2—3 轮博士后（《自然》2020 年调查）。博士后逐渐从"学术预备期"变为"就业观望期"。

**公务员与人才引进成为新选项。**2025 年国考将博士研究生年龄放宽至 40 周岁以下。各地人才引进政策力度持续加大——编制、安家补贴、配偶安置、子女入学等配套政策吸引了越来越多博士进入公共部门。

**人文社科博士转型压力更大。**理工科博士在企业就业市场机会较多，但人文社科博士的非学术就业选择相对受限，且可获取的系统性职业指导资源更少。ImaginePhD 的出现部分填补了欧美人文社科博士的需求，但中国博士没有对应的本土工具。

> **[PM 笔记] 市场数据写到什么程度算合格？**
>
> 不是越多越好。每写一个数据，问自己三个问题：
> 1. 这个数据支持了某个产品决策吗？（如果只是"好看"，删掉）
> 2. 读者能在 10 秒内理解这个数据的意思吗？（如果不能，加一句解释）
> 3. 我能说出这个数据的来源吗？（如果不能，标注 `[待确认]`）
>
> 比如"在学博士生 67.63 万人"支持了"市场规模足够大，值得做一个专属工具"这个判断。如果只是写"博士群体规模庞大"而不加数据，说服力为零。如果写了 10 个数据但没有一个指向产品决策，读者读完只记得"数据好多"但不记得"所以呢"。

---

### 3.3 用户痛点分析

以下痛点基于博士就业的普遍现状和已有学术调研发现，按"用户在什么场景下遇到什么困难"的方式组织。V1 上线后，将通过用户访谈和问卷模块 A04 的埋点数据验证每一条。

#### 痛点 1：不知道有哪些选项

**场景**：一个化学博士在实验室待了 4 年，导师每次组会都在讨论发论文和找博后。他不知道他的有机合成技能在化工企业、专利审查、科学编辑、咨询公司分别值多少钱，甚至不知道这些选项存在。

**本质**：博士培养体系的信息茧房。导师自身的职业路径通常是"读博 → 博后 → 教职"，他对产业界的了解不比学生多。博士生获得职业信息的渠道主要靠同门师兄师姐的口口相传——样本量不超过 10 人。

**产品如何回应**：博士职业地图（8 个职业分类 × 每个分类的代表岗位、典型雇主、转型访谈）把"有什么选项"变成可视化、可检索的结构化信息。

#### 痛点 2：不清楚自己的技能能往哪里迁移

**场景**：一个社会学博士做了三年田野调查和质性分析，但她写简历时只能写"发表 CSSCI 论文 3 篇"。她不知道"设计半结构化访谈提纲""对 50+ 受访者做主题编码分析""撰写面向非专业读者的政策简报"——这些在智库、用户研究、公共政策等方向高度可迁移的技能应该怎么表述。

**本质**：博士训练产出了大量可迁移能力（复杂信息处理、批判性思维、项目管理、写作与汇报），但博士本人往往意识不到这些是"技能"，因为它们被包裹在"做研究"这个单一标签之下。myIDP 和 ImaginePhD 试图解决这个问题，但它们的技能评估题项用的是英语学术界的职业语言，中国博士不一定能对应上。

**产品如何回应**：模块 C 的 24 道技能评估题，全部用"你实际做过的事"来描述（例如"面向非专业读者的写作""跨部门、跨学科的团队协作"），而不是抽象的"沟通能力强"。匹配结果页展示的不是"你的沟通能力 85 分"，而是"你的哪些具体技能往哪个方向迁移最顺畅"。

#### 痛点 3：职业试错成本太高，不敢随便试

**场景**：一个 31 岁的材料学博士还有一年毕业。他听说某新能源企业招博士，但去了以后如果发现不适应，再想回学术圈，35 岁的年龄门槛已经过了。他不敢"先试试看"——因为第一份工作基本决定了后续赛道。

**本质**：博士的职业决策是典型的"高成本、不可逆、信息不足"决策。学术路径有清晰的里程碑（发论文 → 拿基金 → 升职称），非学术路径没有可见的路标。这种"不知道前面是什么但知道回不来了"的恐惧，让大量博士生选择了"继续做博后"作为延迟决策的策略。

**产品如何回应**：职业评估工具给出的是 8 个分类的匹配百分比排序，不是"你就适合做 X"。结果页明确标注"探索起点而非决策终点"的免责声明。结合职业地图中的转型者访谈——让"已经走过的人"提供具体的心理账本（"我用了两年才接受：发表不是终点"）。

#### 痛点 4：现有工具不覆盖中国特有的决策维度

**场景**：一个博士在两个 offer 之间纠结——一个是深圳某 AI 企业年薪 80 万，一个是老家省会的事业编年薪 18 万。钱不是唯一变量。他需要考虑：编制意味着什么？深圳买房要多少年？配偶的工作怎么安置？父母养老怎么办？这些问题 myIDP 和 ImaginePhD 一个都不问。

**本质**：中国博士的职业决策是一个多约束优化问题，变量包括编制类型、地域绑定、薪资底线、家庭责任兼容性、纪律性组织接受度。国际工具完全不具备这些维度，中国本土的通用职业测评工具（如 MBTI、霍兰德）又不理解博士群体的特殊性。

**产品如何回应**：模块 E 的 5 道现实约束题（编制偏好、地域接受度、薪资底线、加班接受度、纪律组织接受度）——不参与匹配排序以保证算法纯净，但在结果页生成冲突提示标签，帮用户看到"你喜欢的这个方向和你最看重的这个条件之间有冲突"。

#### 痛点 5：学术岗位的隐性条款太多，信息不对称

**场景**：一个博士看到某高校"青年研究员"招聘启事，"年薪 25-35 万"看着不错。但他没注意到"首聘期内须主持国家级课题 ≥ 1 项 + SSCI ≥ 5 篇否则不予续聘"——这两行字在 JD 的第 4 段。他不知道 73% 的同类岗位都有预聘制条款，也不知道应该面试时问清楚"国家级课题是否包含青年项目"。

**本质**：学术招聘 JD 的语言高度模板化，关键风险信息往往藏在专业术语（"预聘制""一人一议""特聘"）背后。博士生在求职市场上处于信息弱势一方——除非有师兄师姐在该院系工作，否则几乎无法获取真实的考核细则。

**产品如何回应**：职位预警工具（§7.3）把 JD 中的隐性条款显式化——识别预聘制、数量化考核、年龄门槛、薪资模糊条款，计算风险评分，并给出面试时应该问的具体问题清单。

---

### 3.4 竞品分析

> **[PM 笔记] 竞品写多深？**
>
> 竞品分析的深度取决于你拿它来干什么。如果是用来做产品定位决策（"我们要不要做这个方向"），需要深入分析。如果是用来给团队提供背景（"为什么市面上没有同类产品"），简短对比就够了。本章的目的是后者——我在 §2.3 已经写了差异化对比，这里只补充为什么现有产品的缺口不是偶然的。

#### 3.4.1 myIDP（AAAS / Science Careers）

**概况**：2012 年由美国科学促进会（AAAS）发布，面向 STEM 博士和博士后。评估包含技能、兴趣、价值观三个维度，匹配 20 条职业路径。完全免费。

**核心亮点**：方法论扎实——AAAS 的学术品牌提供了可信度；支持 SMART 目标设定并导出到日历；支持与导师协作（双方各自评估后对比讨论）。

**核心缺陷**：（1）职业建议偏重学术及学术邻近岗位——利物浦大学试验中，"超越学术界的职业规划"评分仅 3/5；（2）评分系统强制分布——不允许所有项目都打高分，部分用户反馈"感觉不必要地受限"；（3）基于北美就业市场，职业信息对中国博士完全不适用；（4）产品已从网站演变为下载型 PDF 手册，基本停更。

**为什么没有解决中国博士的问题**：它是为美国 STEM 博士设计的学术职业发展工具（Individual Development Plan），不是跨学科非学术职业探索工具。它不理解"编制"是什么。

#### 3.4.2 ImaginePhD（Graduate Career Consortium）

**概况**：2017 年由美国研究生职业联盟（56 所大学、80+ 专家）发布，面向人文社科博士和博士后。评估维度与 myIDP 相同（技能、兴趣、价值观），匹配 16 个"职业家族"。完全免费。

**核心亮点**：对人文社科博士的覆盖是 myIDP 没有做的——评估题项的语言专门适配了人文社科的训练背景；16 个职业家族每个都提供了在职 PhD 的访谈案例和简历样本；Liverpool 大学试验中，"识别个人兴趣"评分 4.4/5，为两个工具中最高分。

**核心缺陷**：（1）用户反馈"结果与自我认知高度重合"——一位生物化学博士后说"所有排名靠前的匹配职业都是我早已自我确认为值得追求的方向"；（2）视觉设计被批评为"励志风格"（办公室白领的库存照片）；（3）职业建议依然以北美为中心；（4）设计理念是"帮助你探索"而非"给你结论"——这导致部分用户觉得不够直接。

**为什么没有解决中国博士的问题**：它覆盖了人文社科博士（myIDP 没做的那一半），但它依赖的是美国人文社科博士的就业市场数据——出版、博物馆、非营利组织、高等教育行政——这些方向在中国对应的岗位结构、薪资水平、入职要求完全不同。

#### 3.4.3 国内相关尝试

目前国内没有直接对标的博士非学术职业评估产品。相关但不直接竞争的产品包括：

- **通用职业测评工具**（MBTI、霍兰德、北森等）：面向所有人群，不理解博士群体的技能体系和约束维度
- **高校就业指导中心**：资源集中在校园招聘会和企业宣讲，缺乏系统性职业探索工具
- **博士学术求职平台**（学术志、青塔人才等）：聚焦学术岗位，不覆盖非学术方向
- **综合招聘平台**（Boss 直聘、猎聘等）：连接的是"知道自己要什么"的人和岗位，不解上游的"我该要什么"问题

**这个空白不是没人注意到**——myIDP 和 ImaginePhD 的存在证明博士职业探索是一个已经被识别并验证的需求。空白的原因是：（1）中国博士就业多元化是近 5 年才加速的趋势；（2）做出一个有学术公信力的评估工具，需要同时具备博士培养体系的理解、职业发展方向的知识、和心理测量学的方法论基础——这三组能力通常不在同一个团队里。

---

### 3.5 产品机会总结

| 市场信号 | 机会点 | 「学术之外」的回应 |
|---|---|---|
| 年毕业 9.7 万博士，仅 3.5 万高校教职岗位 | 超 60% 的博士必须走非学术路径，但缺乏系统性的方向探索工具 | 8 个职业分类的匹配评估 + 职业地图 |
| 博士培养以学术为默认路径，缺乏产业界信息 | 博士生急需一个结构化的"非学术职业全景图" | 博士职业地图 + 转型者访谈 |
| 中国博士有编制/地域/薪资底线/纪律组织等独有约束 | 国际工具无法覆盖，国内通用工具无法深入 | 模块 E 现实约束 + 冲突提示 |
| 学术 JD 信息不对称、隐性条款多 | 需要帮博士读懂 JD 的"翻译工具" | 职位预警（识别预聘制、量化考核、模糊条款） |
| 学术公信力是博士群体的信任门槛 | 学者主导的产品比互联网公司更有说服力 | 替代性学术职业专委会出品 + 方法论透明 |

---

*模块 3 结束。下一模块：用户画像。*

---

## 模块 4：用户画像

> **[PM 笔记] Persona 怎么写才不是"写小说"？**
>
> 新手 PM 写 Persona 最容易犯的错误是把 Persona 当成创意写作——起个名字、贴张照片、写上"喜欢喝咖啡"和"周末爱去美术馆"。这些信息对设计和开发没有任何决策价值。
>
> 有效的 Persona 必须回答三个问题：
> 1. **这个人在什么场景下遇到什么困难？**→ 决定了产品在什么时刻介入
> 2. **这个人现在怎么解决这个困难？**→ 决定了替代品是什么（不一定是竞品，可能是"问师兄""刷知乎"）
> 3. **这个人对产品的期望是什么？**→ 决定了 MVP 的最低可接受标准
>
> 以下 3 个 Persona 都遵循这个结构。每个 Persona 包含一个"原话引述"——这句话必须像真实博士说出来的，而不是 PM 代写的。如果你无法想象这个人真的说出这句话，说明 Persona 还不够真实。

---

### Persona 1：迷茫型 · 小陈

#### 基本信息卡

| 维度 | 描述 |
|---|---|
| 姓名 | 小陈 |
| 学科 | 化学（有机合成方向），理工科 |
| 博士阶段 | 2 年级（共 5 年），还有 3 年毕业 |
| 学校层次 | 985 高校 |
| 年龄 | 26 岁 |
| 是否已有心仪方向 | 完全没有 |
| 使用本产品的典型时间 | 周末晚上在实验室等反应，用手机打开 |

#### 一句话画像

小陈每天早上 8 点进实验室，晚上 10 点出来。他的导师说"多发论文、做博后、回来找个教职"。但他已经看到课题组 3 个博后师兄等了 4 年没有等到一个教职。他不知道除了"发论文 → 博后 → 教职"这条路以外，化学博士还能做什么。

#### 当前如何解决职业困惑

- 刷知乎搜索"化学博士 转行""有机合成 出路"
- 问课题组师兄——但师兄们自己也在找教职的路上
- 参加过学院组织的"优秀校友经验分享"，但来的校友全是教授

#### 核心痛点（对应 §3.3）

- **痛点 1（不知道选项）**：这是他最主要的问题。他完全不知道自己的有机合成技能在产业界值多少钱、能做什么。
- **痛点 2（技能迁移困难）**：他认为自己"只会过柱子"（柱层析分离），不知道实验设计能力、数据分析能力、解决复杂问题的能力叫"技能"。

#### 原话引述

> "导师说多发论文就有教职。但组里博后师兄发了 8 篇一区还在等。我能比他更强吗？如果等不到，我该怎么办？我不是不想做学术，我是不知道不做学术的话还能干什么。"

#### 对本产品的期望

- **首要期望**：看到一个"博士毕业后所有可能的方向"的完整地图——不是零散的知乎帖子，是系统性的、可信的
- **次要期望**：有人告诉他，他现在做的实验操作和研究工作，对应到产业界叫什么岗位
- **不会用的部分**：暂时不需要看具体岗位的薪资和要求——他离求职还远

#### 如何触达他

- 公众号文章（"化学博士除了做博后，还有 12 个方向"这种标题会点击）
- 导师在组群里转发（他自己不会主动搜，但导师推的他会看）
- 知乎上"博士就业"相关问题下的推荐链接

---

### Persona 2：纠结型 · 小周

#### 基本信息卡

| 维度 | 描述 |
|---|---|
| 姓名 | 小周 |
| 学科 | 社会学，人文社科 |
| 博士阶段 | 毕业年，已通过答辩，3 个月内必须做决定 |
| 学校层次 | C9 高校 |
| 年龄 | 30 岁 |
| 是否已有心仪方向 | 有大体范围，但无法抉择 |
| 使用本产品的典型时间 | 白天用电脑，反复对比评估结果和岗位信息 |

#### 一句话画像

小周手上有两个选项：一个是老家省会的省属高校讲师岗（事业编，年薪 15 万），一个是上海某智库的研究员岗（合同制，年薪 35 万）。她连续失眠了一周——选了编制就意味着放弃专业成长空间，选了上海就意味着 35 岁前买不起房。她爸妈每天打电话催她选编制，但她的导师说去了省属高校等于学术生涯终结。

#### 当前如何解决职业困惑

- 做了一个 Excel 表，列出两个选项的 10 个对比维度（薪资、编制、城市、晋升、配偶工作安置……）
- 在微信上问已经毕业的师姐——一个选了体制内说"安稳但不甘心"，一个选了企业说"累但有意思"
- 反复刷小红书和脉脉看"社会学博士 就业"相关帖子
- 已经决定不去海外做博后——年龄不允许再拖

#### 核心痛点（对应 §3.3）

- **痛点 3（试错成本高）**：35 岁的年龄门槛让她觉得这是最后一次选择机会
- **痛点 4（中国特有约束）**：编制、地域、配偶安置、父母养老——她的 Excel 表里全是 myIDP 和 ImaginePhD 永远不会问的变量
- **痛点 5（JD 信息不对称）**：省属高校那个 offer 里写了"预聘制"，她不确定这是不是"非升即走"的另一种说法

#### 原话引述

> "我其实知道自己喜欢什么——我喜欢做政策研究，智库方向明显更适合我。但我不能只看'喜欢'做决定。我需要有人告诉我：如果选了智库，5 年后我的退路在哪里？如果选了编制，10 年后我会不会后悔？"

#### 对本产品的期望

- **首要期望**：一个能同时对比多个方向、带具体数据（薪资中位数、晋升年限、转行灵活性）的决策辅助工具
- **次要期望**：看到和她一样处境的人做了什么选择、后来怎么样了
- **可能会用的部分**：JD 预警（扫描省属高校那份 offer 里的风险条款）、职业评估（验证"智库方向"是不是真的适合她）

#### 如何触达她

- 博士求职群里的转发（她每天都在看各种博士求职群）
- 搜索"社会学博士 智库 编制 对比"时 SEO 命中
- 师姐的直接推荐——她最信任的信息源

---

### Persona 3：探索型 · 小赵

#### 基本信息卡

| 维度 | 描述 |
|---|---|
| 姓名 | 小赵 |
| 学科 | 金融学，经管法 |
| 博士阶段 | 3 年级（共 4 年），还有 1 年毕业 |
| 学校层次 | 211 高校 |
| 年龄 | 28 岁 |
| 是否已有心仪方向 | 有大体范围——知道自己要去产业界，但不知道去哪个细分方向 |
| 使用本产品的典型时间 | 写论文写累了，打开浏览器做一次完整的测评 |

#### 一句话画像

小赵从一开始就没打算留学术——他的导师也知道。但他面临的是另一种困难：产业界的选项太多了。券商研究所、公募基金、银行研究院、企业战略部、咨询公司……每个方向都在招博士，但每个方向的实际工作内容、薪资结构、晋升路径完全不同。他不知道从哪里开始对比。

#### 当前如何解决职业困惑

- 关注了几个金融求职公众号，看过一些"卖方研究员的一天"之类的文章
- 在 LinkedIn 上搜了在券商和基金工作的校友，但不好意思直接私信问薪资
- 参加过一次券商校招宣讲会，讲的都是公司多好，没人说工作多累
- 下载过 myIDP 的 PDF，填了 20 分钟发现全是美国的数据，放弃了

#### 核心痛点（对应 §3.3）

- **痛点 2（技能迁移）**：他需要把自己在博士期间做的事"翻译"成产业界的语言——"用 Stata 跑了 3 年面板回归"在简历上应该写成什么？
- **痛点 5（JD 信息不对称）**：他已开始看岗位 JD，但无法判断"宏观研究员"和"行业研究员"的实际区别——JD 上写的都一样模糊

#### 原话引述

> "我不是迷茫，我是选择太多反而不知道怎么选。学术只有一条路——发论文拿教职。但产业界每个方向都说自己'要博士、给高薪'，我分不清哪个方向是真的需要我的研究能力，哪个只是想要一个博士学历的'高级打杂'。"

#### 对本产品的期望

- **首要期望**：一个告诉他"你的技能在哪个方向上最有溢价"的匹配工具——不是泛泛的"你适合做金融"，而是"你的面板数据分析能力在买方研究的量化策略组比在卖方研究的行业覆盖组更有优势"
- **次要期望**：看到每个方向的真实薪资数据和技能迁移路线
- **可能会用的部分**：职业评估（核心需求）、薪资数据页（做决策参考）、岗位精选（看到感兴趣的岗位先收藏）

#### 如何触达他

- 搜索"金融博士 非学术就业"时 SEO 命中
- 同门同学在微信群里分享评估结果
- LinkedIn 上校友的 profile 链接到本平台的数据页

---

### 三个 Persona 的对比

| 维度 | 迷茫型（小陈） | 纠结型（小周） | 探索型（小赵） |
|---|---|---|---|
| 阶段 | 博士前期（还有 3 年） | 博士毕业年（3 个月内决策） | 博士中后期（还有 1 年） |
| 学科 | 理工科（化学） | 人文社科（社会学） | 经管法（金融学） |
| 核心问题 | "除了学术还有什么？" | "两个选项怎么选？" | "选项太多怎么筛？" |
| 最需要的功能 | 职业地图 | 职业评估 + JD 预警 | 职业评估 + 薪资数据 |
| 决策紧迫度 | 低（还在探索） | 极高（倒计时） | 中（已经开始准备） |
| 对产品的信任门槛 | 中（需要看到方法论可信） | 高（需要看到真实案例和数据） | 中（需要看到具体而非泛泛的结果） |
| 首次使用入口 | 公众号文章 → 职业地图 | 朋友推荐 → 职业评估 | 搜索引擎 → 职业评估 |

> **[PM 笔记] 为什么 3 个 Persona 就够了？**
>
> 原则是：**Persona 的数量等于产品需要支持的核心场景数**。如果有两个 Persona 在使用产品时的行为路径完全一样，就应该合并；如果有一个重要的使用场景没有被任何 Persona 覆盖，就应该增加。
>
> 本产品的 3 个 Persona 覆盖了 3 个截然不同的场景：
> - 小陈：在职业地图上**浏览**——被动发现
> - 小赵：在评估工具里**匹配**——主动筛选
> - 小周：在多个结果之间**对比**——紧迫决策
>
> 这三个场景对应三种不同的界面设计需求。如果只有 1 个 Persona，设计师会默认"用户只有一种行为模式"，做出来的界面必然夹生。

---

*模块 4 结束。模块 5（产品目标与成功指标）留待后续补入。*

---

## 模块 6：产品架构总览

> **[PM 笔记] 产品架构总览在整个 PRD 中起什么作用？**
>
> 两个作用：
> 1. **给新读者当导航**：如果一个工程师刚加入项目，他应该先看模块 2（这是什么）再看模块 6（它长什么样），然后跳到自己负责的具体功能模块（模块 7）。不需要读完所有模块才能开工。
> 2. **给开发当骨架**：模块 6 定义了页面之间的层级关系、数据是如何从一处流向另一处的、哪些页面需要权限校验。开发在写第一行代码之前，应该先在这里找到自己负责的页面在整个系统中的位置。

---

### 6.1 信息架构

> 职业分类体系已定稿为 **8 个分类**，替代原型中的 14 个职业族。这一改动同时影响职业地图的展示结构、评估工具的输出维度、以及权重矩阵的设计（详见 §8 算法与匹配模型）。

```
学术之外 (Beyond Academia)
│
├── 首页 /
│   ├── 决策工具卡片（3 个）
│   ├── 职业地图预览（8 个分类入口）
│   ├── 转型者访谈（3 条精选）
│   └── 博士去向趋势图（2019-2025）
│
├── 职业探索
│   ├── 博士职业地图            /careers
│   │   └── 职业分类详情         /careers/:id
│   │       ├── 代表岗位
│   │       ├── 典型雇主
│   │       ├── 转型者访谈
│   │       ├── 正在招聘
│   │       └── 查看该分类薪资数据 → /insights?tab=salary&category=:id
│   ├── 岗位精选                /jobs
│   │   └── 岗位详情             /jobs/:id
│   └── 职业数据                /insights
│       ├── 就业去向 Tab
│       ├── 薪资数据 Tab（支持按分类筛选）
│       └── 转型者访谈 Tab
│
├── 决策工具
│   ├── 工具中心                /tools
│   ├── 学术职位门槛评估         /tools/threshold
│   │   └── 评估结果             /tools/threshold/result
│   ├── 职位预警                /tools/warning
│   └── 博士非学术职业评估       /tools/assessment
│       ├── 模块 A 基本信息（4 题）
│       ├── 模块 B 兴趣评估（题量待按新 8 分类重新编排，见 §8）
│       ├── 模块 C 技能评估（24 题）
│       ├── 模块 D 价值观（18 选 5）
│       ├── 模块 E 现实约束（5 题）
│       └── 评估结果             /tools/assessment/result
│           ├── 8 分类匹配排序
│           ├── 五维雷达图
│           ├── Top 3 分类卡片 → 跳转职业分类详情
│           ├── 价值观追问清单
│           └── 约束冲突提示
│
├── 个人中心                    /me
│   ├── 我的评估记录
│   └── 个人资料
│
└── 管理后台                    /admin
    ├── 仪表盘                  /admin
    ├── 职业分类管理            /admin/categories
    ├── 岗位管理                /admin/jobs
    ├── 职业数据管理            /admin/insights
    ├── 权重矩阵管理            /admin/weights
    ├── 风险规则管理            /admin/rules
    ├── 门槛基准管理            /admin/benchmarks
    └── 用户管理                /admin/users
```

---

### 6.2 8 个职业分类（定稿）

| 编号 | 分类名 | 涵盖范围 | 原 14 族映射来源 |
|---|---|---|---|
| 1 | **学术支撑** | 高校教研岗、高校教辅/管理岗、科研院所——高校与科研机构内部的科研、教学、行政支撑与信息服务 | 原 1.高校教研岗 + 2.高校教辅/管理岗 + 3.科研院所 |
| 2 | **党政管理** | 公务员、选调生、党校——通过选调或公考进入党政机关与参公事业单位 | 原 5.体制内/公务员 |
| 3 | **社会智库** | 行业研究、决策咨询——面向政策制定与社会议题的研究与战略咨询机构 | 原 4.智库/政策研究 |
| 4 | **科技企业** | 企业研发、产业研究——互联网、AI、新能源、金融、咨询等市场化机构的研究与技术岗位 | 原 6.国企/央企研究岗 + 7.企业研究院 + 8.金融/投研 + 10.咨询/战略 |
| 5 | **文化出版** | 公共图书馆、博物馆、编辑出版机构——文化保存、知识传播与内容生产 | 原 9.出版/传媒 + 11.图书馆/博物馆 |
| 6 | **医疗健康** | 医院、公共卫生机构——临床研究、公共卫生、药物研发等健康领域 | **新增**（原 14 族中缺失） |
| 7 | **军警文职** | 军队文职、公安（警务）文职——纪律性组织中的技术与研究岗位 | **新增**（原 9 方向中有 d6，但 14 族中无独立对应） |
| 8 | **其他** | 基础教育（中小学）、金融机构、国际组织等未归入前 7 类的方向 | 原 12.国际组织/NGO + 13.教育/培训创业 + 14.其他/自由职业 |

**8 分类 vs 原 14 职业族对照**：

```
新 8 分类           ←  原 14 职业族

1. 学术支撑         ←  1.高校教研岗 + 2.高校教辅/管理岗 + 3.科研院所
2. 党政管理         ←  5.体制内/公务员
3. 社会智库         ←  4.智库/政策研究
4. 科技企业         ←  6.国企/央企研究岗 + 7.企业研究院 + 8.金融/投研 + 10.咨询/战略
5. 文化出版         ←  9.出版/传媒 + 11.图书馆/博物馆
6. 医疗健康         ←  [新增]
7. 军警文职         ←  [新增]
8. 其他             ←  12.国际组织/NGO + 13.教育/培训创业 + 14.其他/自由职业
```

---

### 6.3 顶部导航栏结构

```
┌──────────────────────────────────────────────────────────────────┐
│  学术之外  Beyond Academia    职业探索  决策工具  岗位精选  职业数据    登录 │
└──────────────────────────────────────────────────────────────────┘
```

| 导航项 | 高亮规则 | 说明 |
|---|---|---|
| 职业探索 | `/careers`、`/careers/:id` 时高亮 | 对应职业地图和分类详情 |
| 决策工具 | `/tools` 及所有子路径时高亮 | 工具中心 + 3 个工具的作答与结果页 |
| 岗位精选 | `/jobs`、`/jobs/:id` 时高亮 | 岗位列表与详情 |
| 职业数据 | `/insights` 时高亮 | 页内 Tab 切换（去向/薪资/访谈） |
| 登录 | 始终显示 | V1 点击后跳转 `/login`（占位页），不实现完整登录 |

**设计约束**：
- 导航栏始终吸顶（sticky），高度不超过 60px
- 移动端（<768px）导航项折叠为汉堡菜单
- Admin 后台 `/admin/*` 系列有独立侧边栏导航，不依赖顶部导航

---

### 6.4 页面流转图

```
                    ┌──────────┐
                    │   首页    │
                    └────┬─────┘
                         │
    ┌──────────┬─────────┼─────────┬──────────┐
    ▼          ▼         ▼         ▼          ▼
┌────────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐
│职业地图 │ │工具中心│ │岗位精选│ │职业数据│ │ 登录(占位) │
│8分类入口│ │3工具  │ │列表   │ │3Tab  │ │          │
└───┬────┘ └──┬───┘ └──┬───┘ └──────┘ └──────────┘
    │         │         │
    ▼         │         ▼
┌────────┐    │    ┌────────┐
│分类详情 │    │    │ 岗位详情 │
│岗位/雇主│◄───┼────│ "门槛"  │
│访谈/招聘│    │    │ "预警"  │
│薪资入口 │──┐ │    └────────┘
└───┬────┘  │ │
    │       │ │         ┌──────────────┐
    │       │ └────────→│  职业数据     │
    │       │           │  (薪资Tab)    │
    │       │           └──────────────┘
    │       │
    │"测匹配度"
    ▼
    │
    │    ┌──────────────────────────┐
    │    │      决策工具（展开）      │
    └───→│                          │
         │  ┌──────────┐            │
         │  │ 门槛评估  │ 4步向导     │
         │  └────┬─────┘            │
         │       ▼                  │
         │  ┌──────────┐            │
         │  │ 评估结果  │ 雷达图+对比表│
         │  └──────────┘            │
         │                          │
         │  ┌──────────┐            │
         │  │ 职位预警  │ JD分析     │
         │  └────┬─────┘            │
         │       ▼                  │
         │  ┌──────────┐            │
         │  │ 风险报告  │ 风险清单    │
         │  └──────────┘            │
         │                          │
         │  ┌──────────┐            │
         │  │ 职业评估  │ 5模块问卷   │
         │  └────┬─────┘            │
         │       ▼                  │
         │  ┌──────────┐            │
         │  │ 匹配结果  │ 8分类排序   │
         │  │ Top3卡片  │──────┐     │
         │  └──────────┘      │     │
         └────────────────────┼─────┘
                              │
                              │ 点击 Top 3 卡片
                              │ 跳转对应分类详情
                              ▼
                        ┌────────┐
                        │ 分类详情 │
                        └────────┘
```

**页面间跳转触发方式汇总**：

| 起点 | 终点 | 触发方式 |
|---|---|---|
| 首页 | 职业分类详情 | 点击首页职业地图预览中的任意分类卡片 |
| 首页 | 职业数据 | 首页"博士去向变化"区域 → 点击"查看完整数据" |
| 首页 | 工具中心 | 点击首页任一决策工具卡片 |
| 职业分类详情 | 职业评估 | 点击侧边栏"测试适配度" |
| 职业分类详情 | 门槛评估 | 点击侧边栏"我离这个岗位的门槛还有多远" |
| 职业分类详情 | JD 预警 | 点击侧边栏"分析这份 JD 的风险条款" |
| 职业分类详情 | 职业数据（薪资 Tab） | 点击侧边栏"查看该分类薪资数据"（携带 `?tab=salary&category=:id`） |
| 岗位详情 | 门槛评估 | 点击侧边栏"我离这个岗位的门槛还有多远" |
| 岗位详情 | JD 预警 | 点击侧边栏"分析这份 JD 的风险条款" |
| 评估结果页 | 职业分类详情 | 点击 Top 3 卡片上的"查看职业地图详情" |
| 导航栏 | 职业数据 | 直接点击"职业数据"导航项 |

---

### 6.5 评估方向与职业分类的统一

本次定稿统一了评估工具的输出维度和职业地图的分类体系——两端都用同一套 8 分类。因此不再需要方向↔分类的映射表，**它们是同一套 ID**。

```typescript
// 评估输出 = 职业地图分类，ID 和名称完全一致
const CATEGORIES = [
  { id: 1, name: "学术支撑" },
  { id: 2, name: "党政管理" },
  { id: 3, name: "社会智库" },
  { id: 4, name: "科技企业" },
  { id: 5, name: "文化出版" },
  { id: 6, name: "医疗健康" },
  { id: 7, name: "军警文职" },
  { id: 8, name: "其他" },
] as const;
```

**评估结果页 → 职业分类详情的跳转逻辑**：Top 3 卡片点击后直接用分类 ID 跳转 `/careers/:id`。

**分类 8 "其他"的特殊处理**：由于"其他"是一个聚合性分类（基础教育 + 金融机构 + 国际组织等），其详情页不展示单一岗位列表，而是列出各子方向的入口链接。

---

### 6.6 路由表

| 路径 | 页面组件 | 需要登录 | 需要 Admin | 说明 |
|---|---|---|---|---|
| `/` | `HomePage` | 否 | 否 | 首页 |
| `/careers` | `MapPage` | 否 | 否 | 博士职业地图（8 分类入口） |
| `/careers/:id` | `CategoryDetailPage` | 否 | 否 | 职业分类详情 |
| `/jobs` | `JobsPage` | 否 | 否 | 岗位精选 |
| `/jobs/:id` | `JobDetailPage` | 否 | 否 | 岗位详情 |
| `/insights` | `InsightsPage` | 否 | 否 | 职业数据（支持 `?tab=` 和 `?category=` 查询参数） |
| `/tools` | `ToolsHubPage` | 否 | 否 | 决策工具中心 |
| `/tools/threshold` | `ThresholdPage` | 否 | 否 | 门槛评估作答 |
| `/tools/threshold/result` | `ThresholdResultPage` | 否 | 否 | 门槛评估结果 |
| `/tools/warning` | `WarningPage` | 否 | 否 | 职位预警 |
| `/tools/assessment` | `AssessmentPage` | 否 | 否 | 职业评估作答（游客可直接使用） |
| `/tools/assessment/result` | `AssessmentResultPage` | 否 | 否 | 职业评估结果 |
| `/login` | `LoginPage` | 否 | 否 | 登录页（V1 占位，不实现完整功能） |
| `/me` | `UserCenterPage` | **V1 占位** | 否 | 个人中心（V1 仅占位，保留接口） |
| `/admin` | `AdminDashboardPage` | 是 | **是** | 管理后台仪表盘 |
| `/admin/categories` | `AdminCategoriesPage` | 是 | **是** | 职业分类管理（CRUD） |
| `/admin/jobs` | `AdminJobsPage` | 是 | **是** | 岗位管理（CRUD） |
| `/admin/insights` | `AdminInsightsPage` | 是 | **是** | 职业数据管理（CRUD） |
| `/admin/weights` | `AdminWeightsPage` | 是 | **是** | 权重矩阵管理 |
| `/admin/rules` | `AdminRulesPage` | 是 | **是** | 风险规则管理 |
| `/admin/benchmarks` | `AdminBenchmarksPage` | 是 | **是** | 门槛基准管理 |
| `/admin/users` | `AdminUsersPage` | 是 | **是** | 用户管理 |

---

### 6.7 V1 功能实现范围与占位级别

#### 6.7.1 占位级别标准

V1 中部分功能标注为"占位"——不完整实现但需要为 V2 预留接口和 UI 入口。为避免开发理解歧义，所有占位功能按以下三级标准执行：

| 级别 | 名称 | 前端 | 后端 | 用户看到什么 | 单功能开发量 |
|---|---|---|---|---|---|
| **L1** | 路由占位 | 路由可访问，页面仅显示一行提示 | 无 | "该功能正在建设中，将在下一版本开放" | ~15 分钟 |
| **L2** | UI 完整 + Mock | 页面 UI 完整，数据用前端 Mock，操作后显示提示 | 接口定义好但返回 `501 Not Implemented` | 完整界面，可点击但操作后提示"即将上线" | ~1-2 天 |
| **L3** | 完整实现 | 前后端打通，正式功能，有完整错误处理和边界逻辑 | 完整实现 | 完整可用 | 3-5 天 |

#### 6.7.2 V1 各功能实现级别

| 功能 | 级别 | 具体规格 |
|---|---|---|
| 管理员登录 | **L3** | JWT 完整实现。初始管理员账号由种子脚本写入数据库（密码 bcrypt 哈希），管理员登录后可访问 `/admin/*` |
| 普通用户注册/登录 | **L2** | `/login` 页面带完整 UI（登录表单 + 注册表单切换），但提交按钮不调后端——点击后弹出 Toast"用户系统即将上线，当前可游客模式使用全部功能"。路由表保留，页面可访问 |
| 个人中心 `/me` | **L1** | 路由可访问，页面居中显示"个人中心即将上线。评估结果已自动保存到您的浏览器。" |
| "保存结果"按钮（评估结果页） | **L2** | 按钮可见可点击。点击后将 `AssessmentResult` JSON 写入 `localStorage`，弹 Toast"已保存到本地。账号系统上线后可同步到云端。" |
| `assessment_results` 表 | **建表不写入** | 数据库 Migration 中包含完整 DDL，Prisma Schema 中定义 Model。但 POST `/api/tools/assessment/save` 返回 `501 Not Implemented`。V2 启用时只需去掉 501 返回 |
| 用户管理 `/admin/users` | **L2** | 页面展示用户列表（硬编码的 Admin 账号一条记录），搜索/禁用/编辑按钮可见但点击提示"V2 启用" |

#### 6.7.3 降级策略触发机制

当 API 不可用时，前端静默降级到本地计算。判定"不可用"的标准：

```typescript
async function fetchWithFallback<T>(url: string, fallback: () => T): Promise<T> {
  try {
    const res = await fetch(url, { signal: AbortSignal.timeout(5000) });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return await res.json();
  } catch (err) {
    // 网络错误、超时、HTTP 非 2xx 均触发降级
    console.warn(`[Fallback] ${url} 不可用，降级到本地计算`, err);
    return fallback();
  }
}
```

降级对用户透明——前端不弹出错误提示，仅在浏览器 Console 输出 warning。首次加载时检测后端可用性，如果连续 3 次降级则在页面底部展示一条不可关闭的信息条："当前为离线模式，部分功能不可用"。

#### 6.7.4 降级数据覆盖矩阵

| 数据 | 前端降级源 | 后端源 | 降级行为 |
|---|---|---|---|
| 8 个分类 | `src/data/categories.ts` | `GET /api/careers/categories` | API 不可用 → 使用本地静态数据 |
| 岗位列表 | `src/data/jobs.ts` | `GET /api/jobs` | API 不可用 → 使用本地静态数据 |
| 薪资/去向/访谈 | `src/data/salaries.ts` 等 | `GET /api/insights/*` | API 不可用 → 使用本地静态数据 |
| 门槛评估计算 | `src/utils/scoring.ts` | `POST /api/tools/threshold/evaluate` | API 不可用 → 前端直接算 |
| JD 风险扫描 | `src/utils/jdAnalyzer.ts` | `POST /api/tools/warning/scan` | API 不可用 → 前端直接算 |
| 职业评估匹配 | `src/utils/matching.ts` | `POST /api/tools/assessment/evaluate` | API 不可用 → 前端直接算 |

---

### 6.8 数据流架构

```
┌─────────────────────────────────────────────────────────┐
│                       前端 (React)                       │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │ 页面组件  │  │ 共享组件  │  │ Zustand  │               │
│  │ (Pages)  │  │(Components)│  │  Store   │               │
│  └────┬─────┘  └──────────┘  └────┬─────┘               │
│       │                           │                      │
│       │  Zustand 管理全局状态       │                      │
│       │  (问卷进度、评估结果)        │                      │
│       │                           │                      │
│       ▼                           ▼                      │
│  ┌─────────────────────────────────────┐                │
│  │           API 调用层 (fetch)          │                │
│  └─────────────────┬───────────────────┘                │
└────────────────────┼────────────────────────────────────┘
                     │ HTTP (JSON)
                     │
┌────────────────────┼────────────────────────────────────┐
│                    ▼            后端 (Fastify)            │
│  ┌─────────────────────────────────────┐                │
│  │           路由层 (Routes)            │                │
│  └─────────────────┬───────────────────┘                │
│                    │                                     │
│  ┌─────────────────▼───────────────────┐                │
│  │          业务逻辑层 (Services)       │                │
│  │  - 匹配算法（matching.ts）           │                │
│  │  - 风险检测（jd-analyzer.ts）        │                │
│  │  - 权限校验（auth.ts）               │                │
│  └─────────────────┬───────────────────┘                │
│                    │                                     │
│  ┌─────────────────▼───────────────────┐                │
│  │          数据访问层 (Prisma)         │                │
│  └─────────────────┬───────────────────┘                │
└────────────────────┼────────────────────────────────────┘
                     │
                     ▼
              ┌────────────┐
              │   MySQL    │
              │   8.0+     │
              └────────────┘
```

**三类数据流**：

| 数据流类型 | 路径 | 示例 |
|---|---|---|
| 前台只读 | 前端 → API → MySQL → 返回 JSON | 获取 8 个分类、岗位列表、薪资数据 |
| 评估计算 | 前端收集答案 → POST API → 后端运行算法 → 返回结果 JSON | 提交评估答案，返回 8 分类匹配度排序 |
| 管理端写入 | Admin 前端 → POST/PUT/DELETE → JWT 校验 → MySQL | 新增/编辑/删除岗位或分类数据 |

**V1 前端降级策略**（网络不可用或后端未部署时）：

| 场景 | 降级方案 |
|---|---|
| 前台只读数据 | 前端内置静态数据副本（`src/data/`），API 不可用时自动降级到本地数据 |
| 评估计算 | 匹配算法在前端 `src/utils/` 中保留纯函数副本——后端不可用时直接在前端完成计算 |

---

### 6.9 管理后台侧边栏导航

管理后台不与前台共用顶部导航栏，使用独立的侧边栏布局：

```
┌──────────────────────────────────────────────┐
│  管理后台  │  Admin Dashboard                │
│            │                                 │
│  ◆ 仪表盘   │  ┌─────────┐ ┌─────────┐     │
│             │  │ 分类 8   │  │ 岗位 N   │     │
│  数据管理   │  └─────────┘ └─────────┘     │
│  ├ 职业分类  │  ┌─────────┐ ┌─────────┐     │
│  ├ 岗位      │  │ 数据 N   │  │ 用户 N   │     │
│  ├ 职业数据  │  └─────────┘ └─────────┘     │
│             │                                 │
│  配置管理   │                                 │
│  ├ 权重矩阵  │                                 │
│  ├ 风险规则  │                                 │
│  ├ 门槛基准  │                                 │
│             │                                 │
│  系统管理   │                                 │
│  └ 用户管理  │                                 │
│             │                                 │
│  ← 返回前台  │                                 │
└──────────────────────────────────────────────┘
```

---

*模块 6 结束。下一模块：模块 7（核心功能详述）。*

---

## 模块 7：核心功能详述

> **本章结构说明**
>
> 每个功能模块分为两层：
> - **产品需求侧**：用户故事、交互流程、业务规则——回答"做什么、为什么"
> - **开发规范侧**：文件清单、类型定义、组件 Props、算法实现、API 契约、状态机、验收条件、测试用例——回答"怎么做、接口长什么样"
>
> 程序员拿到本节应能直接开工，无需追问任何细节。

---

### 7.3 学术职位门槛评估

---

#### 产品需求侧

##### 7.3.1 功能定位

学术职位门槛评估是三大决策工具之一（对应模块 2 功能全景中的 2.1）。它解决的核心问题是：**"我的学术资本在同届同学科博士中排什么位置？离目标院系的入职门槛还有多远？"**

本功能与另外两个工具形成互补关系：

| 工具 | 解决的问题 | 使用时机 |
|---|---|---|
| 门槛评估（本功能） | 我的学术资本够不够？ | 考虑学术岗位时 |
| 职位预警（§7.4） | 这个 JD 里有什么坑？ | 拿到具体 JD 时 |
| 职业评估（§7.5） | 我适合什么非学术方向？ | 探索非学术路径时 |

##### 7.3.2 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-TH-01 | 博士在读生 | 输入学科背景、教育经历、论文与课题数据，获得在同届同学科博士中的竞争力百分位 | 知道自己目前在学术求职市场上的相对位置 |
| US-TH-02 | 博士在读生（已有目标院系） | 在百分位基础上叠加目标院系的入职门槛对比 | 判断自己离目标还有多大差距，哪些维度需要补强 |
| US-TH-03 | 博士在读生（有目标院系） | 粘贴该院系近 3-5 年新入职老师的成果清单 | 获得比通用层次基准更精确的定制化对比 |
| US-TH-04 | 管理者 | 在后台修改各层次院校的入职门槛基准值 | 根据最新数据持续校准对比基准的准确性 |

##### 7.3.3 交互流程

本功能是一个 **4 步向导（Wizard）**，用户按顺序填写表单，最后生成评估报告。Step 之间的切换不触发任何后端请求——仅在第 4 步点击"生成评估报告"时才提交。

**Step 导航规则**：
- 可以自由前进/后退，已填写的内容不会丢失
- 第 1 步无"上一步"按钮
- 第 4 步的按钮文案为"生成评估报告 →"（而非"下一步"）
- 第 4 步若未选择 hasTarget（值为空字符串），按钮置灰不可点击

**各步骤用户看到的界面**：

```
Step 1 ─ 基础信息与学科领域
  ┌─────────────────────────────────────────┐
  │ 学科门类  [经管法] [人文] [理工] [医学] [艺术与其他]  │
  │ 一级学科  [──────下拉选择──────────────]          │
  │ 博士毕业年份 [2025 ▼]  目前年龄 [31]  在读年限 [5]  │
  │                          [下一步 →]              │
  └─────────────────────────────────────────┘

Step 2 ─ 教育背景
  ┌─────────────────────────────────────────┐
  │ 博士院校  [C9] [985] [211] [省属] [海外名校] [其他]│
  │ 硕士院校  [C9] [985] [211] [海外名校] [其他] [无]  │
  │ 本科院校  [C9] [985] [211] [普通本科] [民办] [海外] │
  │ 海外经历≥6个月  [有] [无]                         │
  │                     [← 上一步]  [下一步 →]        │
  └─────────────────────────────────────────┘

Step 3 ─ 学术发表与科研项目
  ┌─────────────────────────────────────────┐
  │ 仅填写以一作/通讯作者身份产出的成果。            │
  │ SCI/SSCI 论文 [3] 篇   CSSCI 论文 [5] 篇      │
  │ 主持国家级课题 [1] 项  主持省部级课题 [1] 项     │
  │                     [← 上一步]  [下一步 →]        │
  └─────────────────────────────────────────┘

Step 4 ─ 是否已有目标院系？
  ┌─────────────────────────────────────────┐
  │ ┌──────────────┐ ┌──────────────┐        │
  │ │ ✅ 已有目标   │ │ ☐ 尚未确定   │        │
  │ │ 百分位+门槛   │ │ 仅百分位     │        │
  │ └──────────────┘ └──────────────┘        │
  │                                          │
  │ (选择"已有目标"后展开以下区域)              │
  │ ┌─────────────────────────────────────┐  │
  │ │ 目标院校层次  [顶尖] [985] [211] ...  │  │
  │ │ 目标院系大类  [经管法] [人文] ...     │  │
  │ │ 目标岗位级别  [讲师] [副教授(预聘)]...│  │
  │ │ 新入职老师成果清单（选填）             │  │
  │ │ [____________________________]      │  │
  │ └─────────────────────────────────────┘  │
  │                     [← 上一步]  [生成评估报告 →] │
  └─────────────────────────────────────────┘
```

**步骤指示器（Stepper）**：4 个步骤的视觉状态为——已完成（绿 ✓）、当前（蓝底数字）、未到达（白底灰边数字），步骤间以连接线分隔，已完成步骤间的连接线为绿色。

**Step 1 中的联动行为**：当用户切换「学科门类」时，「一级学科」下拉列表自动更新为对应门类的学科列表。若当前已选的一级学科不在新列表中，自动重置为新列表的第一个选项。

**提交后 Loading 状态**：显示 `LoadingBlock` 组件，文案为"正在比对同届同学科博士样本数据…"，持续 1.5 秒后自动切换到结果页。这 1.5 秒的等待用于模拟后端计算时间——实际算法在前端执行只需毫秒级，但保留加载过渡可以给用户一种"系统正在认真计算"的心理暗示。

##### 7.3.4 业务规则

**评分的五个维度**：教育背景、论文产出、基金课题、海外经历、学术年龄。每个维度输出 0-100 的标准化分数，五项等权平均得到综合得分。综合得分映射到百分位（百分位越高 = 竞争力越前）。

**目标院系对比的产出**：一份 6 行的对比表 + 一个综合判定（高度匹配 / 部分达标 / 差距较大）。判定依据是达标度百分比，其中"warn"状态按 0.5 权重计入达标度。

**建议系统**分为两组独立的规则：
- **基于分数阈值的建议**：论文 < 50、基金 < 40、海外 < 50、教育 > 80——无论用户是否填了目标院系，只要分数触发阈值就会显示
- **基于目标对比结果的建议**：全部达标时建议多投几家备选；缺口 ≥ 2 时建议下沉一档

**"新入职老师清单"的处理**：当前版本不做 NLP 解析。仅统计粘贴文本中的段落数量（以空行分隔、每段 > 8 个字符视为 1 条有效条目），用段落数量触发一个固定的基准偏移量。未来版本可用 LLM 或结构化表单替换此逻辑。

> **[PM 笔记] 为什么"新入职老师清单"不直接让用户填数字？**
>
> 让用户逐项填写目标院系入职者的中位 SCI 篇数、海外经历比例等，确实更精确。但有两个问题：（1）用户自己也不知道这些数字——他只知道"隔壁组张师兄发了 4 篇 SSCI 进去了"；（2）要求用户做数据采集，会让完成率急剧下降。
>
> 当前设计的权衡是：牺牲精确性换取可用性。用户粘贴他知道的碎片信息，系统给出一个"方向性正确"的对比。如果用户不粘贴，系统也有历史聚合数据兜底。这是"足够好 > 完全没有"的设计哲学。

##### 7.3.5 业务约束

- 算法所有参数均可通过管理后台修改（`/admin/benchmarks`），无需改代码重新部署
- 分析结果仅供参考，结果页必须展示免责声明（措辞与原型一致）
- 用户输入的论文数、课题数可以留空——空值在计算时视为 0
- 年龄可以输入极端值（如 18 或 60），算法不做前端拦截，由公式自然 clamp 到合理区间

---

#### 开发规范侧

> **前置依赖确认**：以下开发规范与模块 1（技术栈）、模块 6（路由表、数据流）保持一致。
> - 前端：Vite + React 18 + TypeScript + Tailwind CSS
> - 路由：react-router v6，路径 `/tools/threshold` 和 `/tools/threshold/result`
> - 状态：本页状态不跨页面共享，使用 `useState` 即可，不需要 Zustand
> - 降级：后端 API 不可用时，前端 `src/utils/scoring.ts` 中的纯函数直接计算

##### 7.3.6 文件清单

```
src/
  pages/
    ThresholdPage.tsx              # [新建] 顶层容器：stage + step 状态管理
  components/
    threshold/
      Stepper.tsx                  # [新建] 4 步骤指示器
      Step1BasicInfo.tsx           # [新建] Step 1 — 学科门类+一级学科+年龄+年限
      Step2Education.tsx           # [新建] Step 2 — 三个学历层次+海外经历
      Step3Publications.tsx        # [新建] Step 3 — 论文数+课题数
      Step4Target.tsx              # [新建] Step 4 — 条件分支（有/无目标）
      ChoiceCard.tsx               # [新建] 二选一卡片（已有目标 或 尚未确定）
      WizardNav.tsx                # [新建] 底部按钮组（上一步/下一步/生成报告）
      ThresholdResultView.tsx      # [新建] 结果页全貌（组合以下子组件）
      ScoreCard.tsx                # [新建] 结果头部卡片（百分位 / 判定结论）
      DimensionBar.tsx             # [新建] 单维度分数进度条
      ComparisonTable.tsx          # [新建] 目标院系对比表（6 行 × 4 列）
      RecommendationList.tsx       # [新建] 条件建议列表
    shared/
      StatusBadge.tsx              # [已有] pass/warn/fail 徽章，无需修改
      LoadingBlock.tsx             # [已有] 加载占位，无需修改
  utils/
    scoring.ts                     # [新建] 评分算法纯函数（详见 §7.3.9）
  types/
    threshold.ts                   # [新建] 全部 Threshold 相关类型定义
  data/
    disciplines.ts                 # [新建] 学科门类 ↔ 一级学科映射常量
    schoolScores.ts                # [新建] 院校层次分值表常量（前端降级用）
    tierBenchmarks.ts              # [新建] 各层次五维基准值常量（前端降级用）
```

**组件拆分原则**（对齐 §1.8 的规范）：
- 每个文件不超过 300 行——ThresholdPage 若超 300 行则将 `evaluate()` 调用逻辑抽取到 `useThreshold` Hook
- 数据常量（学科映射、分值表）与组件代码分离
- 全部文件使用 `.tsx`（含 JSX）或 `.ts`（不含 JSX），禁止使用 `any` 类型

##### 7.3.7 类型定义（`src/types/threshold.ts`）

以下类型为完整定义，开发者可直接复制使用：

```typescript
// ═══ 枚举类型 ═══

export type DisciplineMajor =
  | "经管法" | "人文" | "理工" | "医学" | "艺术与其他";

export type SchoolTier =
  | "C9" | "985" | "211" | "省属" | "海外名校" | "其他";

export type BachelorTier =
  | "C9" | "985" | "211" | "普通本科" | "民办" | "海外";

export type MasterSchoolOption = SchoolTier | "无";

export type TargetTier =
  | "顶尖 (C9 / 海外世界一流)"
  | "985 高校"
  | "211 高校"
  | "省属重点"
  | "一般本科";

export type TargetLevel =
  | "讲师 / 助理教授"
  | "副教授（预聘）"
  | "副教授（长聘）";

export type CompareStatus = "pass" | "warn" | "fail";

// ═══ 表单数据 ═══

export interface ThresholdForm {
  disciplineMajor: DisciplineMajor;
  disciplineMinor: string;
  phdYear: string;
  age: string;
  phdYears: string;
  phdSchool: SchoolTier;
  masterSchool: MasterSchoolOption;
  bachelorSchool: BachelorTier;
  overseas: "有" | "无";
  sci: string;
  cssci: string;
  nationalGrants: string;
  provincialGrants: string;
  hasTarget: "" | "yes" | "no";
  targetTier: TargetTier;
  targetDept: DisciplineMajor;
  targetLevel: TargetLevel;
  targetFacultyList: string;
}

// ═══ 评分中间结果 ═══

export interface ScoreBreakdown {
  eduScore: number;
  paperScore: number;
  grantScore: number;
  overseasScore: number;
  ageScore: number;
  userScore: number;
  percentile: number;
}

// ═══ 雷达图数据点 ═══

export interface RadarDataPoint {
  axis: "教育背景" | "论文产出" | "基金课题" | "海外经历" | "学术年龄";
  you: number;
  target?: number;  // 仅 hasTarget=true 时存在
}

// ═══ 对比表行 ═══

export interface ComparisonRow {
  item: string;
  need: string;
  you: string;
  status: CompareStatus;
}

// ═══ 评估结果（API 响应 & 前端降级共用） ═══

export interface ThresholdResult {
  breakdown: ScoreBreakdown;
  radarData: RadarDataPoint[];
  hasTarget: boolean;
  targetRows?: ComparisonRow[];
  matchPct?: number;
  verdict?: "高度匹配" | "部分达标" | "差距较大";
  facultyN?: number;
  recommendations: string[];
  disciplineMajor: DisciplineMajor;
  disciplineMinor: string;
  phdYear: string;
}

// ═══ 步骤枚举 ═══

export type ThresholdStep = 1 | 2 | 3 | 4;
export type ThresholdStage = "input" | "loading" | "result";

// ═══ 默认表单值 ═══

export const DEFAULT_FORM: ThresholdForm = {
  disciplineMajor: "经管法",
  disciplineMinor: "经济学",
  phdYear: "2025",
  age: "31",
  phdYears: "5",
  phdSchool: "985",
  masterSchool: "985",
  bachelorSchool: "211",
  overseas: "无",
  sci: "3",
  cssci: "5",
  nationalGrants: "1",
  provincialGrants: "1",
  hasTarget: "",
  targetTier: "985 高校",
  targetDept: "经管法",
  targetLevel: "讲师 / 助理教授",
  targetFacultyList: "",
};
```

##### 7.3.8 组件树与 Props 接口

```
ThresholdPage                    ← 页面容器，管理 stage/step/form 状态
├── Crumb                        ← [已有组件] 面包屑导航
├── SectionTitle                 ← [已有组件] 页面标题
├── [stage === "input"]
│   ├── Stepper                  ← current: ThresholdStep
│   ├── Step1BasicInfo           ← form + onChange + onNext
│   ├── Step2Education           ← form + onChange + onNext + onBack
│   ├── Step3Publications        ← form + onChange + onNext + onBack
│   └── Step4Target              ← form + onChange + onBack + onGenerate
│       └── ChoiceCard × 2       ← active + onClick + title + desc + icon
├── [stage === "loading"]
│   └── LoadingBlock             ← text="正在比对同届同学科博士样本数据…"
└── [stage === "result"]
    └── ThresholdResultView      ← result + onReset
        ├── ScoreCard × 2        ← label + value + subValue + detail + color
        ├── RadarChart           ← [Recharts] radarData + hasTarget
        ├── DimensionBar × 5     ← axis + you + target + showTarget
        ├── ComparisonTable      ← rows + facultyN + paperBench + overseasBench
        ├── RecommendationList   ← items: string[]
        └── 免责声明              ← [硬编码文案]
```

**各组件 Props 接口**：

```typescript
// ── Stepper ──
interface StepperProps {
  current: ThresholdStep;
  steps?: { n: number; label: string }[];
  // 内置默认值: [{n:1,label:"基础信息"},{n:2,label:"教育背景"},
  //              {n:3,label:"学术发表"},{n:4,label:"目标院系"}]
}

// ── Step1BasicInfo / Step2Education / Step3Publications ──
interface StepFormProps {
  form: ThresholdForm;
  onChange: <K extends keyof ThresholdForm>(key: K, value: ThresholdForm[K]) => void;
  onNext: () => void;
  onBack?: () => void;  // Step1 无此属性
}

// ── Step4Target（额外包含 onGenerate）──
interface Step4TargetProps extends StepFormProps {
  onGenerate: () => void;
}

// ── ChoiceCard ──
interface ChoiceCardProps {
  active: boolean;
  onClick: () => void;
  title: string;
  desc: string;
  icon: React.ComponentType<{ size?: number; className?: string }>;
    // 接受 lucide-react 的 Target / Compass 等图标组件
}

// ── WizardNav ──
interface WizardNavProps {
  onBack?: () => void;
  onNext?: () => void;
  onGenerate?: () => void;
  canGenerate?: boolean;   // 默认 true，hasTarget="" 时传 false
}

// ── ScoreCard ──
interface ScoreCardProps {
  label: string;           // "同届同学科竞争力" | "目标院系门槛对比"
  value: string;           // "前 25%" | "高度匹配"
  subValue?: string;       // "综合得分 75/100" | "达标度 83% · 达标 4 / 缺口 1"
  detail: string;          // 底部说明文字（来源说明 / 基准说明）
  color: "primary" | "green" | "amber" | "red";
}

// ── DimensionBar ──
interface DimensionBarProps {
  axis: string;
  you: number;
  target?: number;
  showTarget: boolean;
}

// ── ComparisonTable ──
interface ComparisonTableProps {
  rows: ComparisonRow[];
  facultyN?: number;
  paperBench?: number;
  overseasBench?: number;
}

// ── RecommendationList ──
interface RecommendationListProps {
  items: string[];
}

// ── ThresholdResultView ──
interface ThresholdResultViewProps {
  result: ThresholdResult;
  onReset: () => void;
}
```

##### 7.3.9 算法实现（`src/utils/scoring.ts`）

**约束**：以下所有函数必须是**纯函数**（相同输入 = 相同输出，无副作用）。前端降级和 API 后端使用完全相同的函数代码——Node.js 后端直接 `import { evaluate } from "../src/utils/scoring"`。

**常量定义**：

```typescript
// ═══ 院校分值映射表 ═══
// 来源：管理后台 /admin/benchmarks 可修改，此处为前端降级默认值
const SCHOOL_SCORE: Record<string, number> = {
  "C9": 100, "海外名校": 95, "985": 82, "海外": 78, "211": 62,
  "海外其他": 65, "省属": 40, "普通本科": 28, "民办": 12, "其他": 25, "无": 0,
};

// ═══ 层次基准表 ═══
const TIER_BENCHMARK: Record<string, {
  edu: number; paper: number; grant: number; overseas: number; age: number;
}> = {
  "顶尖 (C9 / 海外世界一流)": { edu: 95, paper: 92, grant: 88, overseas: 90, age: 78 },
  "985 高校":                  { edu: 78, paper: 72, grant: 65, overseas: 70, age: 72 },
  "211 高校":                  { edu: 62, paper: 56, grant: 45, overseas: 50, age: 70 },
  "省属重点":                  { edu: 48, paper: 40, grant: 30, overseas: 35, age: 68 },
  "一般本科":                  { edu: 35, paper: 28, grant: 18, overseas: 22, age: 65 },
};

// ═══ 院校层次排序（用于 judgeSchool） ═══
const SCHOOL_ORDER: Record<string, number> = {
  "C9": 5, "海外名校": 5, "985": 4, "211": 3, "省属": 2, "其他": 1,
};
```

**函数签名**：

```typescript
export function clamp(value: number, min?: number, max?: number): number;
export function calcEduScore(phd: string, master: string, bachelor: string): number;
export function calcPaperScore(sci: number, cssci: number): number;
export function calcGrantScore(national: number, provincial: number): number;
export function calcOverseasScore(hasOverseas: boolean): number;
export function calcAgeScore(age: number, phdYears: number): number;
export function calcPercentile(userScore: number): number;
export function judgeSchool(have: string, targetTier: string): CompareStatus;
export function parseFacultyList(text: string): number;
export function getTargetBench(targetTier: string, facultyN: number): {
  edu: number; paper: number; grant: number; overseas: number; age: number;
};
export function generateRecommendations(
  scores: ScoreBreakdown,
  hasTarget: boolean,
  targetRows?: ComparisonRow[],
  currentTier?: TargetTier,
): string[];
export function evaluate(form: ThresholdForm): ThresholdResult;
```

**核心实现**（此处以 `evaluate` 主函数为例，完整实现需覆盖以上所有函数）：

```typescript
export function evaluate(form: ThresholdForm): ThresholdResult {
  // 解析数值，空字符串 / 非数字 → 0
  const sci = parseInt(form.sci) || 0;
  const cssci = parseInt(form.cssci) || 0;
  const ng = parseInt(form.nationalGrants) || 0;
  const pg = parseInt(form.provincialGrants) || 0;
  const age = parseInt(form.age) || 30;
  const phdYears = parseInt(form.phdYears) || 4;

  // 分项评分
  const eduScore = calcEduScore(form.phdSchool, form.masterSchool, form.bachelorSchool);
  const paperScore = calcPaperScore(sci, cssci);
  const grantScore = calcGrantScore(ng, pg);
  const overseasScore = calcOverseasScore(form.overseas === "有");
  const ageScore = calcAgeScore(age, phdYears);
  const userScore = Math.round(
    (eduScore + paperScore + grantScore + overseasScore + ageScore) / 5
  );
  const percentile = calcPercentile(userScore);

  const radarData: RadarDataPoint[] = [
    { axis: "教育背景", you: eduScore },
    { axis: "论文产出", you: paperScore },
    { axis: "基金课题", you: grantScore },
    { axis: "海外经历", you: overseasScore },
    { axis: "学术年龄", you: ageScore },
  ];

  const hasTarget = form.hasTarget === "yes";
  let targetRows: ComparisonRow[] | undefined;
  let matchPct: number | undefined;
  let verdict: ThresholdResult["verdict"];
  let facultyN: number | undefined;

  if (hasTarget) {
    facultyN = parseFacultyList(form.targetFacultyList);
    const bench = getTargetBench(form.targetTier, facultyN);
    radarData[0].target = bench.edu;
    radarData[1].target = bench.paper;
    radarData[2].target = bench.grant;
    radarData[3].target = bench.overseas;
    radarData[4].target = bench.age;

    targetRows = [
      {
        item: "博士院校层次",
        need: form.targetTier.startsWith("顶尖") ? "C9 / 海外名校"
          : form.targetTier.startsWith("985") ? "985 及以上" : "211 及以上",
        you: form.phdSchool,
        status: judgeSchool(form.phdSchool, form.targetTier),
      },
      {
        item: "SCI / SSCI 论文",
        need: `≥ ${Math.round(bench.paper / 14)} 篇`,
        you: `${sci} 篇`,
        status: sci >= Math.round(bench.paper / 14) ? "pass" : "fail",
      },
      {
        item: "CSSCI 论文",
        need: `≥ ${Math.round(Math.max(0, bench.paper - sci * 14) / 7)} 篇（与 SCI 折算）`,
        you: `${cssci} 篇`,
        status: cssci >= 3 ? "pass" : (cssci >= 2 ? "warn" : "fail"),
      },
      {
        item: "国家级 / 省部级课题",
        need: (form.targetTier.startsWith("顶尖") || form.targetTier.startsWith("985"))
          ? "≥ 1 项" : "推荐有",
        you: `国 ${ng} + 省 ${pg}`,
        status: (ng + pg) >= 1 ? "pass"
          : (form.targetTier.startsWith("一般") ? "warn" : "fail"),
      },
      {
        item: "海外经历",
        need: form.targetTier.startsWith("顶尖") ? "强烈推荐" : "推荐有",
        you: form.overseas,
        status: form.overseas === "有" ? "pass" : "warn",
      },
      {
        item: "学术年龄",
        need: `≤ ${form.targetTier.startsWith("顶尖") ? 33 : 35} 岁`,
        you: `${age} 岁`,
        status: age <= (form.targetTier.startsWith("顶尖") ? 33 : 35)
          ? "pass" : "warn",
      },
    ];

    const passN = targetRows.filter(r => r.status === "pass").length;
    const warnN = targetRows.filter(r => r.status === "warn").length;
    matchPct = Math.round((passN + warnN * 0.5) / 6 * 100);

    if (matchPct >= 80) verdict = "高度匹配";
    else if (matchPct >= 60) verdict = "部分达标";
    else verdict = "差距较大";
  }

  const recommendations = generateRecommendations(
    { eduScore, paperScore, grantScore, overseasScore, ageScore, userScore, percentile },
    hasTarget,
    targetRows,
    form.targetTier as TargetTier,
  );

  return {
    breakdown: { eduScore, paperScore, grantScore, overseasScore, ageScore, userScore, percentile },
    radarData, hasTarget, targetRows, matchPct, verdict, facultyN, recommendations,
    disciplineMajor: form.disciplineMajor,
    disciplineMinor: form.disciplineMinor,
    phdYear: form.phdYear,
  };
}
```

**自定义 Hook（`src/hooks/useThreshold.ts`）核心逻辑**：

```typescript
import { useState, useCallback } from "react";
import type { ThresholdForm, ThresholdStep, ThresholdStage, ThresholdResult } from "../types/threshold";
import { DEFAULT_FORM } from "../types/threshold";
import { evaluate } from "../utils/scoring";

export function useThreshold() {
  const [step, setStep] = useState<ThresholdStep>(1);
  const [stage, setStage] = useState<ThresholdStage>("input");
  const [form, setForm] = useState<ThresholdForm>({ ...DEFAULT_FORM });

  const onChange = useCallback(
    <K extends keyof ThresholdForm>(key: K, value: ThresholdForm[K]) => {
      setForm(prev => ({ ...prev, [key]: value }));
    }, []
  );

  const onNext = useCallback(() => setStep(s => Math.min(4, s + 1) as ThresholdStep), []);
  const onBack = useCallback(() => setStep(s => Math.max(1, s - 1) as ThresholdStep), []);

  const onGenerate = useCallback(async () => {
    setStage("loading");

    let result: ThresholdResult;
    try {
      const res = await fetch("/api/tools/threshold/evaluate", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(form),
        signal: AbortSignal.timeout(5000),
      });
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      result = await res.json();
    } catch {
      console.warn("[Threshold] API 不可用，降级为本地计算");
      result = evaluate(form);
    }

    // 保持 loading 至少 1.5s 以确保过渡动画完整
    setTimeout(() => {
      // 将 result 存入 sessionStorage 以便 ThresholdResultView 读取
      sessionStorage.setItem("threshold-result", JSON.stringify(result));
      setStage("result");
    }, Math.max(0, 1500 - Date.now() + startTime));
  }, [form]);

  const onReset = useCallback(() => {
    setStage("input");
    setStep(1);
    // 注意：不重置 form —— 保留用户已填内容
  }, []);

  return { step, stage, form, onChange, onNext, onBack, onGenerate, onReset };
}
```

**使用 sessionStorage 而非 URL 参数传结果**：评估结果对象较大（含雷达图数据、对比表、建议列表），不适合放在 URL 中。`ThresholdResultView` 挂载时从 `sessionStorage.getItem("threshold-result")` 读取，若无数据则重定向到 `/tools/threshold`。

##### 7.3.10 API 契约

```
POST /api/tools/threshold/evaluate
Content-Type: application/json
```

**Request Body**（完整 JSON 示例）：

```json
{
  "disciplineMajor": "经管法",
  "disciplineMinor": "应用经济学",
  "phdYear": "2025",
  "age": "31",
  "phdYears": "5",
  "phdSchool": "985",
  "masterSchool": "985",
  "bachelorSchool": "211",
  "overseas": "无",
  "sci": "3",
  "cssci": "5",
  "nationalGrants": "1",
  "provincialGrants": "1",
  "hasTarget": "yes",
  "targetTier": "985 高校",
  "targetDept": "经管法",
  "targetLevel": "讲师 / 助理教授",
  "targetFacultyList": ""
}
```

**Response 200**（完整 JSON 示例）：

```json
{
  "breakdown": {
    "eduScore": 76,
    "paperScore": 77,
    "grantScore": 52,
    "overseasScore": 18,
    "ageScore": 81,
    "userScore": 61,
    "percentile": 39
  },
  "radarData": [
    { "axis": "教育背景", "you": 76, "target": 78 },
    { "axis": "论文产出", "you": 77, "target": 72 },
    { "axis": "基金课题", "you": 52, "target": 65 },
    { "axis": "海外经历", "you": 18, "target": 70 },
    { "axis": "学术年龄", "you": 81, "target": 72 }
  ],
  "hasTarget": true,
  "targetRows": [
    { "item": "博士院校层次", "need": "985 及以上", "you": "985", "status": "pass" },
    { "item": "SCI / SSCI 论文", "need": "≥ 5 篇", "you": "3 篇", "status": "fail" },
    { "item": "CSSCI 论文", "need": "≥ 5 篇（与 SCI 折算）", "you": "5 篇", "status": "pass" },
    { "item": "国家级 / 省部级课题", "need": "≥ 1 项", "you": "国 1 + 省 1", "status": "pass" },
    { "item": "海外经历", "need": "推荐有", "you": "无", "status": "warn" },
    { "item": "学术年龄", "need": "≤ 35 岁", "you": "31 岁", "status": "pass" }
  ],
  "matchPct": 75,
  "verdict": "部分达标",
  "facultyN": 0,
  "recommendations": [
    "海外经历缺失。可考虑国家公派访学、博后或合作项目作为补强。",
    "「教育背景」是你的明显优势，建议在求职材料中突出展示。"
  ],
  "disciplineMajor": "经管法",
  "disciplineMinor": "应用经济学",
  "phdYear": "2025"
}
```

**Response 400**（缺少必填字段）：

```json
{ "error": "VALIDATION_ERROR", "message": "disciplineMajor 为必填字段" }
```

**Response 500**（后端异常）：

```json
{ "error": "INTERNAL_ERROR", "message": "评估计算失败" }
```

##### 7.3.11 状态机

```
                    ┌─────────┐
         ┌─────────→│  input   │ step ∈ {1,2,3,4}
         │          └────┬─────┘
         │               │ onGenerate() 触发
         │               ▼
         │          ┌─────────┐
         │          │ loading  │ 展示 LoadingBlock，≥1.5s
         │          └────┬─────┘
         │               │ 超时后自动
         │               ▼
         │          ┌─────────┐
         │  重置    │ result   │ 从 sessionStorage 读取 ThresholdResult
         └──────────│          │
         onReset()  └─────────┘
```

**状态合法性校验**：`ThresholdPage` 挂载时检查 `stage` / `step` 值。若不在合法范围内，静默重置为 `{ stage: "input", step: 1 }`。

**路由守卫**：`ThresholdResultView` 挂载时若 `sessionStorage.getItem("threshold-result")` 为 `null`，执行 `navigate("/tools/threshold", { replace: true })`。

##### 7.3.12 验收条件（Gherkin）

```
Feature: 学术职位门槛评估

  Scenario: 有目标-完整路径
    Given 用户在 Step 1 选择"理工"→"计算机科学与技术"，年龄 29，在读 5 年
    And   用户在 Step 2 选择 博士 C9、硕士 985、本科 211、无海外经历
    And   用户在 Step 3 输入 SCI 3、CSSCI 0、国家级 1、省部级 0
    And   用户在 Step 4 选择"已有目标"→"顶尖"→"理工"→"讲师/助理教授"，不粘贴清单
    When  用户点击"生成评估报告"
    Then  显示 Loading 1.5s
    And   结果页显示百分位卡片 + 判定卡片
    And   雷达图含两条折线
    And   对比表含 6 行且每行有状态徽章
    And   显示建议列表 + 免责声明

  Scenario: 无目标-精简结果
    Given 用户在 Step 4 选择"尚未确定目标院系"
    When  用户点击"生成评估报告"
    Then  结果页不含目标对比卡片和对比表
    And   雷达图仅一条折线

  Scenario: hasTarget 未选择时按钮置灰
    Given 用户在 Step 4 的 hasTarget 值为 ""
    Then  "生成评估报告"按钮 disabled

  Scenario: 粘贴清单触发定制基准
    Given 用户在成果清单中粘贴了 3 段有效内容
    When  用户点击"生成评估报告"
    Then  对比表上方显示"已识别 3 位新入职老师"

  Scenario: 学科联动
    Given 用户当前"经管法"→"应用经济学"
    When  切换为"理工"
    Then  一级学科列表变为理工类
    And   已选值重置为"数学"

  Scenario: 非数字输入不崩溃
    Given 用户在 SCI 输入 "abc"
    When  点击"生成评估报告"
    Then  "abc" 被 parse 为 0，正常生成结果

  Scenario: 直接访问结果页重定向
    When  用户在浏览器输入 /tools/threshold/result（无 sessionStorage）
    Then  重定向到 /tools/threshold

  Scenario: 重新评估保留表单数据
    When  用户在结果页点击"重新评估"
    Then  回到 Step 1，表单数据保留
```

##### 7.3.13 测试用例（数值验证）

开发者须知：以下用例用于验证 `scoring.ts` 中算法的正确性。推荐写入 `src/utils/scoring.test.ts`。

| 用例编号 | 输入摘要 | 分项预期 | 综合预期 |
|---|---|---|---|
| T01-满分 | C9博 + C9硕 + C9本 + 海外有 + SCI=8 + 国课题=3 + 年龄=28 + 年限=4 | edu=100, paper=100(clamp), grant=100(clamp), overseas=92, age=100 | userScore=98, percentile≈2 |
| T02-最低 | 其他博 + 无硕 + 民办本 + 海外无 + 论文=0 + 课题=0 + 年龄=50 + 年限=10 | edu≈24, paper=0, grant=0, overseas=18, age=0 | userScore≤10, percentile≥90 |
| T03-中庸 | 211博 + 211硕 + 普通本科 + 海外无 + SCI=2 + CSSCI=3 + 国课题=0 + 省课题=1 + 年龄=30 + 年限=5 | edu≈57, paper=49, grant=14, overseas=18, age=86 | userScore≈45, percentile≈55 |
| T04-粘贴解析 | targetFacultyList="张\n\n李\n\n王"（"王"长度≤8） | facultyN=2 | — |
| T05-空清单 | targetFacultyList="" | facultyN=0 | 偏移量为 0 |
| T06-学校判定 | phdSchool="C9", targetTier="顶尖" | judgeSchool → "pass" | — |
| T07-学校判定 | phdSchool="211", targetTier="985 高校" | judgeSchool → "warn" | — |
| T08-学校判定 | phdSchool="省属", targetTier="211 高校" | judgeSchool → "fail" | — |

##### 7.3.14 管理后台配置存储

`/admin/benchmarks` 页面对应的数据库表（对齐模块 6 中的管理后台功能定义）：

```sql
CREATE TABLE benchmarks (
  id         INT PRIMARY KEY AUTO_INCREMENT,
  `key`      VARCHAR(64)  NOT NULL UNIQUE,
  -- 可选值: "school_scores" | "tier_benchmarks" | "recommendation_rules"
  data       JSON         NOT NULL,
  updated_at DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 种子数据（V1 部署时执行）
INSERT INTO benchmarks (`key`, data) VALUES
('school_scores',
  '{"C9":100,"海外名校":95,"985":82,"海外":78,"211":62,"海外其他":65,"省属":40,"普通本科":28,"民办":12,"其他":25,"无":0}'),
('tier_benchmarks',
  '{"顶尖 (C9 / 海外世界一流)":{"edu":95,"paper":92,"grant":88,"overseas":90,"age":78},"985 高校":{"edu":78,"paper":72,"grant":65,"overseas":70,"age":72},"211 高校":{"edu":62,"paper":56,"grant":45,"overseas":50,"age":70},"省属重点":{"edu":48,"paper":40,"grant":30,"overseas":35,"age":68},"一般本科":{"edu":35,"paper":28,"grant":18,"overseas":22,"age":65}}'),
('recommendation_rules',
  '[{"condition":"paperScore < 50","text":"「论文产出」是主要短板。建议在 1-2 年内集中冲击 SCI / SSCI 一作 1-2 篇。"},{"condition":"grantScore < 40","text":"「基金课题」较弱。可尝试国社科青年 / 国自然青年 / 省部级一般项目。"},{"condition":"overseasScore < 50","text":"海外经历缺失。可考虑国家公派访学、博后或合作项目作为补强。"},{"condition":"eduScore > 80","text":"「教育背景」是你的明显优势，建议在求职材料中突出展示。"}]');
```

**后端读取方式**：API 启动时从 `benchmarks` 表读取配置缓存到内存，`evaluate()` 使用缓存值计算。管理后台修改配置后通过 API 触发缓存刷新。

---

*§7.3 学术职位门槛评估 结束。*

---

### 7.1 博士职业地图

---

#### 产品需求侧

##### 7.1.1 功能定位

博士职业地图是用户进入产品后接触的第一个核心模块，对应模块 2 功能全景中的「了解选项」层。它回答的问题是：**"博士毕业后，我到底有哪些职业方向可以选？"**

本功能与另外两个浏览型模块并列构成"职业探索"导航栏下的全部内容：

| 模块 | 解决的问题 | 用户行为 |
|---|---|---|
| 职业地图（本功能） | 有什么方向可以选？每个方向具体做什么？ | 浏览 → 点进感兴趣的 → 看岗位/雇主/访谈 |
| 岗位精选（§7.2） | 现在有哪些具体岗位在招？ | 搜索 → 筛选 → 看详情 |
| 职业数据（§7.6） | 每个方向的真实薪资和去向数据？ | 查看图表 → 搜索数据表 |

##### 7.1.2 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-MAP-01 | 迷茫型博士生（Persona 小陈） | 看到一个系统性的、可信的"博士非学术职业全景图" | 知道除了学术以外还有哪些方向存在，每个方向具体做什么 |
| US-MAP-02 | 探索型博士生（Persona 小赵） | 深入查看某几个我感兴趣的类别的代表岗位、典型雇主、真实薪资 | 把模糊的"我想去产业界"细化为"我想去科技企业的 AI 研究岗" |
| US-MAP-03 | 评估完成用户 | 从评估结果页的 Top 3 卡片直接跳转到对应职业分类详情 | 把评估给我指的方向变成具体可投递的岗位信息 |
| US-MAP-04 | 纠结型博士生（Persona 小周） | 在对比两个方向时，查看每个方向里真实转型者的经历 | 从"走过这条路的人"的判断中获取决策参考 |
| US-MAP-05 | 管理者 | 在后台增删改职业分类及其下属的岗位、雇主、访谈、招聘信息 | 保持地图数据的时效性和准确性 |

##### 7.1.3 交互流程

本功能包含两个页面：地图总览页（分类入口）和分类详情页。

**地图总览页（`/careers`）**：

```
┌─────────────────────────────────────────────────┐
│  面包屑：首页 > 博士职业地图                       │
│                                                  │
│  博士职业地图                                     │
│  8 类博士毕业生职业分类。点击进入查看代表岗位、       │
│  典型雇主与转型路径。                              │
│                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ 1 学术支撑│ │ 2 党政管理│ │ 3 社会智库│         │
│  │ 高校/科研 │ │ 公务员/  │ │ 行业研究 │         │
│  │ 机构的教研│ │ 选调生/  │ │ 决策咨询 │         │
│  │ 行政与支撑│ │ 党校     │ │          │         │
│  │ N 个岗位  │ │ N 个岗位  │ │ N 个岗位  │         │
│  └──────────┘ └──────────┘ └──────────┘         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ 4 科技企业│ │ 5 文化出版│ │ 6 医疗健康│         │
│  └──────────┘ └──────────┘ └──────────┘         │
│  ┌──────────┐ ┌──────────┐                      │
│  │ 7 军警文职│ │ 8 其他   │                      │
│  └──────────┘ └──────────┘                      │
└─────────────────────────────────────────────────┘
```

每张分类卡片展示：分类名称 + 涵盖范围简述 + 该分类下的代表岗位数量。

**分类详情页（`/careers/:id`）**：

```
┌─────────────────────────────────────────────────┐
│  面包屑：首页 > 博士职业地图 > 学术支撑            │
│                                                  │
│  学术支撑                                         │
│  高校教研岗、高校教辅/管理岗、科研院所——高校与       │
│  科研机构内部的科研、教学、行政支撑与信息服务。       │
│                                                  │
│  ┌──────────────────────┐  ┌──────────────────┐  │
│  │                      │  │  测试适配度       │  │
│  │  代表岗位             │  │  你与「学术支撑」  │  │
│  │  ┌─────────────────┐ │  │  匹配吗？        │  │
│  │  │ 助理教授          │ │  │                  │  │
│  │  │ 20-35 万         │ │  │  [开始职业评估]   │  │
│  │  └─────────────────┘ │  └──────────────────┘  │
│  │  ┌─────────────────┐ │  ┌──────────────────┐  │
│  │  │ 副研究员          │ │  │  正在招聘         │  │
│  │  │ 18-35 万         │ │  │  · 青年研究员     │  │
│  │  └─────────────────┘ │  │  · 助理教授       │  │
│  │  ...                 │ │  │  [查看全部职位]   │  │
│  │                      │  └──────────────────┘  │
│  │  典型雇主             │                       │
│  │  [C9高校] [985高校]   │  ┌──────────────────┐  │
│  │  [中科院系统] ...     │  │  相关工具         │  │
│  │                      │  │  · 门槛评估       │  │
│  │  转型者访谈           │  │  · JD 预警        │  │
│  │  「...」 —— 王女士    │  │  · 薪资数据       │  │
│  │  「...」 —— 陈女士    │  └──────────────────┘  │
│  └──────────────────────┘                       │
└─────────────────────────────────────────────────┘
```

**详情页侧边栏三个按钮的跳转目标**（与模块 6 §6.4 流转图一致）：

| 按钮 | 跳转路径 |
|---|---|
| 开始非学术职业评估 | `/tools/assessment` |
| 查看该分类薪资数据 | `/insights?tab=salary&family=:id` |
| 我离这个岗位的门槛还有多远 | `/tools/threshold` |
| 分析这份 JD 的风险条款 | `/tools/warning` |

**详情页布局**：左侧主内容区（代表岗位 + 典型雇主 + 转型者访谈），右侧粘性侧边栏（测试适配度卡片 + 正在招聘卡片 + 相关工具卡片）。

##### 7.1.4 业务规则

- 分类 ID 1-7 有完整的岗位列表和雇主信息展示；分类 8（其他）因其聚合属性，展示的是子方向入口链接（基础教育 / 金融机构 / 国际组织等），而非统一岗位列表
- 每个分类的 `positions` 数组至少包含 2 个代表岗位，每个岗位展示：职衔、薪资范围、一句话描述
- 转型者访谈来自 `interviews` 数据，展示引用语 + 受访者信息
- "正在招聘"区域展示该分类下的岗位（来自 `jobs` 数据中 `type` 或 `familyId` 匹配的条目），最多展示 3 条，超出部分通过"查看全部职位 →"跳转到岗位精选页
- 分类 6（医疗健康）和分类 7（军警文职）是新增分类，V1 的数据量可能较少——在详情页标注"数据持续完善中"

---

#### 开发规范侧

##### 7.1.5 文件清单

```
src/
  pages/
    MapPage.tsx                     # [新建] 8 分类卡片网格
    CategoryDetailPage.tsx          # [新建] 分类详情（左内容 + 右侧边栏）
  components/
    career/
      CategoryCard.tsx              # [新建] 地图总览页中的单张分类卡片
      PositionItem.tsx              # [新建] 代表岗位行
      EmployerTag.tsx               # [新建] 典型雇主标签
      InterviewQuote.tsx            # [新建] 转型者访谈引用卡片
      CategorySidebar.tsx           # [新建] 详情页右侧边栏容器
  data/
    categories.ts                   # [新建] 8 个分类的完整数据（原 14 族合并重组）
  types/
    career.ts                       # [新建] Category、Position、Interview 等类型
```

##### 7.1.6 类型定义（`src/types/career.ts`）

```typescript
// ═══ 代表岗位 ═══
export interface Position {
  title: string;     // "助理教授 / 特聘副研究员"
  salary: string;    // "20-35 万"
  desc: string;      // 一句话描述
}

// ═══ 转型者访谈 ═══
export interface CareerInterview {
  name: string;      // "王女士"
  quote: string;     // 访谈引用语
  path: string;      // 转型路径描述，如 "C9 副教授 → 国家级智库研究员"
}

// ═══ 招聘简讯 ═══
export interface OpeningBrief {
  title: string;     // "青年研究员（预聘）"
  org: string;       // "复旦大学新闻学院"
  location: string;  // "上海"
}

// ═══ 职业分类（8 个） ═══
export interface Category {
  id: number;                            // 1-8，与 §6.2 的定义一致
  name: string;                          // "学术支撑" | "党政管理" | ...
  tagline: string;                       // 一行简述，用于地图总览卡片
  overview: string;                      // 详细描述，用于详情页头部
  positions: Position[];                 // 代表岗位（≥2 个）
  employers: string[];                   // 典型雇主列表
  interviews: CareerInterview[];         // 转型者访谈
  openings: OpeningBrief[];              // 正在招聘简讯
}
```

##### 7.1.7 组件 Props 接口

```typescript
// ── MapPage ──
// 无外部 Props。内部从 categories 数据渲染 8 张 CategoryCard。

// ── CategoryCard ──
interface CategoryCardProps {
  category: Category;
  onClick: (id: number) => void;
}

// ── CategoryDetailPage ──
// 通过 react-router 的 useParams 获取 :id，从 categories 数据中查找。
// 若 id 无效（非 1-8），渲染 404 提示："该分类不存在"。

// ── PositionItem ──
interface PositionItemProps {
  position: Position;
}

// ── EmployerTag ──
interface EmployerTagProps {
  name: string;
}

// ── InterviewQuote ──
interface InterviewQuoteProps {
  interview: CareerInterview;
}

// ── CategorySidebar ──
interface CategorySidebarProps {
  categoryId: number;
  categoryName: string;
  openings: OpeningBrief[];
}
```

##### 7.1.8 数据层设计

`src/data/categories.ts` 导出 `CATEGORIES: Category[]`，为 8 个分类的完整数据数组。该数据从原 `FAMILIES_DATA`（14 个族）按 §6.2 的映射关系合并重组而来：

| 新分类 | 数据来源 |
|---|---|
| 1.学术支撑 | 合并原 1.高校教研岗 + 2.高校教辅/管理岗 + 3.科研院所 的 positions/employers/interviews/openings |
| 2.党政管理 | 直接继承原 5.体制内/公务员 |
| 3.社会智库 | 直接继承原 4.智库/政策研究 |
| 4.科技企业 | 合并原 6.国企/央企研究岗 + 7.企业研究院 + 8.金融/投研 + 10.咨询/战略 |
| 5.文化出版 | 合并原 9.出版/传媒 + 11.图书馆/博物馆 |
| 6.医疗健康 | **新建数据**，V1 至少需准备 2 个代表岗位、3 个典型雇主、1 条访谈 |
| 7.军警文职 | **新建数据**，V1 至少需准备 2 个代表岗位、3 个典型雇主 |
| 8.其他 | 合并原 12.国际组织/NGO + 13.教育/培训创业 + 14.其他/自由职业 |

**前端降级**：`categories.ts` 为静态数据，页面渲染不依赖后端 API。当后端 API 可用时，`MapPage` 从 API 获取数据以支持管理后台动态更新；API 不可用时降级到本地静态数据。

##### 7.1.9 API 契约

```
GET /api/careers/categories
```

**Response 200**：

```json
{
  "categories": [
    {
      "id": 1,
      "name": "学术支撑",
      "tagline": "高校、科研机构内部的教研、行政与信息服务",
      "overview": "面向高校与科研机构的科研、教学、行政支撑与信息服务岗位……",
      "positions": [
        { "title": "助理教授 / 特聘副研究员", "salary": "20-35 万",
          "desc": "讲师起步，3 年内须达成论文 + 课题门槛。" }
      ],
      "employers": ["C9 高校", "985 高校", "211 高校", "中科院系统", "省属重点高校"],
      "interviews": [
        { "name": "王女士", "quote": "三年预聘期是最难熬的阶段……",
          "path": "C9 副教授 → 国家级智库研究员" }
      ],
      "openings": [
        { "title": "青年研究员（预聘）", "org": "复旦大学新闻学院", "location": "上海" }
      ]
    }
  ]
}
```

```
GET /api/careers/categories/:id
```

**Response 200**：返回单个 Category 对象，结构同上 `categories[0]`。`id` 无效时返回 404。

##### 7.1.10 管理后台接口

管理员通过 `/admin/categories` 页面对分类数据进行 CRUD，API 路径前缀为 `/api/admin/careers/categories`。支持：
- `GET /api/admin/careers/categories` — 列表
- `PUT /api/admin/careers/categories/:id` — 编辑（修改 overview、positions、employers 等）
- 注意：分类的 `id` 和 `name`（8 个分类名）不可删除或修改——它们与评估工具的输出维度绑定

##### 7.1.11 验收条件

```
Feature: 博士职业地图

  Scenario: 浏览 8 个分类
    Given 用户访问 /careers
    Then  页面展示 8 张分类卡片
    And   每张卡片显示分类名称、涵盖简述、代表岗位数量
    And   卡片可按 3 列网格排列（桌面端）

  Scenario: 进入分类详情
    Given 用户在地图总览页
    When  用户点击"学术支撑"分类卡片
    Then  跳转到 /careers/1
    And   详情页展示概述、代表岗位、典型雇主、转型者访谈
    And   右侧边栏显示"测试适配度""正在招聘""相关工具"三个卡片

  Scenario: 无效分类 ID
    Given 用户访问 /careers/99
    Then  展示"该分类不存在"提示

  Scenario: 侧边栏跳转
    Given 用户在分类详情页
    When  用户点击"查看该分类薪资数据"
    Then  跳转到 /insights?tab=salary&family=1

  Scenario: 分类 8 特殊处理
    Given 用户进入分类 8（其他）详情页
    Then  展示子方向入口链接，而非统一岗位列表
```

---

### 7.2 岗位精选

---

#### 产品需求侧

##### 7.2.1 功能定位

岗位精选是用户从"了解方向"进入"开始行动"的过渡模块。它不与招聘平台竞争——不撮合投递、不收集简历。它的定位是：**让用户在完成方向探索后，看到"这个方向上现在有什么真实岗位在招"，以验证该方向的市场存在感。**

##### 7.2.2 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-JOB-01 | 探索型博士生（Persona 小赵） | 按类型和地区筛选岗位，并用关键词搜索 | 快速找到"科技企业"方向下"上海"区域的 AI 研究岗 |
| US-JOB-02 | 纠结型博士生（Persona 小周） | 看到每个岗位的完整职责、要求和福利 | 对比学术岗位和非学术岗位的实际工作内容差异 |
| US-JOB-03 | 迷茫型博士生（Persona 小陈） | 看到岗位的薪资范围和来源机构 | 建立对不同方向"市场价值"的初步认知 |
| US-JOB-04 | 管理者 | 在后台上架、编辑、下架岗位 | 保持岗位信息的准确性 |

##### 7.2.3 交互流程

**岗位列表页（`/jobs`）**：

- 顶部搜索框：输入关键词搜索职位名称、单位名称
- 筛选栏：类型筛选（全部 / 学术 / 智库/研究院 / 产业研究 / 出版传媒 / 体制内 / 国际组织）+ 地区筛选（全部 / 北京 / 上海 / 广深 / 江浙 / 海外）
- 搜索结果：每条岗位显示职衔、单位、地区、类型标签、薪资、发布时间。点击进入详情
- 结果计数："找到 N 个岗位"

**岗位详情页（`/jobs/:id`）**：

- 左侧主体：岗位职衔 + 单位/地区/发布日期 + 类型与薪资标签 + 岗位职责列表 + 任职要求列表 + 薪酬福利列表 + Editor Note（如有）
- 右侧侧边栏：投递引导卡片（声明"本平台不撮合招聘，将跳转至单位官方页面"）+ 相关工具卡片（门槛评估 / JD 预警）

##### 7.2.4 业务规则

- 岗位数据来自管理后台的增删改，同时前端保留静态数据副本作为降级
- 岗位的 `type` 字段用于类型筛选，可选值与 8 个分类不完全相同——它是招聘市场常用的岗位分类，比职业分类更细粒度
- "立即投递"按钮是一个外链——跳转至单位官方招聘页面，本平台不承载投递流程
- 岗位详情页底部不展示"相似岗位"推荐（V1 不做推荐算法）

---

#### 开发规范侧

##### 7.2.5 文件清单

```
src/
  pages/
    JobsPage.tsx                    # [新建] 岗位列表（搜索 + 筛选 + 结果）
    JobDetailPage.tsx               # [新建] 岗位详情页
  components/
    jobs/
      JobSearchBar.tsx              # [新建] 搜索框
      JobFilterRow.tsx              # [新建] 筛选按钮组（类型/地区）
      JobCard.tsx                   # [新建] 搜索结果中的单条岗位卡片
      JobSidebar.tsx                # [新建] 详情页右侧边栏
  data/
    jobs.ts                         # [新建] 岗位静态数据（前端降级用）
  types/
    job.ts                          # [新建] Job 相关类型
```

##### 7.2.6 类型定义（`src/types/job.ts`）

```typescript
export interface Job {
  id: number;
  title: string;                     // "青年研究员（特聘）"
  org: string;                       // "复旦大学新闻学院"
  region: string;                    // "上海" | "北京" | "广深" | "江浙" | "海外"
  type: string;                      // "学术" | "智库/研究院" | "产业研究" | "出版传媒" | "体制内" | "国际组织"
  salary: string;                    // "25-35 万"
  posted: string;                    // "3 天前" | "今天" | "1 周前"
  responsibilities: string[];        // 岗位职责（3-5 条）
  requirements: string[];            // 任职要求（3-5 条）
  benefits: string[];                // 薪酬福利
  note?: string;                     // Editor Note，可选
}

// 筛选状态
export interface JobFilters {
  type: string;                      // "全部" 或其他类型值
  region: string;                    // "全部" 或其他地区值
}

// 类型选项常量
export const JOB_TYPES = [
  "全部", "学术", "智库/研究院", "产业研究", "出版传媒", "体制内", "国际组织"
] as const;

export const JOB_REGIONS = [
  "全部", "北京", "上海", "广深", "江浙", "海外"
] as const;
```

##### 7.2.7 组件 Props 接口

```typescript
// ── JobsPage ──
// 内部管理 query / filters 状态。不跨页面共享，使用 useState。

// ── JobSearchBar ──
interface JobSearchBarProps {
  value: string;
  onChange: (value: string) => void;
  onSearch: () => void;
}

// ── JobFilterRow ──
interface JobFilterRowProps {
  label: string;                     // "类型" | "地区"
  options: readonly string[];       // JOB_TYPES 或 JOB_REGIONS
  value: string;
  onChange: (value: string) => void;
}

// ── JobCard ──
interface JobCardProps {
  job: Job;
  onClick: (id: number) => void;
}

// ── JobDetailPage ──
// 通过 useParams 获取 :id，从 jobs 数据中查找。
// 若 id 无效，展示"该岗位不存在"。

// ── JobSidebar ──
interface JobSidebarProps {
  job: Job;
}
```

##### 7.2.8 搜索与筛选实现逻辑

```typescript
// JobsPage 中的 useMemo 过滤逻辑
const filteredJobs = useMemo(() => {
  return jobs.filter(job => {
    const matchType = filters.type === "全部" || job.type === filters.type;
    const matchRegion = filters.region === "全部" || job.region === filters.region;
    const matchQuery = !query.trim()
      || job.title.includes(query)
      || job.org.includes(query);
    return matchType && matchRegion && matchQuery;
  });
}, [jobs, filters, query]);
```

##### 7.2.9 API 契约

```
GET /api/jobs
  Query params: ?type=&region=&q=
```

**Response 200**：

```json
{
  "jobs": [
    {
      "id": 0,
      "title": "青年研究员（特聘）",
      "org": "复旦大学新闻学院",
      "region": "上海",
      "type": "学术",
      "salary": "25-35 万",
      "posted": "3 天前",
      "responsibilities": [
        "承担本科生和研究生新闻传播学相关课程教学",
        "独立开展新闻传播学领域研究"
      ],
      "requirements": [
        "新闻传播学、社会学、政治学等相关专业博士学位",
        "35 周岁以下"
      ],
      "benefits": ["聘期 3+3 年", "年薪 25-35 万", "周转房"],
      "note": "学院近三年新入职者背景中位画像：博士院校 985 及以上……"
    }
  ],
  "total": 12
}
```

```
GET /api/jobs/:id
```

返回单个 Job 对象。`id` 无效时返回 404。

##### 7.2.10 管理后台接口

管理员通过 `/admin/jobs` 页面进行 CRUD，API 路径前缀为 `/api/admin/jobs`：

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/api/admin/jobs` | 列表（含已下架） |
| `POST` | `/api/admin/jobs` | 新增岗位 |
| `PUT` | `/api/admin/jobs/:id` | 编辑岗位 |
| `DELETE` | `/api/admin/jobs/:id` | 删除岗位（软删除，设置 `active=false`） |

**岗位表结构**（`jobs` 表）：

```sql
CREATE TABLE jobs (
  id              INT PRIMARY KEY AUTO_INCREMENT,
  title           VARCHAR(128)  NOT NULL,
  org             VARCHAR(128)  NOT NULL,
  region          VARCHAR(32)   NOT NULL,
  type            VARCHAR(32)   NOT NULL,
  salary          VARCHAR(32)   NOT NULL,
  posted          VARCHAR(32)   NOT NULL DEFAULT '今天',
  responsibilities JSON         NOT NULL,          -- string[]
  requirements    JSON         NOT NULL,          -- string[]
  benefits        JSON         NOT NULL,          -- string[]
  note            TEXT         NULL,
  active          BOOLEAN      NOT NULL DEFAULT TRUE,
  created_at      DATETIME     DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**与 8 分类的关联**：如需按职业分类筛选岗位，在 `jobs` 表中增加 `category_id` 字段（可选外键，关联分类表）。V1 前端通过 `job.type` 与分类名称做模糊匹配来关联，暂不引入强外键约束。

##### 7.2.11 验收条件

```
Feature: 岗位精选

  Scenario: 搜索岗位
    Given 用户在岗位列表页
    When  用户在搜索框输入"研究员"并点击搜索
    Then  列表仅展示 title 或 org 包含"研究员"的岗位
    And   显示"找到 N 个岗位"

  Scenario: 类型筛选
    Given 用户在岗位列表页
    When  用户选择类型筛选为"体制内"
    Then  列表仅展示 type="体制内"的岗位

  Scenario: 组合筛选
    Given 用户选择类型="产业研究"且地区="北京"
    When  筛选结果渲染
    Then  仅展示同时满足两个条件的岗位

  Scenario: 进入岗位详情
    Given 用户在岗位列表页
    When  用户点击某个岗位卡片
    Then  跳转到 /jobs/:id
    And   详情页展示完整的职责、要求、福利、Editor Note

  Scenario: 侧边栏工具跳转
    Given 用户在岗位详情页
    When  用户点击侧边栏"我离这个岗位的门槛还有多远"
    Then  跳转到 /tools/threshold
    When  用户点击"分析这份 JD 的风险条款"
    Then  跳转到 /tools/warning

  Scenario: 无效岗位 ID
    Given 用户访问 /jobs/999
    Then  展示"该岗位不存在"提示
```

---

*§7.1、§7.2 结束。*

---

### 7.4 职位预警（JD Warning）

---

#### 产品需求侧

##### 7.4.1 功能定位

职位预警是三大决策工具中的"保护性工具"。门槛评估回答"我够不够格"，职业评估回答"我适合什么方向"，职位预警回答"**这份 JD 里有什么坑**"。

它解决的核心问题是：学术招聘 JD 使用高度模板化的语言，关键风险信息隐藏在专业术语（"预聘制""一人一议""特聘"）背后。博士生作为求职市场的信息弱势方，往往入职后才发现条款含义。

##### 7.4.2 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-WARN-01 | 博士在读生（纠结型 Persona 小周） | 把一份学术招聘 JD 粘贴进去，系统自动扫描并标记风险条款 | 不用自己逐字解读那些"一人一议""非升即走"是什么意思 |
| US-WARN-02 | 博士在读生 | 看到每条风险背后的具体解释和面试时应该确认的问题清单 | 拿着这份清单去面试，不会被 HR 含糊其辞地带过去 |
| US-WARN-03 | 博士在读生 | 看到同类岗位的风险对照数据 | 知道自己面对的不是个别现象，而是这一类型岗位的普遍情况 |
| US-WARN-04 | 管理者 | 在后台增删改风险识别规则（关键词、风险等级、权重、面试问题模板） | 随着招聘条款的演变持续更新检测逻辑 |

##### 7.4.3 交互流程

```
┌─────────────────────────────────────────┐
│  职位预警                                │
│  粘贴一份学术招聘 JD，识别风险信号。        │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  粘贴招聘 JD 全文        [填入示例]  │   │
│  │  ┌────────────────────────────┐  │   │
│  │  │                            │  │   │
│  │  │  (textarea 12行)           │  │   │
│  │  │                            │  │   │
│  │  └────────────────────────────┘  │   │
│  │  [开始扫描]                       │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
        │  点击"开始扫描"
        ▼
┌─────────────────────────────────────────┐
│  Loading: "正在解析 JD…"  (1.5s)         │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  风险评分                                 │
│  ┌─────────────────────────┐            │
│  │ 高风险 / 中风险 / 低风险  │  78/100   │
│  │ 识别到 N 项关键风险      │            │
│  └─────────────────────────┘            │
│                                          │
│  识别到的风险       面试时建议确认         │
│  ┌──────────────┐  ┌──────────────┐     │
│  │ 预聘制 [高]   │  │ 1. 首聘期考核 │     │
│  │ 首聘期3年…   │  │ 未通过是否有  │     │
│  │              │  │ 过渡岗位？    │     │
│  │ 年龄门槛 [中] │  │ 2. 课题是否   │     │
│  │ ≤35岁…      │  │ 包含青年项目？│     │
│  │ ...          │  │ ...          │     │
│  └──────────────┘  └──────────────┘     │
│                                          │
│  同类岗位对照                             │
│  近两年同类岗位中约 73% 含预聘制条款…      │
│                                          │
│  [分析另一份]                             │
└─────────────────────────────────────────┘
```

**"填入示例 JD"按钮**：点击后在 textarea 中填入预设的示例 JD 文本，方便用户快速体验。示例 JD 需选择一个典型的"有风险"案例，而不是一个干净的理想 JD。

##### 7.4.4 业务规则：风险识别

V1 不引入 NLP/LLM，采用**关键词 + 模式匹配**。每个风险类型由一组触发条件定义：

| 风险类型 | 触发关键词/模式 | 风险等级 | 得分权重 |
|---|---|---|---|
| 预聘制 (Tenure-Track) | "预聘""特聘""tenure-track""3+3""聘期" | 高 | 25 |
| 数量化考核条款 | 正则 `/发表.*≥\s*\d+\s*篇/` 或 `/SSCI.*\d+\s*篇/` 或 `"课题.*≥\s*\d+\s*项"` | 高 | 25 |
| 年龄门槛 | "≤ 35 岁""35 周岁以下""不超过 35" | 中 | 20 |
| 薪资模糊条款 | "一人一议""含绩效""薪资面议""待遇从优" | 中 | 15 |
| 非升即走 | "非升即走""不予续聘""不续聘""考核不合格" | 高 | 25 |
| 安家费/住房模糊 | "提供住房""周转房"（未注明面积/产权） | 低 | 10 |

**风险评分算法**：`总分 = 匹配的风险项得分权重之和`（上限 100）。V1 中风险等级判定：`≥70 高风险`、`40-69 中风险`、`<40 低风险`。

**面试问题生成**：每条匹配的风险类型对应 1-2 条预置面试问题。结果页将这些问题的合集展示为编号列表。

**同类岗位对照**：当前使用静态对照文本（由管理者在后台配置），V2 可升级为基于真实数据库的动态统计。

---

#### 开发规范侧

##### 7.4.5 文件清单

```
src/
  pages/
    WarningPage.tsx                 # [新建] 顶层容器：stage 状态管理
  components/
    warning/
      JdInput.tsx                   # [新建] textarea + 示例按钮 + 提交按钮
      RiskScoreCard.tsx             # [新建] 风险评分头部卡片
      RiskItem.tsx                  # [新建] 单条风险明细
      RiskList.tsx                  # [新建] 风险列表容器
      InterviewChecklist.tsx        # [新建] 面试确认问题清单
      PeerComparison.tsx            # [新建] 同类岗位对照卡片
  utils/
    jdAnalyzer.ts                   # [新建] JD 扫描引擎（纯函数，前端降级用）
  types/
    warning.ts                      # [新建] Warning 相关类型
```

##### 7.4.6 类型定义（`src/types/warning.ts`）

```typescript
export type RiskLevel = "高" | "中" | "低";

export interface RiskRule {
  id: number;
  type: string;                     // "预聘制 (Tenure-Track)"
  level: RiskLevel;
  scoreWeight: number;              // 0-100 中的权重
  patterns: string[];               // 触发关键词/正则模式
  description: string;              // 风险描述模板
  interviewQuestions: string[];     // 面试时应确认的问题
}

export interface DetectedRisk {
  type: string;
  level: RiskLevel;
  desc: string;                     // 根据 JD 内容填充的具体描述
}

export interface WarningResult {
  score: number;                    // 0-100
  level: "高风险" | "中风险" | "低风险";
  riskCount: number;
  risks: DetectedRisk[];
  interviewQuestions: string[];
  peerComparison: string;           // 同类岗位对照文本
}

export type WarningStage = "input" | "scanning" | "result";
```

##### 7.4.7 扫描引擎实现（`src/utils/jdAnalyzer.ts`）

```typescript
import type { RiskRule, DetectedRisk, WarningResult } from "../types/warning";
import { RISK_RULES } from "../data/riskRules";  // 静态默认规则，后端 API 可用时覆盖

export function scanJD(jdText: string, rules: RiskRule[] = RISK_RULES): WarningResult {
  const detected: DetectedRisk[] = [];
  let totalScore = 0;

  for (const rule of rules) {
    const matched = rule.patterns.some(pattern => {
      try {
        return new RegExp(pattern, "i").test(jdText);
      } catch {
        return jdText.includes(pattern);
      }
    });

    if (matched) {
      totalScore += rule.scoreWeight;
      detected.push({
        type: rule.type,
        level: rule.level,
        desc: rule.description,
      });
    }
  }

  totalScore = Math.min(100, totalScore);

  const allQuestions = detected.flatMap(
    r => rules.find(rule => rule.type === r.type)?.interviewQuestions || []
  );
  const uniqueQuestions = [...new Set(allQuestions)];

  return {
    score: totalScore,
    level: totalScore >= 70 ? "高风险" : totalScore >= 40 ? "中风险" : "低风险",
    riskCount: detected.length,
    risks: detected,
    interviewQuestions: uniqueQuestions,
    peerComparison: generatePeerComparison(detected),
  };
}

function generatePeerComparison(risks: DetectedRisk[]): string {
  // V1: 返回静态模板文本。V2: 基于数据库动态生成。
  if (risks.length === 0) return "未识别到常见风险条款，该岗位 JD 相对透明。";
  return "近两年同类岗位中，以上识别到的条款在学术招聘 JD 中出现频率较高。建议结合面试深入核实。";
}
```

##### 7.4.8 API 契约

```
POST /api/tools/warning/scan
Content-Type: application/json

Request:  { "jdText": "XX 大学新闻与传播学院诚聘…" }
Response: { WarningResult }
```

前端降级：API 不可用时调用本地 `scanJD(jdText)`。

##### 7.4.9 管理后台配置存储

```sql
CREATE TABLE risk_rules (
  id                INT PRIMARY KEY AUTO_INCREMENT,
  type              VARCHAR(64)   NOT NULL,
  level             VARCHAR(8)    NOT NULL,     -- "高" | "中" | "低"
  score_weight      INT           NOT NULL,
  patterns          JSON          NOT NULL,     -- string[]
  description       TEXT          NOT NULL,
  interview_questions JSON        NOT NULL,     -- string[]
  active            BOOLEAN       DEFAULT TRUE,
  created_at        DATETIME      DEFAULT CURRENT_TIMESTAMP,
  updated_at        DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

##### 7.4.10 验收条件

```
Feature: 职位预警

  Scenario: 扫描示例 JD 识别风险
    Given 用户在输入页
    When  用户点击"填入示例 JD"
    And   点击"开始扫描"
    Then  结果页显示风险评分 > 0
    And   识别到至少 2 项风险
    And   显示面试确认问题清单
    And   显示同类岗位对照

  Scenario: 空文本无法提交
    Given 用户未粘贴任何内容
    Then  "开始扫描"按钮 disabled

  Scenario: 干净 JD 评分低
    Given 用户粘贴一段不含风险关键词的 JD
    When  点击"开始扫描"
    Then  风险评分 < 40，判定为"低风险"

  Scenario: 分析另一份回到输入页
    Given 用户在结果页
    When  点击"分析另一份"
    Then  回到输入页，textarea 清空
```

---

*§7.4 结束。*

---

### 7.5 博士非学术职业评估

---

#### 产品需求侧

##### 7.5.1 功能定位

博士非学术职业评估是整个产品的**核心差异化工功能**，也是 V1 最重的新建模块（原型中仅有一个占位演示页）。它解决的问题是：**"如果我不走学术这条路，我的兴趣和技能最适合匹配到哪个职业分类？"**

本功能定位为"探索起点而非决策终点"——给出方向性参考，不替用户做决定。

##### 7.5.2 总体结构

| 模块 | 名称 | 题量 | 作答方式 | 是否参与匹配算法 | 预计用时 |
|---|---|---|---|---|---|
| A | 基本信息 | 4 题 | 单选 | 仅用于常模分组，不参与排序 | 30 秒 |
| B | 兴趣评估 | 按 8 分类重新编排 | 1-5 李克特量表 | ✅ 权重 50% | 3 分钟 |
| C | 技能评估 | 24 题 | 1-5 李克特量表 | ✅ 权重 50% | 2 分钟 |
| D | 价值观 | 18 项中选 5 项并排序 | 拖拽排序 | ❌ 生成个性化追问清单 | 1.5 分钟 |
| E | 现实约束 | 5 题 | 单选/李克特 | ❌ 生成冲突提示标签 | 1 分钟 |
| **合计** | | 约 80 题 | | | **约 8 分钟** |

**输出**：8 个职业分类的匹配度降序排列 + 五维雷达图 + Top 3 分类卡片（可跳转职业地图详情）+ 价值观追问清单 + 约束冲突提示。

##### 7.5.3 设计原则

1. **兴趣 + 技能入算法，价值观不入**——避免普遍性偏好（"钱多""稳定"）污染匹配区分度（参照 myIDP 和 ImaginePhD 的成熟设计）
2. **题项问"活动"不问"身份"**——"我喜欢把研究发现写成面向决策者的短报告"，而非"我适合去智库吗"
3. **强制使用全区间**——作答页提示"请使用 1-5 的完整范围"，若某模块全部评分相同则弹出提醒
4. **专家权重矩阵**——V1 由编辑部预填权重矩阵，上线后推进专家调查校准
5. **中国情境模块 E**——编制、地域、薪资底线、纪律性组织接受度，不参与排序，仅在结果页生成冲突提示

##### 7.5.4 模块 B 按 8 分类重新编排

原问卷按 9 个方向（d1-d9）各 4 题 = 36 题。按 8 分类定稿后：

| 新分类 | 题量 | 来源 |
|---|---|---|
| 1.学术支撑 | 12 题 | 合并原 d1 教育教研(4题) + d2 科研院所(4题) + d3 学术支撑(4题) |
| 2.党政管理 | 4 题 | 继承原 d5 |
| 3.社会智库 | 4 题 | 继承原 d4 |
| 4.科技企业 | 4 题 | 继承原 d7 |
| 5.文化出版 | 4 题 | 继承原 d8 |
| 6.医疗健康 | 4 题 | 继承原 d9 |
| 7.军警文职 | 4 题 | 继承原 d6 |
| 8.其他 | 0 题 | 聚合性分类，不设专属兴趣题——由权重矩阵中的交叉载荷自然覆盖 |

**模块 B 总题量**：12 + 4×6 = 36 题（数量不变，归属重新编排）。

**前端随机打乱**：36 题以随机顺序呈现，避免用户识别出分类分组——此设计来自原问卷规范的约定。

##### 7.5.5 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-ASMT-01 | 迷茫型博士生（小陈） | 花 8 分钟完成一套测评，得到我最匹配的 Top 3 职业分类 | 知道"除了学术以外还有哪些方向适合我"，有一个探索的起点 |
| US-ASMT-02 | 探索型博士生（小赵） | 看到每个方向的匹配百分比和五维技能雷达图 | 理解自己为什么适合某个方向——而不仅是一个结论 |
| US-ASMT-03 | 纠结型博士生（小周） | 在价值观排序中选出对我最重要的 5 项，并看到对应的问题清单 | 在多个选项中做取舍时，有一个结构化的"我该问什么"清单 |
| US-ASMT-04 | 有现实约束的博士 | 在模块 E 中标注我的编制偏好、地域限制和薪资底线 | 结果页明确标注"你适合的方向和你最看重的条件之间是否存在冲突" |
| US-ASMT-05 | 管理者 | 在后台修改权重矩阵、题项文本、冲突提示规则 | 根据专家调查结果和用户反馈持续优化匹配准确度 |

##### 7.5.6 交互流程

作答页采用**分步模式**（5 个模块依次展示，顶部 Stepper + 底部"下一步"按钮组）。选择分步而非单页纵向滚动的原因：（1）87 题全量纵向滚动在移动端体验极差（需要持续滚屏 8 分钟）；（2）分步模式让用户在每个模块内有明确的"完成感"，降低中途放弃率；（3）Zustand store 的 `currentModule` 状态驱动 Stepper 和底部按钮，支持在已完成模块间自由跳转。

```
┌─────────────────────────────────────────┐
│  博士非学术职业评估                       │
│  约 80 题 · 预计 8 分钟 · 结果可保存       │
│                                          │
│  【模块 A】基本信息（4 题）                 │
│  学科门类  [经管法] [人文] …              │
│  目前阶段  [博士在读(前期)] …             │
│  毕业年份  [2026]                         │
│  是否有心仪方向  [完全没有] …             │
│                                          │
│  【模块 B】兴趣评估（36 题，随机顺序）       │
│  提示：凭直觉判断你对"做这件事本身"的喜欢     │
│  程度，而非它的收入或地位。请使用 1-5 完整范围│
│                                          │
│  B12 向初学者讲解复杂概念…  [1][2][3][4][5] │
│  B07 撰写学术论文并逐条回应…  [1][2][3][4][5] │
│  ...（36 题逐题展示）                      │
│                                          │
│  【模块 C】技能评估（24 题）                 │
│  提示：评估当前实际水平，允许出现低分          │
│  C01 独立设计并完整执行一项研究 [1]…[5]     │
│  ...（24 题逐题展示）                      │
│                                          │
│  【模块 D】价值观（从 18 项中拖拽选出 Top 5） │
│  提示：系统限制最多 5 项——被迫取舍正是本环节   │
│  的意义                                   │
│  [工作稳定性] [高收入] [学术自由] …          │
│  你的 Top 5：[________________]  (拖拽区域) │
│                                          │
│  【模块 E】现实约束（5 题）                  │
│  编制偏好  [必须有编制] …                  │
│  地域范围  [锁定特定城市] …                 │
│  年薪底线  [25-40 万]                      │
│  加班接受度 [1][2][3][4][5]                │
│  纪律组织接受度 [可以接受] …               │
│                                          │
│  [提交评估]                                │
└─────────────────────────────────────────┘
        │  提交
        ▼
┌─────────────────────────────────────────┐
│  Loading + 算法计算 (约 1s，前端本地计算)    │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  评估结果                                 │
│                                          │
│  🏆 你的 Top 1：科技企业 — 匹配度 92%       │
│  一句话画像："你擅长将研究能力转化为商业     │
│  解决方案，在快节奏、结果导向的环境中如鱼得水。" │
│                                          │
│  8 分类匹配条形排序                        │
│  ┌──────────────────────────┐           │
│  │ 科技企业    ████████████ 92% │           │
│  │ 社会智库    ██████████   78% │           │
│  │ 学术支撑    ████████     65% │           │
│  │ ...                         │           │
│  └──────────────────────────┘           │
│                                          │
│  五维雷达图                               │
│  (研究/表达/组织/技术/服务)                │
│                                          │
│  Top 3 方向卡片（含跳转职业地图）            │
│  价值观追问清单                            │
│  约束冲突提示 ⚠                            │
│                                          │
│  免责声明 + [保存结果（即将上线）]            │
│  [重新评估]                               │
└─────────────────────────────────────────┘
```

##### 7.5.7 业务规则：匹配算法

（算法逻辑沿用问卷设计文档 v1.0，适配 8 分类后公式不变，仅维度数从 9 变为 8。）

```
用户兴趣向量 I[n]（模块 B 的 n 题原始分 1-5）
用户技能向量 S[24]（模块 C 的 24 题原始分 1-5）

第1步 个体内中心化（抵消打分风格差异）：
  I'[i] = I[i] - mean(I)
  S'[i] = S[i] - mean(S)

第2步 分分类加权得分（d = 1..8）：
  interest_score(d) = Σ_i(I'[i] × W_I[d][i]) / Σ_i W_I[d][i]
  skill_score(d)    = Σ_i(S'[i] × W_S[d][i]) / Σ_i W_S[d][i]

第3步 合成并归一化到 0-100：
  raw(d) = 0.5 × interest_score(d) + 0.5 × skill_score(d)
  match%(d) = minmax_normalize(raw, across 8 分类) × 100

第4步 排序输出。并按模块 E 规则附加提示标签。
```

**五维雷达图折算**（展示用，不参与排序）：

| 雷达维度 | 来源（模块 C 技能域） |
|---|---|
| 研究倾向 | C-1 研究与分析（C01-C04 均值） |
| 表达传播 | C-2 写作与表达（C05-C08 均值） |
| 组织协调 | C-3 沟通协作 + C-4 组织管理（C09-C16 均值） |
| 技术数据 | C-5 数据与技术（C17-C20 均值） |
| 服务育人 | C-6 教学与服务（C21-C24 均值） |

##### 7.5.8 业务规则：模块 D 价值观追问清单

用户从 18 项价值观中选出 Top 5 并排序。系统根据所选价值观生成"在调研或面试时应当追问的问题清单"（每项价值观对应 1-2 条预置问题）。价值观不参与匹配算法。

##### 7.5.9 业务规则：模块 E 冲突提示

| 用户选择 | 触发冲突的分类 | 提示文案 |
|---|---|---|
| E01=必须有编制 | 科技企业 | "该分类以市场化聘用为主，与你的编制偏好存在冲突" |
| E01=倾向市场化 | 党政管理、军警文职 | "该分类为体制内身份，与你的市场化偏好存在冲突" |
| E03=40 万以上 | 学术支撑、文化出版、党政管理 | "该分类薪资中位数通常低于你的底线" |
| E04 ≤ 2 | 科技企业 | "该分类普遍节奏较快，与你的节奏偏好存在张力" |
| E05=不接受 | 军警文职、党政管理 | "该分类对纪律性与政治要求较高，与你的偏好冲突" |

提示标签仅显示在对应分类的匹配条形图旁边（角标），**不改变排序位置**。

---

#### 开发规范侧

##### 7.5.10 文件清单

```
src/
  pages/
    AssessmentPage.tsx              # [新建] 作答页：5 模块纵向布局 + 提交
    AssessmentResultPage.tsx        # [新建] 结果页：匹配排序 + 雷达图 + Top3 + 追问 + 冲突
  components/
    assessment/
      ModuleA.tsx                   # [新建] 基本信息 4 题
      ModuleB.tsx                   # [新建] 兴趣评估 36 题（随机顺序）
      ModuleC.tsx                   # [新建] 技能评估 24 题
      ModuleD.tsx                   # [新建] 价值观 18 选 5（拖拽排序）
      ModuleE.tsx                   # [新建] 现实约束 5 题
      LikertScale.tsx               # [新建] 1-5 李克特量表单选组件
      DragSortList.tsx              # [新建] 拖拽排序组件（模块 D 用）
      CategoryBar.tsx               # [新建] 单条匹配度条形
      RadarChart.tsx                # [复用] Recharts 雷达图
      Top3Card.tsx                  # [新建] Top 3 分类卡片
      ValuesQuestionList.tsx        # [新建] 价值观追问清单
      ConflictTag.tsx               # [新建] 冲突提示标签
      AssessmentSidebar.tsx         # [新建] 作答页侧边栏（进度指示 + 模块导航）
  hooks/
    useAssessment.ts                # [新建] 评估作答状态管理（Zustand store）
  utils/
    matching.ts                     # [新建] 匹配算法纯函数（前端降级用）
    radarCalc.ts                    # [新建] 五维雷达图折算
    conflictCheck.ts                # [新建] 冲突检测逻辑
  types/
    assessment.ts                   # [新建] Assessment 全部类型
  data/
    questions.ts                    # [新建] 模块 B/C/D/E 全部题项
    weights.ts                      # [新建] 权重矩阵（8 分类 × 题目）
    valuesQuestions.ts              # [新建] 价值观 → 追问清单映射
    conflictRules.ts                # [新建] 模块 E 冲突提示规则
```

##### 7.5.11 核心类型定义（`src/types/assessment.ts`）

```typescript
// ═══ 题项 ═══
export interface Question {
  id: string;                       // "B01", "C12", "D03", "E01"
  module: "A" | "B" | "C" | "D" | "E";
  text: string;                     // 中文题干
  scaleType: "likert5" | "single_choice" | "rank_select";
  options?: string[];               // single_choice 的选项列表
  dimension?: string;               // B: 分类代码, C: 技能域代码
}

// ═══ 用户答案 ═══
export interface Answers {
  A: Record<string, string>;                     // A01-A04
  B: Record<string, number>;                     // B01-B36: 1-5
  C: Record<string, number>;                     // C01-C24: 1-5
  D: string[];                                    // 选中的 5 个价值观 ID，按序
  E: Record<string, string | number>;            // E01-E05
}

// ═══ 匹配结果 ═══
export interface CategoryMatch {
  categoryId: number;               // 1-8
  categoryName: string;             // "科技企业" | "学术支撑" | ...
  matchPercent: number;             // 0-100
  interestScore: number;
  skillScore: number;
  conflicts: string[];              // 冲突提示文案列表
}

// ═══ 五维雷达 ═══
export interface RadarScores {
  research: number;                 // 研究倾向
  communication: number;            // 表达传播
  organization: number;             // 组织协调
  technical: number;                // 技术数据
  service: number;                  // 服务育人
}

// ═══ 评估结果 ═══
export interface AssessmentResult {
  matches: CategoryMatch[];         // 8 个分类，按 matchPercent 降序
  radar: RadarScores;
  top1Label: string;                // 一句话画像文案
  valuesQuestions: string[];        // 价值观追问清单
  createdAt: string;                // ISO 时间戳
}
```

##### 7.5.12 状态管理（Zustand Store）

```typescript
// src/hooks/useAssessment.ts
import { create } from "zustand";
import type { Answers, AssessmentResult } from "../types/assessment";

interface AssessmentState {
  currentModule: "A" | "B" | "C" | "D" | "E";
  answers: Answers;
  stage: "answering" | "calculating" | "result";
  result: AssessmentResult | null;

  setModule: (mod: AssessmentState["currentModule"]) => void;
  setAnswer: <M extends keyof Answers>(module: M, key: string, value: unknown) => void;
  submit: () => void;
  reset: () => void;
}
```

**Stage 流转**：`answering → (submit) → calculating → (compute done) → result → (reset) → answering`

**注意**：Store 中暂存答案的 `answers` 对象跨 5 个模块持久化。用户在模块间跳转时（通过侧边栏导航），已填内容不丢失。作答页 `useEffect` 中设置 `window.onbeforeunload` = 提示"评估结果尚未保存，确定离开吗？"

##### 7.5.13 匹配算法实现（`src/utils/matching.ts`）

前端降级和后端 API 使用同一份纯函数，核心签名：

```typescript
export function computeMatch(
  answersB: Record<string, number>,    // 36 个兴趣分 (1-5)
  answersC: Record<string, number>,    // 24 个技能分 (1-5)
  weightMatrix: number[][]             // [8分类][60题] 的权重矩阵
): CategoryMatch[];

export function computeRadar(answersC: Record<string, number>): RadarScores;

export function checkConflicts(
  answersE: Record<string, string | number>,
  matches: CategoryMatch[]
): CategoryMatch[];   // 返回注入了 conflicts 字段的 matches

export function generateValuesQuestions(
  top5Ids: string[]
): string[];
```

##### 7.5.14 模块 D 拖拽排序实现

使用 HTML5 Drag and Drop API 或 `@dnd-kit/core` 库。组件规格：
- 左侧：18 项价值观池（未选中）
- 右侧：Top 5 排序区（最多 5 项，可拖拽调整顺序）
- 超过 5 项后禁止从左侧拖入（显示提示"已选满 5 项"）
- 从右侧拖回左侧即可取消选择

##### 7.5.15 API 契约

```
POST /api/tools/assessment/evaluate
Content-Type: application/json

Request:  { "answers": Answers }
Response: { AssessmentResult }
```

前端降级：API 不可用时调用本地 `computeMatch()` + `computeRadar()` + `checkConflicts()`。

##### 7.5.16 数据库表结构

```sql
-- 题项表
CREATE TABLE assessment_items (
  item_id     VARCHAR(4)   PRIMARY KEY,       -- "B01", "C12", ...
  module      CHAR(1)      NOT NULL,           -- "A"|"B"|"C"|"D"|"E"
  dimension   VARCHAR(32),
  text_zh     TEXT         NOT NULL,
  scale_type  VARCHAR(16)  NOT NULL,
  options     JSON,                             -- single_choice 的选项
  sort_order  INT          DEFAULT 0,
  version     INT          DEFAULT 1
);

-- 权重矩阵表 (8分类 × 题项)
CREATE TABLE assessment_weights (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  item_id     VARCHAR(4)   NOT NULL,
  category_id TINYINT      NOT NULL,            -- 1-8
  weight      TINYINT      NOT NULL,            -- 0-3
  source      VARCHAR(16)  DEFAULT 'editorial', -- editorial | expert_survey
  FOREIGN KEY (item_id) REFERENCES assessment_items(item_id)
);

-- 价值观追问模板
CREATE TABLE values_questions (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  value_id    VARCHAR(4)   NOT NULL,            -- "D01"-"D18"
  question    TEXT         NOT NULL
);

-- 冲突提示规则
CREATE TABLE conflict_rules (
  id            INT PRIMARY KEY AUTO_INCREMENT,
  field         VARCHAR(8)  NOT NULL,            -- "E01"|"E02"|...
  condition     VARCHAR(64) NOT NULL,            -- "必须有编制"|"40 万以上"|...
  category_id   TINYINT     NOT NULL,            -- 触发的分类 1-8
  message       TEXT        NOT NULL
);
```

##### 7.5.17 验收条件（关键场景）

```
Feature: 博士非学术职业评估

  Scenario: 完成全部 5 个模块并获取结果
    Given 用户在作答页
    When  用户依次完成模块 A/B/C/D/E 并点击"提交评估"
    Then  计算完成后显示结果页
    And   显示 8 分类匹配度降序排列
    And   显示五维雷达图
    And   显示 Top 3 分类卡片（含"查看职业地图"链接）
    And   显示价值观追问清单
    And   显示免责声明

  Scenario: 模块 D 限制最多选 5 项
    Given 用户在模块 D 已选择 5 项价值观
    When  用户尝试再拖入第 6 项
    Then  操作被阻止，显示提示"已选满 5 项"

  Scenario: 模块 B 全打相同分数时提醒
    Given 用户在模块 B 的 36 题全部选择 3 分
    When  用户点击"提交评估"
    Then  弹出提醒"建议使用 1-5 的完整范围以获得更准确的匹配结果"
    And   用户可选择"继续提交"或"返回修改"

  Scenario: 模块 E 冲突提示
    Given 用户 E01 选择"必须有编制"且 Top 3 中包含"科技企业"
    Then  结果页"科技企业"旁显示冲突标签 ⚠

  Scenario: 直接访问结果页重定向
    When  用户直接访问 /tools/assessment/result（无答案数据）
    Then  重定向到 /tools/assessment

  Scenario: 页面刷新时提示保存
    Given 用户在作答页已填写部分答案
    When  用户尝试刷新或关闭页面
    Then  浏览器弹出确认对话框"评估尚未提交，确定离开吗？"
```

---

*§7.5 结束。*

---

### 7.6 职业数据洞察

---

#### 产品需求侧

##### 7.6.1 功能定位

职业数据洞察是三大浏览型模块之一（与职业地图、岗位精选并列），为用户的职业决策提供**量化参考**。用户在做"去学术还是去产业""选北京还是选上海""要编制还是要高薪"这类抉择时，需要看到客观数据而不仅仅是定性描述。

##### 7.6.2 用户故事

| 编号 | 作为… | 我想要… | 以便… |
|---|---|---|---|
| US-DATA-01 | 博士生 | 查看博士就业去向的变化趋势（2019-2025） | 理解"学术路径收窄"不是我个人的问题，而是整体趋势 |
| US-DATA-02 | 纠结型博士生 | 按职业分类筛选薪资数据，看到真实薪资中位数和样本量 | 在对比两个 offer 时有一个客观的参照系 |
| US-DATA-03 | 博士生 | 阅读已经完成转型的博士的真实访谈 | 从"走过这条路的人"的经验中获得心理支持 |

##### 7.6.3 页面结构（3 个 Tab）

```
┌─────────────────────────────────────────┐
│  职业数据                                │
│  [非学术就业去向] [薪资数据] [转型者访谈]   │
│                                          │
│  (当前激活 Tab 的内容区)                   │
└─────────────────────────────────────────┘
```

**Tab 1 — 就业去向**：堆叠面积图（2019-2025 趋势，学术/体制内/产业/其他）+ 可搜索的数据明细表（匿名编号、毕业学校、专业、入职机构、岗位类型、届数、入职地点）

**Tab 2 — 薪资数据**：按职业分类和年份筛选的数据表（机构、职位、职业分类、薪资中位数、年份、样本数、数据来源）

**Tab 3 — 转型者访谈**：双列访谈卡片网格，每条含引述语、受访者信息、转型路径、日期

##### 7.6.4 入口汇总（与 §6.4 流转图一致）

| 来源 | 触发方式 | 携带参数 |
|---|---|---|
| 首页 → 职业数据 | 点击"博士去向变化"区域的"查看完整数据" | — |
| 导航栏 | 点击"职业数据" | — |
| 职业分类详情 → 职业数据 | 点击侧边栏"查看该分类薪资数据" | `?tab=salary&family=:id` |

---

#### 开发规范侧

##### 7.6.5 文件清单

```
src/
  pages/
    InsightsPage.tsx                # [新建] 3 Tab 容器
  components/
    insights/
      DestinationChart.tsx          # [新建] 堆叠面积图 (Recharts)
      DestinationTable.tsx          # [新建] 去向数据表（搜索）
      SalaryTable.tsx               # [新建] 薪资数据表（筛选）
      InterviewGrid.tsx             # [新建] 访谈卡片网格
      InterviewCard.tsx             # [新建] 单条访谈卡片
  types/
    insights.ts                     # [新建] Destination, Salary, Interview 类型
  data/
    destinations.ts                 # [新建] 去向静态数据 (18 条)
    salaries.ts                     # [新建] 薪资静态数据 (19 条)
    interviews.ts                   # [新建] 访谈静态数据 (8 条)
```

##### 7.6.6 核心类型

```typescript
export interface Destination {
  id: string;           // "PHD-2025-001"
  school: string;       // "北京大学"
  major: string;        // "新闻学"
  org: string;          // "复旦大学新闻学院"
  type: "学术" | "产业" | "体制内" | "智库" | "国际组织";
  year: string;         // "2025届"
  location: string;     // "上海"
}

export interface SalaryRecord {
  org: string;          // "复旦大学"
  position: string;     // "青年研究员（特聘）"
  family: string;       // "高校教研岗" (V1保留旧分类名，待数据迁移后统一)
  salary: number;       // 28 (万元/年)
  year: number;         // 2025
  n: number;            // 样本数 18
  source: string;       // "公开聘任信息"
}

export interface InterviewRecord {
  name: string;
  from: string;
  to: string;
  date: string;         // "2026.04"
  quote: string;
}

export type InsightsTab = "destination" | "salary" | "interviews";
```

##### 7.6.7 路由与 Tab 联动

路径 `/insights`。通过 URL query 参数 `?tab=` 控制激活的 Tab，支持 `?tab=salary&family=1` 跳转时自动切换到薪资 Tab 并筛选特定分类。

##### 7.6.8 验收条件

```
Feature: 职业数据

  Scenario: 三个 Tab 切换
    Given 用户访问 /insights
    When  用户点击"薪资数据" Tab
    Then  展示薪资数据表，隐藏去向图

  Scenario: 从分类详情跳转携带筛选
    Given 用户从学术支撑分类详情点击"查看该分类薪资数据"
    When  页面加载 /insights?tab=salary&family=1
    Then  自动切换到薪资 Tab
    And   筛选器预选"学术支撑"

  Scenario: 去向表搜索
    Given 用户在就业去向 Tab
    When  用户在搜索框输入"北京大学"
    Then  数据表仅显示 school 包含"北京大学"的行
```

---

### 7.7 管理后台

---

#### 产品需求侧

##### 7.7.1 功能定位

管理后台是管理者（Admin 角色）维护平台数据和配置的入口。普通用户不可见。

##### 7.7.2 子模块清单（7 个）

| 编号 | 模块 | 管理者可以做什么 | 路由 |
|---|---|---|---|
| 7.7.1 | 仪表盘 | 查看数据条目计数（分类/岗位/数据/用户总数） | `/admin` |
| 7.7.2 | 职业分类管理 | 编辑 8 个分类的概述、代表岗位、典型雇主、关联访谈、招聘简讯 | `/admin/categories` |
| 7.7.3 | 岗位管理 | 新增/编辑/下架岗位 | `/admin/jobs` |
| 7.7.4 | 职业数据管理 | 新增/编辑/删除去向记录、薪资记录、访谈记录 | `/admin/insights` |
| 7.7.5 | 权重矩阵管理 | 查看/编辑/批量导入题目 × 分类的权重值（0-3） | `/admin/weights` |
| 7.7.6 | 风险规则管理 | 增删改风险识别规则（关键词、等级、权重、面试问题） | `/admin/rules` |
| 7.7.7 | 门槛基准管理 | 修改各层次院校的五维基准值和院校分值表 | `/admin/benchmarks` |
| 7.7.8 | 用户管理 | 查看用户列表、设置/撤销 Admin 权限、禁用账号 | `/admin/users` |

##### 7.7.3 业务规则

- 管理员登录为真实的 JWT 认证（V1 唯一实现的认证）
- 管理员账号由开发者在部署时直接写入数据库，不提供公开注册
- 仪表盘仅展示计数和最近更新摘要，不包含用户行为分析（V1 无埋点）

---

#### 开发规范侧

##### 7.7.4 文件清单

```
src/
  pages/admin/
    AdminLayout.tsx                 # [新建] 管理后台整体布局（侧边栏 + 内容区）
    AdminDashboardPage.tsx          # [新建] 仪表盘
    AdminCategoriesPage.tsx         # [新建] 职业分类 CRUD
    AdminJobsPage.tsx               # [新建] 岗位 CRUD
    AdminInsightsPage.tsx           # [新建] 职业数据 CRUD
    AdminWeightsPage.tsx            # [新建] 权重矩阵管理
    AdminRulesPage.tsx              # [新建] 风险规则管理
    AdminBenchmarksPage.tsx         # [新建] 门槛基准管理
    AdminUsersPage.tsx              # [新建] 用户管理
  components/admin/
    AdminSidebar.tsx                # [新建] 侧边栏导航
    DataTable.tsx                   # [新建] 通用数据表格（排序/分页/行操作）
    WeightMatrixEditor.tsx          # [新建] 权重矩阵编辑器（双向表格）
    ImportExportBar.tsx             # [新建] CSV/Excel 导入导出工具栏
```

##### 7.7.5 路由守卫

所有 `/admin/*` 路由的页面组件挂载时，检查 JWT Token 的有效性。无效或未登录时重定向到 `/login?redirect=/admin`。Token 存储在 `localStorage`，每次请求通过 `Authorization: Bearer <token>` 头发送。

##### 7.7.6 通用 API 模式

所有管理接口前缀 `/api/admin/*`。后端对每个请求校验 `req.user.role === "admin"`。每个 CRUD 模块遵循 RESTful 规范：

```
GET    /api/admin/{resource}          # 列表 (支持 ?page=&limit=&sort=)
GET    /api/admin/{resource}/:id      # 详情
POST   /api/admin/{resource}          # 新增
PUT    /api/admin/{resource}/:id      # 编辑
DELETE /api/admin/{resource}/:id      # 删除（软删除或硬删除视资源而定）
```

##### 7.7.7 权重矩阵编辑器（§7.7.5 专用）

权重矩阵是 8 列（分类）× N 行（题目）的双向表格，每个单元格是一个 0-3 的数字。编辑器需支持：
- 行冻结（左侧固定题号与题干）
- 列冻结（顶部固定分类名）
- 单元格单击编辑（点击后变为 input，失焦保存）
- 批量导入（上传 CSV/Excel 覆盖当前矩阵，需管理员确认）
- 导出当前矩阵为 CSV

##### 7.7.8 验收条件

```
Feature: 管理后台

  Scenario: 非 Admin 访问被拦截
    Given 用户未登录或角色不是 Admin
    When  用户访问 /admin 任意路径
    Then  重定向到 /login?redirect=/admin

  Scenario: 编辑职业分类
    Given 管理员在职业分类管理页
    When  编辑"学术支撑"的 overview 字段并保存
    Then  PUT /api/admin/careers/categories/1 成功
    And   前台 /careers/1 显示更新后的内容

  Scenario: 权重矩阵导入
    Given 管理员在权重矩阵管理页
    When  上传一个合法的 CSV 文件并确认导入
    Then  数据库 weights 表更新
    And   此后的评估计算使用新权重

  Scenario: 仪表盘数据计数
    Given 管理员访问 /admin
    Then  显示分类数=8、岗位数、数据条目数、用户数
    And   显示最近更新的条目列表
```

---

*模块 7 全部结束。下一模块：模块 8（算法与匹配模型）。*

---

## 模块 8：算法与匹配模型

> **本章定位**：汇总产品中所有算法的设计原理、数学模型、输入/输出规格。具体实现代码见模块 7 各节的开发规范侧，本章聚焦于**为什么这样设计**以及**算法之间的数据依赖关系**。

---

### 8.1 算法全景

本产品包含三类计算逻辑，按复杂度递增排列：

| 序号 | 算法 | 所属功能 | 输入 | 输出 | 运行位置 |
|---|---|---|---|---|---|
| A1 | 门槛评估评分 | §7.3 | 14 个表单字段 | 5 维得分 + 百分位 + 对比表 + 建议 | 前端优先（降级），后端可选 |
| A2 | JD 风险扫描 | §7.4 | 自由文本 | 风险评分 + 风险清单 + 面试问题 | 前端优先（降级），后端可选 |
| A3 | 职业评估匹配 | §7.5 | 约 60 个李克特分值 + 权重矩阵 | 8 分类匹配度排序 + 雷达图 | 前端优先（降级），后端可选 |

**三个算法的共同设计原则**：

1. **所有算法都有前端纯函数降级副本**——在模块 6 的降级策略下，即使后端不可用，核心计算仍能完成
2. **所有可调参数都存储在数据库**（不在代码中硬编码），管理者通过后台修改后即时生效
3. **所有算法都是确定性的**——相同输入严格产生相同输出，不引入随机性（百分位计算中的简化模型除外，已明确标注）

---

### 8.2 门槛评估评分算法（A1）

#### 8.2.1 设计原理

门槛评估要回答的是"你的学术资本在求职市场上处于什么位置"。学术资本被拆解为 5 个可量化维度，每个维度的权重通过领域经验设定。

**为什么是 5 个维度、等权平均？**

博士求职时，这 5 个维度在实际录用决策中的重要性大致相当——一个教育背景再好但论文为零的人几乎没有竞争力，反之亦然。等权设计反映了"木桶效应"：最短的板决定你的天花板。

**为什么学校层次分值是差异化赋值而非等距？**

现实求职中，C9 和 985 之间的差距 > 211 到省属之间的差距。学术劳动力市场对"头部"学校的溢价是非线性的。因此分值表采用经验映射而非等距公式。

#### 8.2.2 算法详述

详见 §7.3.9。核心公式：

```
分项得分:
  eduScore     = Σ(学校层次分 × 学历权重)                                // 博士 0.5 / 硕士 0.3 / 本科 0.2
  paperScore   = min(100, SCI×14 + CSSCI×7)                             // 14 和 7 为经验权重
  grantScore   = min(100, 国家级×38 + 省部级×14)
  overseasScore = 92 (有) / 18 (无)                                     // 二值化
  ageScore     = max(0, 100 - (年龄-28)×5 - (年限-4)×4)

综合得分 = avg(5 项分项得分)
百分位   = max(1, min(99, 100 - 综合得分))                              // 简化模型
```

#### 8.2.3 目标院系对比

6 行对比表，每行将用户值与目标层次基准比对，输出 pass / warn / fail 三态判定。达标度 = `(pass数 + warn数×0.5) / 6 × 100`。判定阈值：≥80 高度匹配、60-79 部分达标、<60 差距较大。

#### 8.2.4 可调参数

| 参数 | 存储位置 | 管理者可改 | 修改影响 |
|---|---|---|---|
| 学校层次分值表 | `benchmarks.school_scores` | ✅ | 所有用户的 eduScore |
| 层次基准表 | `benchmarks.tier_benchmarks` | ✅ | 目标院系对比基准 |
| 论文/课题经验权重（14, 7, 38, 14） | 当前硬编码 | V1 不可改 | paperScore, grantScore |
| 年龄/年限惩罚系数（5, 4） | 当前硬编码 | V1 不可改 | ageScore |
| 建议模板阈值与文案 | `benchmarks.recommendation_rules` | ✅ | 结果页建议列表 |

---

### 8.3 JD 风险扫描算法（A2）

#### 8.3.1 设计原理

JD 扫描的核心不是"理解语言"，而是**关键词 + 正则模式匹配**。学术招聘 JD 的语言高度模板化——"预聘制""非升即走""3+3 年""一人一议"这些词的含义在整个行业内是一致的。不需要 NLP 就能达到足够的召回率。

**为什么不做 NLP？**

V1 的定位是"覆盖 80% 的常见风险条款"。NLP 能提升剩余 20% 的边界 case，但引入的成本（模型部署、延迟增加、误判处理）远超收益。关键词匹配的缺点是漏掉新颖表述（如"我校实行岗位动态管理机制"代替"非升即走"），但这个缺点可以通过管理后台持续添加新关键词来缓解。

#### 8.3.2 算法详述

详见 §7.4.7。核心逻辑：

```
对每条风险规则 rule:
  对 rule.patterns 中的每个正则/关键词:
    如果 jdText 匹配 → 触发该条风险
                      → 总分 += rule.scoreWeight
                      → 面试问题 += rule.interviewQuestions

风险等级: ≥70 高风险 / 40-69 中风险 / <40 低风险
```

#### 8.3.3 可调参数

风险规则全部存储在 `risk_rules` 表，管理者可通过 `/admin/rules` 增删改。修改即时生效。

---

### 8.4 职业评估匹配算法（A3）——核心算法

#### 8.4.1 设计原理

这是产品最核心的差异化算法，设计参照 myIDP 和 ImaginePhD 的方法论。它要解决的问题是：**给定用户在 60 个题项上的自评分（1-5 李克特），输出用户在 8 个职业分类上的匹配度排序**。

**为什么用"个体内中心化"而不是直接用原始分？**

不同用户的打分风格差异很大。有人习惯给高分（"全打 4 或 5"），有人习惯保守（"全打 2 或 3"）。如果直接用原始分，高风格用户的兴趣向量绝对值偏高，会在所有分类上获得虚高的匹配度。中心化消除了这种"风格偏差"——只保留用户**相对于自己平均水平的偏好差异**。

**为什么兴趣和技能各占 50%？**

参照 myIDP 和 ImaginePhD 的设计——两个工具都将兴趣和技能的权重设为相等。这是基于"职业适配 = 你喜欢做什么 × 你擅长做什么"的直觉模型。如果一个方向你很有兴趣但完全不具备相关技能，匹配度应该适中而非虚高。

**为什么价值观不入算法？**

"钱多""稳定""离家近"是几乎所有人类的普遍偏好。如果把价值观纳入排序，这些普遍性偏好会主导匹配结果，挤压兴趣和技能的区分度。因此价值观改为生成个性化的"面试时应该问的问题清单"——这既是 myIDP 和 ImaginePhD 的验证过的设计，也更符合"帮你做功课而不是替你做决定"的产品哲学。

#### 8.4.2 算法详述

详见 §7.5.7 和 §7.5.13。核心公式：

```
给定：兴趣向量 I[36]，技能向量 S[24]（原始 1-5），权重矩阵 W_I[8][36] 和 W_S[8][24]

第1步 — 个体内中心化：
  mean_I = ΣI[i]/36,  I'[i] = I[i] - mean_I
  mean_S = ΣS[i]/24,  S'[i] = S[i] - mean_S

第2步 — 分分类加权得分（d = 1..8）：
  interest_score[d] = Σ(I'[i] × W_I[d][i]) / Σ W_I[d][i]    // 分母为权重和，避免"题多权大"
  skill_score[d]    = Σ(S'[i] × W_S[d][i]) / Σ W_S[d][i]

第3步 — 合成并归一化：
  raw[d] = 0.5 × interest_score[d] + 0.5 × skill_score[d]
  match%[d] = (raw[d] - min(raw)) / (max(raw) - min(raw)) × 100    // min-max 归一化

第4步 — 降序排列，附加冲突标签
```

**特殊情况**：若 `max(raw) == min(raw)`（所有分类得分相同），则所有 match% 设为 50（均匀分布，表示"当前信息不足以区分"）。

#### 8.4.3 权重矩阵

权重矩阵是本算法的核心参数。每个格子记录"某个分类 × 某个题项"的相关度：

| 权重值 | 含义 | 举例 |
|---|---|---|
| 0 | 无关 | "C17 编程能力"对"党政管理"分类的权重为 0 |
| 1 | 略相关 | "B02 讲解概念"对"社会智库"分类的权重为 1 |
| 2 | 相关 | "C06 非专业写作"对"科技企业"分类的权重为 2 |
| 3 | 核心要求 | "B13 政策建议转化"对"社会智库"分类的权重为 3 |

**V1 权重矩阵来源**：编辑部按 8 分类重新编排预填。具体编排规则：
- 原 d1/d2/d3 合并为"学术支撑"时，三组的题项全部保留在 B 模块，权重按新分类语义重新评定（不是简单地将旧权重相加或平均——因为"学术支撑"这个新分类的内涵比旧三方向各自的范围更广）
- 其余 5 个一对一映射的分类（党政管理、社会智库、科技企业、文化出版、医疗健康、军警文职），权重继承原问卷的对应方向权重

**V2 升级路径**：专家调查（≥20 人）替换 editorial 权重，数据源字段从 `editorial` 切换为 `expert_survey`。

---

### 8.5 五维雷达图折算（关联 A3）

#### 8.5.1 设计原理

匹配算法输出的是 8 个职业分类的匹配度，但用户还需要理解"为什么我适合科技企业"——是因为我研究能力强？还是表达能力强？五维雷达图回答这个问题。

雷达图维度与匹配算法是两个独立的计算树——匹配度来自**整个 B + C 模块的加权组合**，雷达图仅来自**C 模块的 6 个技能域聚合**。兴趣分（B 模块）不进雷达图。

#### 8.5.2 折算公式

详见 §7.5.7。6 技能域 → 5 雷达维度的映射：

| 雷达维度 | 技能域 | 计算公式 |
|---|---|---|
| 研究倾向 | C-1 研究与分析 | mean(C01, C02, C03, C04) |
| 表达传播 | C-2 写作与表达 | mean(C05, C06, C07, C08) |
| 组织协调 | C-3 沟通协作 + C-4 组织管理 | mean(C09-C16) |
| 技术数据 | C-5 数据与技术 | mean(C17, C18, C19, C20) |
| 服务育人 | C-6 教学与服务 | mean(C21, C22, C23, C24) |

**为什么"沟通协作"和"组织管理"合并为一个维度？**

在博士职业场景中，这两个技能域高度相关——沟通能力强的博士通常也擅长组织管理。将它们分为两个独立维度会导致雷达图上出现"假区别"（一个维度高一个维度低但实际含义重叠）。合并为"组织协调"后，5 个维度的信息密度更均衡。

---

### 8.6 冲突检测规则引擎（关联 A3）

#### 8.6.1 设计原理

模块 E 的 5 道现实约束题不参与匹配排序（保持算法纯净），但在结果页通过标签系统提醒用户"你喜欢的分类和你最看重的条件之间有潜在冲突"。

#### 8.6.2 触发规则

详见 §7.5.9。每条规则包含 3 个元素：`(触发条件, 目标分类, 提示文案)`。规则存储在 `conflict_rules` 表中，管理者可增删改。

**设计约束**：冲突标签仅显示在匹配条形图旁的角标位置，不改变分类的排序位置。这是产品哲学——"工具负责呈现信息，用户负责做决定"。

---

### 8.7 建议生成规则

#### 8.7.1 门槛评估建议（关联 A1）

条件渲染——每条建议有一个触发条件（如 `paperScore < 50`），条件满足时显示对应文案。详见 §7.3.5 和 §7.3.9 中的 `generateRecommendations` 函数。文案存储在 `benchmarks.recommendation_rules` JSON 中。

#### 8.7.2 价值观追问清单（关联 A3）

用户选中的每个价值观对应 1-2 条预置追问。生成逻辑：`用户的 Top 5 价值观 ID → 查表 → 拼接问题文本`。详见 §7.5.8。

---

### 8.8 算法版本管理

所有算法的参数（权重矩阵、分值表、风险规则、基准值）存储在数据库中，天然支持版本管理——`updated_at` 字段记录最后修改时间。未来如需回滚到历史参数，通过数据库备份实现。

评估算法本身（公式逻辑）的版本通过代码 Git 管理。若公式逻辑发生变更（如 V2 引入非线性权重），需同步更新：
1. 前端 `src/utils/matching.ts` 中的降级函数
2. 后端 API 中的同构实现
3. 本文档的对应小节

---

*模块 8 结束。下一模块：模块 9（数据模型汇总）。*

---

## 模块 9：数据模型汇总

> **本章定位**：汇总全产品涉及的全部数据库表，提供 ER 关系总览和每张表的完整 DDL。模块 7 中各功能模块已分散定义了各自的表结构，本章做统一索引和补充缺失的表。

---

### 9.1 ER 关系总览

```
┌──────────┐     ┌──────────────────┐     ┌──────────────────────┐
│  users   │────→│ assessment_results│←────│ assessment_items     │
│  (用户)   │     │ (评估结果快照)     │     │ (题项)               │
└──────────┘     └──────────────────┘     └──────────┬───────────┘
      │                                               │
      │ (Admin 可管理)                                  │ 权重关联
      ▼                                               ▼
┌──────────┐                                  ┌──────────────────┐
│ categories│                                  │ assessment_weights│
│ (8 分类)  │                                  │ (权重矩阵)        │
└────┬─────┘                                  └──────────────────┘
     │
     │ 可关联
     ▼
┌──────────┐     ┌──────────┐     ┌──────────┐
│   jobs   │     │destinations│    │ salaries │
│  (岗位)   │     │ (去向)    │     │ (薪资)   │
└──────────┘     └──────────┘     └──────────┘

┌──────────────┐  ┌───────────────┐  ┌────────────────┐
│  benchmarks  │  │  risk_rules   │  │conflict_rules  │
│  (门槛基准)   │  │  (风险规则)    │  │ (冲突规则)      │
└──────────────┘  └───────────────┘  └────────────────┘

┌──────────────────┐
│ values_questions │
│ (价值观追问模板)  │
└──────────────────┘

┌──────────┐
│ interviews│
│ (访谈)    │
└──────────┘
```

**表分类**：

| 类别 | 表名 | 用途 | 管理方式 |
|---|---|---|---|
| 用户 | `users` | 管理员账号 + 未来普通用户 | Admin 后台 |
| 业务核心 | `categories` | 8 个职业分类数据 | Admin CRUD |
| 业务核心 | `jobs` | 岗位信息 | Admin CRUD |
| 业务核心 | `assessment_items` | 评估题项（A/B/C/D/E 模块） | Admin 后台 |
| 业务核心 | `assessment_weights` | 权重矩阵 | Admin 后台 + 批量导入 |
| 业务核心 | `assessment_results` | 用户评估结果快照（V1 占位） | 用户提交后自动写入 |
| 内容 | `destinations` | 去向明细 | Admin CRUD |
| 内容 | `salaries` | 薪资数据 | Admin CRUD |
| 内容 | `interviews` | 转型者访谈 | Admin CRUD |
| 配置 | `benchmarks` | 门槛评估基准值 | Admin 后台 |
| 配置 | `risk_rules` | JD 风险检测规则 | Admin 后台 |
| 配置 | `conflict_rules` | 模块 E 冲突提示规则 | Admin 后台 |
| 配置 | `values_questions` | 价值观 → 追问映射 | Admin 后台 |

---

### 9.2 核心表 DDL

#### 9.2.1 `users`

```sql
CREATE TABLE users (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  username    VARCHAR(64)   NOT NULL UNIQUE,
  password    VARCHAR(255)  NOT NULL,           -- bcrypt hash
  role        ENUM('user','admin') NOT NULL DEFAULT 'user',
  discipline_major VARCHAR(32),                  -- 模块 A01，可选
  stage       VARCHAR(32),                       -- 模块 A02，可选
  active      BOOLEAN       NOT NULL DEFAULT TRUE,
  created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- V1 种子数据：一个初始管理员账号
-- 密码在部署时通过环境变量或脚本注入
```

#### 9.2.2 `categories`

```sql
CREATE TABLE categories (
  id          TINYINT       PRIMARY KEY,        -- 1-8，与 §6.2 定义一致
  name        VARCHAR(32)   NOT NULL UNIQUE,    -- "学术支撑" | "党政管理" | ...
  tagline     VARCHAR(128)  NOT NULL,
  overview    TEXT          NOT NULL,
  positions   JSON          NOT NULL,            -- Position[]
  employers   JSON          NOT NULL,            -- string[]
  interviews  JSON          NOT NULL,            -- CareerInterview[]
  openings    JSON          NOT NULL,            -- OpeningBrief[]
  updated_at  DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

> `positions`、`employers`、`interviews`、`openings` 使用 JSON 列而非独立关联表——每个分类的子数据量小（3-5 个岗位、5-8 个雇主），JSON 列简化了查询和编辑。管理后台直接读写整个 category 行即可。

#### 9.2.3 `jobs`

```sql
CREATE TABLE jobs (
  id              INT PRIMARY KEY AUTO_INCREMENT,
  title           VARCHAR(128)  NOT NULL,
  org             VARCHAR(128)  NOT NULL,
  region          VARCHAR(32)   NOT NULL,
  type            VARCHAR(32)   NOT NULL,
  salary          VARCHAR(32)   NOT NULL,
  posted          VARCHAR(32)   NOT NULL DEFAULT '今天',
  responsibilities JSON         NOT NULL,
  requirements    JSON          NOT NULL,
  benefits        JSON          NOT NULL,
  note            TEXT          NULL,
  category_id     TINYINT       NULL,            -- 关联分类，可选
  active          BOOLEAN       NOT NULL DEFAULT TRUE,
  created_at      DATETIME      DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
);
```

#### 9.2.4 `assessment_items`

```sql
CREATE TABLE assessment_items (
  item_id     VARCHAR(4)   PRIMARY KEY,          -- "B01", "C12", ...
  module      CHAR(1)      NOT NULL,              -- "A"|"B"|"C"|"D"|"E"
  dimension   VARCHAR(32)  NULL,                  -- B: 分类代码, C: 技能域代码
  text_zh     TEXT         NOT NULL,
  scale_type  VARCHAR(16)  NOT NULL,              -- likert5 | single_choice | rank_select
  options     JSON         NULL,                  -- single_choice 的选项
  sort_order  INT          DEFAULT 0,
  version     INT          DEFAULT 1
);
```

#### 9.2.5 `assessment_weights`

```sql
CREATE TABLE assessment_weights (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  item_id     VARCHAR(4)   NOT NULL,
  category_id TINYINT      NOT NULL,              -- 1-8
  weight      TINYINT      NOT NULL CHECK (weight BETWEEN 0 AND 3),
  source      VARCHAR(16)  DEFAULT 'editorial',
  FOREIGN KEY (item_id) REFERENCES assessment_items(item_id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE,
  UNIQUE KEY uk_item_category (item_id, category_id)
);
```

#### 9.2.6 `assessment_results`（V1 占位）

```sql
CREATE TABLE assessment_results (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  user_id     INT           NULL,                 -- V1 为 NULL（游客模式），V2 必填
  answers     JSON          NOT NULL,             -- 完整的 Answers 对象快照
  result      JSON          NOT NULL,             -- 完整的 AssessmentResult 对象快照
  created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);
```

> V1 中 `user_id` 为 NULL——评估结果仅在前端 sessionStorage 存储。此表预建，V2 引入用户登录后启用。

---

### 9.3 内容表 DDL

#### 9.3.1 `destinations`

```sql
CREATE TABLE destinations (
  id          VARCHAR(16)   PRIMARY KEY,          -- "PHD-2025-001"
  school      VARCHAR(64)   NOT NULL,
  major       VARCHAR(64)   NOT NULL,
  org         VARCHAR(128)  NOT NULL,
  type        VARCHAR(16)   NOT NULL,             -- "学术"|"产业"|"体制内"|"智库"|"国际组织"
  year        VARCHAR(8)    NOT NULL,             -- "2025届"
  location    VARCHAR(32)   NOT NULL
);
```

#### 9.3.2 `salaries`

```sql
CREATE TABLE salaries (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  org         VARCHAR(128)  NOT NULL,
  position    VARCHAR(128)  NOT NULL,
  family      VARCHAR(64)   NOT NULL,             -- 所属分类名
  salary      INT           NOT NULL,             -- 万元/年
  year        INT           NOT NULL,
  n           INT           NOT NULL DEFAULT 1,   -- 样本量
  source      VARCHAR(64)   NOT NULL
);
```

#### 9.3.3 `interviews`

```sql
CREATE TABLE interviews (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  name        VARCHAR(32)   NOT NULL,
  `from`      VARCHAR(128)  NOT NULL,
  `to`        VARCHAR(128)  NOT NULL,
  date        VARCHAR(16)   NOT NULL,             -- "2026.04"
  quote       TEXT          NOT NULL
);
```

---

### 9.4 配置表 DDL

#### 9.4.1 `benchmarks`

```sql
CREATE TABLE benchmarks (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  `key`       VARCHAR(64)   NOT NULL UNIQUE,
  data        JSON          NOT NULL,
  updated_at  DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 9.4.2 `risk_rules`

```sql
CREATE TABLE risk_rules (
  id                  INT PRIMARY KEY AUTO_INCREMENT,
  type                VARCHAR(64)   NOT NULL,
  level               VARCHAR(8)    NOT NULL,
  score_weight        INT           NOT NULL,
  patterns            JSON          NOT NULL,
  description         TEXT          NOT NULL,
  interview_questions JSON          NOT NULL,
  active              BOOLEAN       DEFAULT TRUE,
  created_at          DATETIME      DEFAULT CURRENT_TIMESTAMP,
  updated_at          DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 9.4.3 `conflict_rules`

```sql
CREATE TABLE conflict_rules (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  field       VARCHAR(8)   NOT NULL,
  condition   VARCHAR(64)  NOT NULL,
  category_id TINYINT      NOT NULL,
  message     TEXT         NOT NULL,
  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE
);
```

#### 9.4.4 `values_questions`

```sql
CREATE TABLE values_questions (
  id          INT PRIMARY KEY AUTO_INCREMENT,
  value_id    VARCHAR(4)   NOT NULL,              -- "D01"-"D18"
  question    TEXT         NOT NULL
);
```

---

### 9.5 V1 种子数据要求

部署时需要执行以下种子数据插入：

| 表 | 种子内容 | 条数 |
|---|---|---|
| `users` | 1 个初始 Admin 账号 | 1 |
| `categories` | 8 个分类（从原 14 族合并重组） | 8 |
| `jobs` | 至少 12 条示例岗位 | ≥12 |
| `destinations` | 至少 18 条去向记录 | ≥18 |
| `salaries` | 至少 19 条薪资记录 | ≥19 |
| `interviews` | 至少 8 条转型者访谈 | ≥8 |
| `assessment_items` | 模块 A(4) + B(36) + C(24) + D(18) + E(5) | 87 |
| `assessment_weights` | 8 分类 × 60 题（B+C）= 480 格 | 480 |
| `benchmarks` | school_scores + tier_benchmarks + recommendation_rules | 3 |
| `risk_rules` | 至少 6 条风险规则（预聘制、量化考核、年龄门槛、薪资模糊、非升即走、安家费模糊） | ≥6 |
| `conflict_rules` | 至少 6 条冲突规则（见 §7.5.9） | ≥6 |
| `values_questions` | 18 项价值观各 1-2 条追问模板 | ≥18 |

---

*模块 9 结束。下一模块：模块 11（技术架构详述）。*

---

## 模块 11：技术架构详述

> **本章定位**：在模块 1 锁定技术栈的基础上，展开系统架构设计、前后端分层、API 规范、安全策略、部署方案和开发工作流。本章面向后端开发者和 DevOps。

---

### 11.1 系统架构总览

```
                        用户浏览器
                            │
                    HTTPS (TLS 1.2+)
                            │
                    ┌───────▼────────┐
                    │     Nginx      │
                    │  静态文件服务    │
                    │  反向代理 :3000 │
                    └───────┬────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
         /assets/*     /api/*         /* (SPA)
         静态文件      Fastify       index.html
         (Nginx直返)   :3000          (前端路由)
                            │
              ┌─────────────┼─────────────┐
              │             │             │
         Routes层      Services层     Prisma ORM
         (参数校验)     (业务逻辑)      (数据访问)
                                        │
                                   ┌────▼────┐
                                   │  MySQL  │
                                   │  8.0+   │
                                   └─────────┘
```

**前端**：Vite 构建的 React SPA，构建产物为纯静态文件（HTML + JS + CSS）。Nginx 直接响应静态文件请求，`/api/*` 路径代理到 Fastify。

**后端**：Fastify 提供 RESTful API。Prisma ORM 连接 MySQL。JWT 中间件保护 `/api/admin/*` 路由。

---

### 11.2 前端架构

#### 11.2.1 目录分层

```
src/
  main.tsx              ← 入口：挂载 <App/>
  App.tsx               ← 根组件：react-router <Routes>
  pages/                ← 页面组件（一个路由对应一个文件）
    admin/              ← 管理后台页面（共用 AdminLayout）
  components/           ← 共享 UI 组件
    assessment/         ← 评估工具专用组件
    threshold/          ← 门槛评估专用组件
    warning/            ← 职位预警专用组件
    career/             ← 职业地图专用组件
    jobs/               ← 岗位精选专用组件
    insights/           ← 职业数据专用组件
    admin/              ← 管理后台共享组件
    shared/             ← 跨模块通用组件（Nav, Footer, StatusBadge…）
  hooks/                ← 自定义 Hook
  utils/                ← 纯函数（算法、格式化、校验）
  types/                ← TypeScript 类型定义
  data/                 ← 静态数据常量（前端降级用）
  styles/               ← Tailwind 配置 + 全局 CSS
```

#### 11.2.2 路由设计

基于 react-router v6 的 `createBrowserRouter`。路由配置集中在一个 `src/router.tsx` 文件中：

```typescript
const router = createBrowserRouter([
  { path: "/",                     element: <HomePage /> },
  { path: "/careers",              element: <MapPage /> },
  { path: "/careers/:id",          element: <CategoryDetailPage /> },
  { path: "/jobs",                 element: <JobsPage /> },
  { path: "/jobs/:id",             element: <JobDetailPage /> },
  { path: "/insights",             element: <InsightsPage /> },
  { path: "/tools",                element: <ToolsHubPage /> },
  { path: "/tools/threshold",      element: <ThresholdPage /> },
  { path: "/tools/threshold/result", element: <ThresholdResultPage /> },
  { path: "/tools/warning",        element: <WarningPage /> },
  { path: "/tools/assessment",     element: <AssessmentPage /> },
  { path: "/tools/assessment/result", element: <AssessmentResultPage /> },
  { path: "/login",                element: <LoginPage /> },
  { path: "/me",                   element: <UserCenterPage /> },
  {
    path: "/admin",
    element: <AdminGuard><AdminLayout /></AdminGuard>,
    children: [
      { index: true,               element: <AdminDashboardPage /> },
      { path: "categories",        element: <AdminCategoriesPage /> },
      { path: "jobs",              element: <AdminJobsPage /> },
      { path: "insights",          element: <AdminInsightsPage /> },
      { path: "weights",           element: <AdminWeightsPage /> },
      { path: "rules",             element: <AdminRulesPage /> },
      { path: "benchmarks",        element: <AdminBenchmarksPage /> },
      { path: "users",             element: <AdminUsersPage /> },
    ]
  },
]);
```

`AdminGuard` 组件逻辑：
- 从 `localStorage` 读取 JWT Token
- 若无 Token → `navigate("/login?redirect=" + currentPath)`
- 若有 Token → 解码检查 `exp` 是否过期 → 过期则清除并重定向 → 未过期渲染 `<Outlet/>`

#### 11.2.3 状态管理分布

| 数据 | 存储位置 | 理由 |
|---|---|---|
| 登录态（JWT Token） | `localStorage` | 跨页面 + 跨会话持久化 |
| 评估作答进度（模块 A-E 的答案） | Zustand store | 跨 5 个模块页面共享，组件重渲染可控 |
| 门槛评估表单 | `useState` + sessionStorage 降级 | 单页面内使用，不跨路由 |
| 搜索结果/筛选状态 | `useState` | 单页面内使用 |
| API 响应缓存 | TanStack Query（可选，V1 不强制） | 减少重复请求 |

#### 11.2.4 构建配置

`vite.config.ts` 关键配置：

```typescript
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": "/src",                  // import ... from "@/utils/scoring"
    }
  },
  server: {
    port: 5173,
    proxy: {
      "/api": "http://localhost:3000"  // 开发时代理到本地后端
    }
  },
  build: {
    outDir: "dist",
    sourcemap: false,                // 生产环境不输出 sourcemap
  }
});
```

---

### 11.3 后端架构

#### 11.3.1 目录分层

```
server/
  src/
    index.ts            ← 入口：创建 Fastify 实例 + 注册插件 + 启动监听
    config.ts            ← 环境变量读取 + 配置对象导出
    plugins/             ← Fastify 插件
      auth.ts            ← JWT 校验 + req.user 注入
      cors.ts            ← CORS 配置
      swagger.ts         ← OpenAPI 文档生成（可选，V1 不强制）
    routes/              ← 路由定义（按资源分组）
      careers.ts         ← /api/careers/*
      jobs.ts            ← /api/jobs/*
      threshold.ts       ← /api/tools/threshold/*
      warning.ts         ← /api/tools/warning/*
      assessment.ts      ← /api/tools/assessment/*
      insights.ts        ← /api/insights/*
      admin/             ← /api/admin/*
        categories.ts
        jobs.ts
        insights.ts
        weights.ts
        rules.ts
        benchmarks.ts
        users.ts
    services/            ← 业务逻辑层
      scoring.ts         ← 复用前端 src/utils/scoring.ts（路径别名）
      matching.ts        ← 复用前端 src/utils/matching.ts
      jdAnalyzer.ts      ← 复用前端 src/utils/jdAnalyzer.ts
    lib/
      prisma.ts          ← PrismaClient 单例
      jwt.ts             ← Token 签发/校验
  prisma/
    schema.prisma        ← 数据库 Schema
    migrations/          ← 迁移文件
  package.json
  tsconfig.json
```

#### 11.3.2 Fastify 实例配置

```typescript
// server/src/index.ts
import Fastify from "fastify";
import cors from "@fastify/cors";
import { authPlugin } from "./plugins/auth";
import careerRoutes from "./routes/careers";
import jobRoutes from "./routes/jobs";
// ... 其余路由导入

const app = Fastify({ logger: true });

// 全局中间件
await app.register(cors, { origin: process.env.CORS_ORIGIN || "*" });
await app.register(authPlugin);           // 注入 req.user

// 公开路由
await app.register(careerRoutes, { prefix: "/api/careers" });
await app.register(jobRoutes, { prefix: "/api/jobs" });
await app.register(thresholdRoutes, { prefix: "/api/tools/threshold" });
await app.register(warningRoutes, { prefix: "/api/tools/warning" });
await app.register(assessmentRoutes, { prefix: "/api/tools/assessment" });
await app.register(insightsRoutes, { prefix: "/api/insights" });

// Admin 路由（JWT + role 校验）
await app.register(adminRoutes, { prefix: "/api/admin" });

const port = parseInt(process.env.PORT || "3000");
await app.listen({ port, host: "0.0.0.0" });
```

#### 11.3.3 前后端代码共享

后端 `server/src/services/` 中的算法函数通过 TypeScript 路径别名直接引用前端 `src/utils/` 中的纯函数，保证**同一段算法代码在前端降级和后端 API 中运行结果完全一致**。

```typescript
// server/tsconfig.json
{
  "compilerOptions": {
    "paths": {
      "@shared/*": ["../src/*"]      // 引用前端代码
    }
  }
}
```

注意事项：被共享的函数不能引用浏览器 API（`window`、`document`、`localStorage`），只能使用 Node.js 和浏览器共通的 JavaScript 标准库。模块 7 中所有算法函数已满足此要求。

---

### 11.4 API 设计规范

#### 11.4.1 命名约定

| 规则 | 示例 |
|---|---|
| 资源名用复数名词 | `/api/jobs` 非 `/api/job` |
| 嵌套资源表示归属 | `/api/admin/careers/categories/:id` |
| 动作通过 HTTP Method 表达 | `POST`=创建, `PUT`=更新, `DELETE`=删除 |
| 查询参数用于筛选和分页 | `?type=学术&region=北京&page=1` |

#### 11.4.2 统一响应格式

```typescript
// 成功
{ "data": T }

// 列表
{ "data": T[], "total": number, "page": number }

// 错误
{ "error": "ERROR_CODE", "message": "人类可读的描述" }
```

#### 11.4.3 错误码

| HTTP 状态码 | 含义 | 示例场景 |
|---|---|---|
| 200 | 成功 | — |
| 400 | 请求参数错误 | 必填字段缺失、数值类型错误 |
| 401 | 未认证 | 缺少 Token 或 Token 过期 |
| 403 | 无权限 | 非 Admin 访问 `/api/admin/*` |
| 404 | 资源不存在 | 无效的分类 ID 或岗位 ID |
| 500 | 服务端错误 | 数据库连接失败、算法异常 |

---

### 11.5 安全架构

#### 11.5.1 管理员认证

- 登录接口 `POST /api/auth/login` 接受 `{ username, password }`，校验通过后返回 JWT Token（有效期 24 小时）
- Token 负载：`{ userId, username, role }`
- Token 存储在前端 `localStorage`，每次 Admin API 请求通过 `Authorization: Bearer <token>` 头发送
- 后端 `authPlugin` 对 `/api/admin/*` 路径强制校验 Token + role=admin

#### 11.5.2 输入校验

Fastify 默认对路由 Schema 定义做 JSON Schema 校验（Fastify 原生能力）。每个路由定义 `body` 和 `query` 的 JSON Schema，Fastify 自动拒绝不符合 Schema 的请求并返回 400。

#### 11.5.3 密码存储

管理员密码使用 `bcrypt` 哈希存储，不存储明文。验证时使用 `bcrypt.compare()`。

#### 11.5.4 HTTPS

生产环境 Nginx 配置 SSL 证书（Let's Encrypt 或阿里云/腾讯云免费证书），所有 HTTP 请求 301 重定向到 HTTPS。

---

### 11.6 部署架构

#### 11.6.1 服务器软件栈

| 组件 | 版本 | 端口 | 管理方式 |
|---|---|---|---|
| Nginx | 1.24+ | 80/443 | systemd |
| Node.js | 20 LTS | 3000（仅 localhost） | PM2 |
| MySQL | 8.0+ | 3306（仅 localhost） | systemd |

#### 11.6.2 Nginx 配置要点

```nginx
server {
    listen 443 ssl;
    server_name beyond-academia.cn;

    root /var/www/phd-career/dist;

    # 静态文件
    location /assets/ { expires 1y; add_header Cache-Control "public, immutable"; }

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SPA 回退——非 /api/ 非静态文件 → index.html
    location / {
        try_files $uri /index.html;
    }
}
```

#### 11.6.3 PM2 配置

```javascript
// ecosystem.config.cjs
module.exports = {
  apps: [{
    name: "phd-career-api",
    script: "./server/dist/index.js",
    instances: 1,                      // V1 单实例
    env: {
      NODE_ENV: "production",
      PORT: 3000,
      DATABASE_URL: "mysql://user:pass@localhost:3306/phd_career",
      JWT_SECRET: process.env.JWT_SECRET,
      CORS_ORIGIN: "https://beyond-academia.cn",
    }
  }]
};
```

#### 11.6.4 CI/CD（GitHub Actions）

```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20" }
      - run: npm ci
      - run: npm run build          # 前端构建 → dist/
      - run: cd server && npm ci && npm run build  # 后端构建
      - name: rsync to server
        run: |
          rsync -avz dist/ deploy@server:/var/www/phd-career/dist/
          rsync -avz server/dist/ deploy@server:/var/www/phd-career/server/dist/
      - name: restart PM2
        run: ssh deploy@server "pm2 reload phd-career-api"
```

---

### 11.7 开发工作流

#### 11.7.1 本地启动

```bash
# Terminal 1: 后端
cd server && npm run dev          # tsx watch → localhost:3000

# Terminal 2: 前端
npm run dev                       # Vite → localhost:5173 (proxy /api → :3000)

# 数据库
docker run -d --name mysql-dev -e MYSQL_ROOT_PASSWORD=dev -e MYSQL_DATABASE=phd_career -p 3306:3306 mysql:8.0
npx prisma migrate dev
npx prisma db seed
```

#### 11.7.2 技术栈版本锁定

| 包 | 版本 | 说明 |
|---|---|---|
| react | ^18.3 | LTS |
| react-dom | ^18.3 | — |
| react-router-dom | ^6.26 | — |
| vite | ^5.4 | — |
| typescript | ^5.5 | — |
| tailwindcss | ^3.4 | — |
| zustand | ^4.5 | — |
| recharts | ^2.12 | — |
| lucide-react | ^0.44 | — |
| fastify | ^4.28 | — |
| @fastify/cors | ^9.0 | — |
| prisma | ^5.19 | — |
| @prisma/client | ^5.19 | — |
| jsonwebtoken | ^9.0 | — |
| bcrypt | ^5.1 | — |
| mysql2 | ^3.11 | Prisma 的 MySQL driver |

---

*模块 11 结束。*

---

## 附录 A：API 清单总览

| 编号 | Method | Path | 用途 | 需认证 | 所属章节 |
|---|---|---|---|---|---|
| API-01 | `GET` | `/api/careers/categories` | 获取 8 个分类列表 | 否 | §7.1.9 |
| API-02 | `GET` | `/api/careers/categories/:id` | 获取单个分类详情 | 否 | §7.1.9 |
| API-03 | `GET` | `/api/jobs` | 岗位列表（支持 `?type=&region=&q=`） | 否 | §7.2.9 |
| API-04 | `GET` | `/api/jobs/:id` | 岗位详情 | 否 | §7.2.9 |
| API-05 | `GET` | `/api/insights/destinations` | 去向数据列表（支持 `?q=`） | 否 | §7.6 |
| API-06 | `GET` | `/api/insights/salaries` | 薪资数据列表（支持 `?family=&year=`） | 否 | §7.6 |
| API-07 | `GET` | `/api/insights/interviews` | 访谈列表 | 否 | §7.6 |
| API-08 | `POST` | `/api/tools/threshold/evaluate` | 提交门槛评估表单，返回结果 | 否 | §7.3.10 |
| API-09 | `POST` | `/api/tools/warning/scan` | 提交 JD 文本，返回风险报告 | 否 | §7.4.8 |
| API-10 | `POST` | `/api/tools/assessment/evaluate` | 提交评估答案，返回匹配结果 | 否 | §7.5.15 |
| API-11 | `POST` | `/api/auth/login` | 管理员登录，返回 JWT | 否 | §11.5.1 |
| API-12 | `GET/POST/PUT/DELETE` | `/api/admin/careers/categories` | 分类 CRUD | **Admin JWT** | §7.7.6 |
| API-13 | `GET/POST/PUT/DELETE` | `/api/admin/jobs` | 岗位 CRUD | **Admin JWT** | §7.7.6 |
| API-14 | `GET/POST/PUT/DELETE` | `/api/admin/insights/*` | 职业数据 CRUD | **Admin JWT** | §7.7.6 |
| API-15 | `GET/PUT` | `/api/admin/weights` | 权重矩阵查看/批量更新 | **Admin JWT** | §7.7.6 |
| API-16 | `GET/POST/PUT/DELETE` | `/api/admin/rules` | 风险规则 CRUD | **Admin JWT** | §7.7.6 |
| API-17 | `GET/PUT` | `/api/admin/benchmarks` | 基准值查看/更新 | **Admin JWT** | §7.7.6 |
| API-18 | `GET/PUT` | `/api/admin/users` | 用户列表/角色修改 | **Admin JWT** | §7.7.6 |

**全局约定**：
- 统一响应格式：成功 `{ "data": T }` 或 `{ "data": T[], "total": number }`，错误 `{ "error": "ERROR_CODE", "message": "..." }`
- 统一错误码：400（参数错误）、401（未认证）、403（无权限）、404（不存在）、500（服务端错误）
- 管理端列表接口支持 `?page=&limit=&sort=` 分页排序参数

---

## 附录 B：枚举字典

| 枚举名 | 使用位置 | 合法值 |
|---|---|---|
| `DisciplineMajor` | ThresholdForm, AssessmentModuleA | `"经管法"`, `"人文"`, `"理工"`, `"医学"`, `"艺术与其他"` |
| `SchoolTier` | ThresholdForm | `"C9"`, `"985"`, `"211"`, `"省属"`, `"海外名校"`, `"其他"` |
| `BachelorTier` | ThresholdForm | `"C9"`, `"985"`, `"211"`, `"普通本科"`, `"民办"`, `"海外"` |
| `TargetTier` | ThresholdForm | `"顶尖 (C9 / 海外世界一流)"`, `"985 高校"`, `"211 高校"`, `"省属重点"`, `"一般本科"` |
| `TargetLevel` | ThresholdForm | `"讲师 / 助理教授"`, `"副教授（预聘）"`, `"副教授（长聘）"` |
| `CompareStatus` | ThresholdResult, ComparisonRow | `"pass"`, `"warn"`, `"fail"` |
| `RiskLevel` | RiskRule, DetectedRisk | `"高"`, `"中"`, `"低"` |
| `WarningStage` | WarningPage | `"input"`, `"scanning"`, `"result"` |
| `ThresholdStage` | ThresholdPage | `"input"`, `"loading"`, `"result"` |
| `InsightsTab` | InsightsPage | `"destination"`, `"salary"`, `"interviews"` |
| `UserRole` | User | `"user"`, `"admin"` |
| `CategoryId` | Category, AssessmentWeight | `1`~`8`（1=学术支撑, 2=党政管理, 3=社会智库, 4=科技企业, 5=文化出版, 6=医疗健康, 7=军警文职, 8=其他） |
| `AssessmentModule` | AssessmentItem | `"A"`, `"B"`, `"C"`, `"D"`, `"E"` |
| `ScaleType` | AssessmentItem | `"likert5"`, `"single_choice"`, `"rank_select"` |
| `PlaceholderLevel` | §6.7 | `"L1"`（路由占位）, `"L2"`（UI完整+Mock）, `"L3"`（完整实现） |

---

## 附录 C：数据库索引补充

以下索引定义应在执行 `CREATE TABLE` 后追加。Prisma Schema 文件（`server/prisma/schema.prisma`）中已包含等价 `@@index` 声明。

```sql
-- jobs
CREATE INDEX idx_jobs_type       ON jobs(type);
CREATE INDEX idx_jobs_region     ON jobs(region);
CREATE INDEX idx_jobs_active     ON jobs(active);
CREATE INDEX idx_jobs_category   ON jobs(category_id);
CREATE INDEX idx_jobs_type_region ON jobs(type, region);

-- assessment_items
CREATE INDEX idx_items_module    ON assessment_items(module);

-- assessment_weights（高频查询：每次匹配计算需全量读取）
CREATE INDEX idx_weights_item     ON assessment_weights(item_id);
CREATE INDEX idx_weights_category ON assessment_weights(category_id);

-- assessment_results
CREATE INDEX idx_results_user     ON assessment_results(user_id);

-- destinations
CREATE INDEX idx_dest_type        ON destinations(type);
CREATE INDEX idx_dest_year        ON destinations(year);

-- salaries
CREATE INDEX idx_salaries_family  ON salaries(family);
CREATE INDEX idx_salaries_year    ON salaries(year);

-- risk_rules（只查 active=true 的规则）
CREATE INDEX idx_risk_active      ON risk_rules(active);

-- conflict_rules
CREATE INDEX idx_conflict_cat     ON conflict_rules(category_id);

-- values_questions
CREATE INDEX idx_values_vid       ON values_questions(value_id);
```

**索引设计说明**：
- `assessment_weights` 的联合唯一约束 `(item_id, category_id)` 自带索引效果，无需额外建联合索引
- `jobs` 表 `(type, region)` 联合索引用于岗位列表页的组合筛选（最高频查询场景）
- `benchmarks` 表只有 3 行，不需要索引
- `interviews` 表 V1 只有 8 行，不需要索引

---

*附录结束。文档当前版本 V0.9。*
